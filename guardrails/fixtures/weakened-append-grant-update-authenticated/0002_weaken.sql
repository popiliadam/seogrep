-- SYNTHETIC WEAKENING - never applied. Layered on top of fixtures/healthy by
-- check-guards-selftest.sh. check-append-only.sh MUST go RED on this.
grant update on public.credit_ledger to authenticated;
