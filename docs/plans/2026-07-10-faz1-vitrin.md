# Faz 1 — Vitrin + Docs + Waitlist Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ranklens'in pazarlama vitrini (landing + pricing + how-it-works + legal taslakları), Fumadocs docs hub v1 ve çalışan waitlist'i (Resend contact + PostHog event) — hepsi lokal prod build'de Lighthouse ≥90 kanıtıyla, feature branch + PR akışında.

**Architecture:** apps/web (Next.js 15 App Router) içinde `(marketing)` route group + `/docs` Fumadocs alanı. Waitlist iş mantığı `packages/core/src/waitlist/`'te framework'süz port/adapter deseniyle yaşar (ContactStore=Resend Audiences, AnalyticsClient=PostHog capture — ikisi de fetch-tabanlı ince adapter, testlerde fixture'lı fake). apps/web yalnız ince bir route handler + form component taşır.

**Tech Stack:** Next.js 15 (mevcut) · Tailwind CSS v4 (`@tailwindcss/postcss`) · Fumadocs v16 (`fumadocs-ui@^16.11.2`, `fumadocs-core@^16.11.2`, `fumadocs-mdx@^15.1.0`) · zod@^4 (packages/core) · @lhci/cli@^0.15 (root dev) · vitest (mevcut). Yeni runtime bağımlılığı SADECE zod; Resend/PostHog SDK'sı YOK (fetch adapter).

## Global Constraints

- Marka: **Ranklens** · domain copy'de **ranklens.app** (DNS/satın alma İNSANDA — koda domain bağlama işi yok, yalnız metin/metadata).
- Tüm site copy'si **İngilizce** (spec D4).
- **Uydurma metrik / müşteri yorumu / logo YOK** (CLAUDE.md NEVER #7). Chat demo örnek verisi "Illustrative example" etiketiyle verilir; ölçülmemiş hız/başarı iddiası yazılmaz.
- Fiyat/kredi rakamları spec §3'ten HARFİYEN alınır, "Beta" rozetiyle; değiştirmek insan onayı ister (NEVER #6). Testler bu rakamları string olarak pinler.
- Test/CI'da paralı API'ye gerçek çağrı = 0 (NEVER #5). Resend/PostHog yalnız enjekte edilmiş `fetchFn` fixture'larıyla test edilir. Gerçek smoke (`pnpm waitlist:smoke`) yalnız insan anahtar sağlayınca elle koşulur.
- Yeni bağımlılıklar (zod, fumadocs-*, tailwind, @types/mdx, @lhci/cli) MIT/Apache-2.0 — hakem lisans kontrolü yapar (contract.md).
- Node **>=22** (Task 0'da sabitlenir), pnpm 11, `pnpm-workspace.yaml`'daki `allowBuilds` anahtarı pnpm 11'de DOĞRUDUR (v10.26+; `onlyBuiltDependencies`'in halefi — pnpm.io/settings). Değiştirme; teyit yorumu ekle.
- Her commit <200 satır hedefi (NEVER #10); task toplam diff >400 satır ise hakem Fable.
- Kapı: her task sonunda `bash guardrails/verify.sh` → `VERIFY: PASS` çıktısı zorunlu.
- UI task'larında (2,3,4,5a,5b) hakem PASS sonrası ŞEF verify-change skill + Claude Browser kanıtı üretir (dev server'da gerçek etkileşim, konsolda 0 hata).
- Immutability, `console.log` yasak (`console.error` catch içinde serbest), dosya <400 satır, fonksiyon <50 satır (global coding-style).
- MCP endpoint/URL şekli UYDURULMAZ (NEVER #9): docs ve copy'de kişisel MCP URL'i her zaman `YOUR_MCP_URL` placeholder'ı + "available from your dashboard at launch" ifadesiyle geçer.

## İş ↔ Task ↔ PR haritası

| PLAN.md işi | done_when | Task'lar | PR |
|---|---|---|---|
| İş C: Waitlist | verify yeşil · test e-postası kayıt id'siyle · PostHog waitlist_signup | T1 (çekirdek+API+form), T3'te sayfaya gömme | PR2 (T1), PR3 (gömme) |
| İş A: Landing+pricing+how-it-works | verify yeşil · lhci perf/a11y/SEO ≥90 · gerçek marka, uydurma metrik yok | T2 (kabuk), T3 (landing), T4 (pricing+hiw+legal) | PR3 (T2+T3), PR4 (T4) |
| İş B: Docs hub v1 | verify yeşil · /docs build'de statik · nav spec §4 birebir (Tools Reference hariç — Faz 3) | T5a (wiring), T5b (içerik) | PR5 |
| Hijyen (Faz 0 devri) | engines>=22 · CI permissions · allowBuilds teyidi | T0 | PR1 |

**PR yığını (stacked):** PR1 `feat/faz1-hygiene` → main · PR2 `feat/faz1-waitlist` → PR1 branch · PR3 `feat/faz1-landing` → PR2 branch · PR4 `feat/faz1-pages` → PR3 branch · PR5 `feat/faz1-docs` → PR4 branch. İnsan PR1'i merge edince GitHub PR2'nin base'ini main'e çevirir (otomatik), sırayla devam. main origin'den 2 docs commit'i öndedir (push insan kuyruğunda) — PR1 diff'inde bu 2 commit görünür, PR gövdesinde not düşülür.

**Dispatch:** T0=Sonnet · T1,T2,T3,T4,T5a=Opus · T5b=Sonnet. Hakem: taze Opus; toplam diff >400 satırsa (T3 ve T5b'de beklenir) taze Fable. Para kodu (ledger/webhook/auth/RLS) bu fazda YOK.

**İnsan kuyruğuna eklenecekler (şef, faz sonunda PLAN.md'ye):** Resend hesabı + Audience oluştur + `RESEND_API_KEY`/`RESEND_AUDIENCE_ID` · PostHog projesi + `POSTHOG_API_KEY` (EU host) · anahtarlar gelince `pnpm waitlist:smoke` gerçek kanıtı · Vercel deploy onayı · PR merge'leri.

## Dosya yapısı (hedef)

```
apps/web/
├── app/
│   ├── layout.tsx                    # html/body + metadata + globals.css (+T5a: RootProvider)
│   ├── globals.css                   # Tailwind v4 + marka token'ları (+T5a: fumadocs css)
│   ├── robots.ts · sitemap.ts · icon.svg
│   ├── (marketing)/
│   │   ├── layout.tsx                # SiteHeader + SiteFooter kabuğu
│   │   ├── page.tsx                  # Landing (T3)
│   │   ├── page.test.tsx             # Landing render testleri
│   │   ├── pricing/page.tsx (+test)  # T4
│   │   ├── how-it-works/page.tsx     # T4
│   │   ├── terms/page.tsx · privacy/page.tsx  # T4 (DRAFT etiketli)
│   ├── docs/  layout.tsx · [[...slug]]/page.tsx      # T5a
│   ├── api/
│   │   ├── search/route.ts           # Fumadocs arama (T5a)
│   │   └── waitlist/route.ts (+test) # T1
├── components/
│   ├── site-header.tsx · site-footer.tsx            # T2
│   ├── waitlist-form.tsx (+test)     # T1 (client component)
│   ├── chat-demo.tsx                 # T3 (illustrative, etiketli)
│   └── pricing-table.tsx             # T4
├── lib/
│   ├── waitlist-deps.ts              # env → adapter kablolama (T1)
│   ├── source.ts · layout.shared.tsx # T5a
├── content/docs/**                   # T5a iskelet + T5b tam içerik
├── mdx-components.tsx · source.config.ts             # T5a
├── postcss.config.mjs                # T2
packages/core/src/waitlist/
├── waitlist.ts                       # şema + port'lar + joinWaitlist (T1)
├── resend-store.ts · posthog-analytics.ts · memory.ts # T1
├── waitlist.test.ts · adapters.test.ts               # T1
scripts/waitlist-smoke.mjs            # T1 (elle, gerçek anahtarla)
lighthouserc.json                     # T3 (+T4/T5a URL ekler)
.claude/launch.json                   # T2 (Claude Browser dev server)
goals/lighthouse-90.md · landing-live.md · docs-static.md · waitlist-works.md  # ilgili iş PASS olunca şef commit'ler
```

---

### Task 0: Faz 0 devreden hijyen (engines, CI permissions, allowBuilds teyidi)

**Model:** Sonnet · **Hakem:** Opus · **Branch:** `feat/faz1-hygiene` (main'den)

**Files:**
- Modify: `package.json` (root — engines)
- Modify: `.github/workflows/ci.yml` (permissions bloğu)
- Modify: `pnpm-workspace.yaml` (yorum satırı — teyit notu)

**Interfaces:**
- Consumes: —
- Produces: Node >=22 garantisi (apps/mcp `--experimental-strip-types` Node 22 ister); CI token'ı en az yetkiyle çalışır. Sonraki task'lar bu branch üstüne stack'lenir.

- [ ] **Step 1: engines'i >=22 yap**

`package.json` içinde:
```json
"engines": { "node": ">=22" }
```
(mevcut değer `">=20"`).

- [ ] **Step 2: CI'ya workflow-level permissions ekle**

`.github/workflows/ci.yml` en üste, `on:` bloğundan sonra:
```yaml
permissions:
  contents: read
```

- [ ] **Step 3: allowBuilds teyit yorumu**

`pnpm-workspace.yaml` içinde `allowBuilds:` satırının üstüne:
```yaml
# pnpm >=10.26 anahtarı (onlyBuiltDependencies'in halefi) — pnpm.io/settings, teyit: 2026-07-10
```

- [ ] **Step 4: Kapıyı çalıştır**

Run: `bash guardrails/verify.sh`
Expected: son satır `VERIFY: PASS`

- [ ] **Step 5: Commit**

```bash
git add package.json .github/workflows/ci.yml pnpm-workspace.yaml
git commit -m "chore: node>=22 engines, CI permissions bloğu, allowBuilds teyit notu"
```

---

### Task 1: Waitlist çekirdeği + API + form (İş C'nin kod tamamı)

**Model:** Opus · **Hakem:** Opus (diff >400 satırsa Fable) · **Branch:** `feat/faz1-waitlist` (T0 branch'inden)

**Files:**
- Create: `packages/core/src/waitlist/waitlist.ts`, `packages/core/src/waitlist/memory.ts`, `packages/core/src/waitlist/resend-store.ts`, `packages/core/src/waitlist/posthog-analytics.ts`
- Create: `packages/core/src/waitlist/waitlist.test.ts`, `packages/core/src/waitlist/adapters.test.ts`
- Modify: `packages/core/src/index.ts` (re-export), `packages/core/package.json` (zod dependency)
- Create: `apps/web/lib/waitlist-deps.ts`, `apps/web/app/api/waitlist/route.ts`, `apps/web/app/api/waitlist/route.test.ts`
- Create: `apps/web/components/waitlist-form.tsx`, `apps/web/components/waitlist-form.test.tsx`
- Create: `scripts/waitlist-smoke.mjs`
- Modify: `.env.example` (RESEND_API_KEY, RESEND_AUDIENCE_ID, POSTHOG_API_KEY, POSTHOG_HOST), root `package.json` (`waitlist:smoke` script)

**Interfaces:**
- Consumes: —
- Produces (sonraki task'lar bunlara güvenir):
  - `@pseo/core` → `joinWaitlist(raw: unknown, deps: WaitlistDeps): Promise<WaitlistResult>`; `WaitlistDeps = { store: ContactStore; analytics: AnalyticsClient }`; `WaitlistResult = { ok: true; id: string; alreadyExisted: boolean }`; hata: `WaitlistValidationError` (message alanı kullanıcıya gösterilebilir). Adapters: `createResendContactStore({apiKey, audienceId, fetchFn?})`, `createPostHogAnalytics({apiKey, host?, fetchFn?})`, `createMemoryContactStore()`, `createCapturingAnalytics()`.
  - `POST /api/waitlist` gövde `{ email: string, source?: string, website?: string }` → 200 `{ ok: true, id, alreadyExisted }` · 400 `{ ok: false, error }` · 503 `{ ok: false, error }` (prod'da anahtar yoksa). `website` honeypot: doluysa sessizce 200 `{ ok: true }` döner, hiçbir yan etki üretmez.
  - `<WaitlistForm source="hero" />` client component — Task 3 landing'e gömer.

- [ ] **Step 1: Çekirdek testini yaz (RED)**

`packages/core/package.json` dependencies'e `"zod": "^4.4.3"` ekle, `pnpm install` çalıştır. Sonra `packages/core/src/waitlist/waitlist.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { joinWaitlist, WaitlistValidationError } from "./waitlist.js";
import { createMemoryContactStore } from "./memory.js";
import { createCapturingAnalytics } from "./memory.js";

const deps = () => {
  const store = createMemoryContactStore();
  const analytics = createCapturingAnalytics();
  return { store, analytics };
};

describe("joinWaitlist", () => {
  it("creates a contact and captures waitlist_signup with hashed distinct id", async () => {
    const d = deps();
    const result = await joinWaitlist({ email: "Ada@Example.com", source: "hero" }, d);
    expect(result.ok).toBe(true);
    expect(result.id).toMatch(/^wl_/);
    expect(result.alreadyExisted).toBe(false);
    expect(d.store.contacts).toEqual([{ id: result.id, email: "ada@example.com", source: "hero" }]);
    const [event] = d.analytics.captured;
    expect(event.name).toBe("waitlist_signup");
    expect(event.distinctId).toMatch(/^[a-f0-9]{64}$/); // sha256(email)
    expect(event.properties).toEqual({ email_domain: "example.com", source: "hero" });
  });

  it("rejects invalid email with WaitlistValidationError", async () => {
    await expect(joinWaitlist({ email: "not-an-email" }, deps())).rejects.toBeInstanceOf(
      WaitlistValidationError,
    );
  });

  it("reports alreadyExisted on duplicate email without failing", async () => {
    const d = deps();
    const first = await joinWaitlist({ email: "ada@example.com" }, d);
    const second = await joinWaitlist({ email: "ada@example.com" }, d);
    expect(second).toEqual({ ok: true, id: first.id, alreadyExisted: true });
    expect(d.store.contacts).toHaveLength(1);
  });

  it("still succeeds when analytics capture throws", async () => {
    const d = deps();
    const failing = { capture: async () => { throw new Error("posthog down"); } };
    const result = await joinWaitlist({ email: "ada@example.com" }, { store: d.store, analytics: failing });
    expect(result.ok).toBe(true);
  });

  it("defaults source to 'landing'", async () => {
    const d = deps();
    await joinWaitlist({ email: "ada@example.com" }, d);
    expect(d.store.contacts[0].source).toBe("landing");
  });
});
```

- [ ] **Step 2: RED'i doğrula**

Run: `pnpm --filter @pseo/core exec vitest run src/waitlist`
Expected: FAIL — modüller yok ("Cannot find module ./waitlist.js").

- [ ] **Step 3: Çekirdeği yaz**

`packages/core/src/waitlist/waitlist.ts`:
```typescript
import { createHash } from "node:crypto";
import { z } from "zod";

export const waitlistSignupSchema = z.object({
  email: z.string().trim().toLowerCase().max(254).pipe(z.email()),
  source: z.string().trim().min(1).max(100).default("landing"),
});

export type WaitlistSignup = z.infer<typeof waitlistSignupSchema>;

export interface ContactStore {
  createContact(input: WaitlistSignup): Promise<{ id: string; alreadyExisted: boolean }>;
}

export interface AnalyticsClient {
  capture(event: {
    name: string;
    distinctId: string;
    properties?: Record<string, string>;
  }): Promise<void>;
}

export interface WaitlistDeps {
  store: ContactStore;
  analytics: AnalyticsClient;
}

export interface WaitlistResult {
  ok: true;
  id: string;
  alreadyExisted: boolean;
}

export class WaitlistValidationError extends Error {}

export async function joinWaitlist(raw: unknown, deps: WaitlistDeps): Promise<WaitlistResult> {
  const parsed = waitlistSignupSchema.safeParse(raw);
  if (!parsed.success) {
    throw new WaitlistValidationError("Please enter a valid email address.");
  }
  const signup = parsed.data;
  const { id, alreadyExisted } = await deps.store.createContact(signup);
  try {
    await deps.analytics.capture({
      name: "waitlist_signup",
      distinctId: createHash("sha256").update(signup.email).digest("hex"),
      properties: { email_domain: signup.email.split("@")[1] ?? "", source: signup.source },
    });
  } catch (error) {
    console.error("waitlist analytics capture failed:", error);
  }
  return { ok: true, id, alreadyExisted };
}
```
Not: zod v4'te `z.string().email()` deprecate — `z.email()` kullanılıyor; `.pipe(z.email())` trim+lowercase sonrası doğrular. İşçi: zod v4 API'sini typecheck'e göre gerekirse `z.email()` top-level şekline uyarlar (davranış: trim → lowercase → email doğrulaması).

`packages/core/src/waitlist/memory.ts`:
```typescript
import type { AnalyticsClient, ContactStore, WaitlistSignup } from "./waitlist.js";

interface MemoryContact {
  id: string;
  email: string;
  source: string;
}

export interface MemoryContactStore extends ContactStore {
  contacts: readonly MemoryContact[];
}

export function createMemoryContactStore(): MemoryContactStore {
  let contacts: readonly MemoryContact[] = [];
  return {
    get contacts() {
      return contacts;
    },
    async createContact(input: WaitlistSignup) {
      const existing = contacts.find((c) => c.email === input.email);
      if (existing) return { id: existing.id, alreadyExisted: true };
      const contact = { id: `wl_${contacts.length + 1}`, email: input.email, source: input.source };
      contacts = [...contacts, contact];
      return { id: contact.id, alreadyExisted: false };
    },
  };
}

interface CapturedEvent {
  name: string;
  distinctId: string;
  properties?: Record<string, string>;
}

export interface CapturingAnalytics extends AnalyticsClient {
  captured: readonly CapturedEvent[];
}

export function createCapturingAnalytics(): CapturingAnalytics {
  let captured: readonly CapturedEvent[] = [];
  return {
    get captured() {
      return captured;
    },
    async capture(event: CapturedEvent) {
      captured = [...captured, event];
    },
  };
}
```

`packages/core/src/index.ts`'e ekle:
```typescript
export * from "./waitlist/waitlist.js";
export * from "./waitlist/memory.js";
export * from "./waitlist/resend-store.js";
export * from "./waitlist/posthog-analytics.js";
```
(mevcut export'lara dokunma; dosya boşsa yalnız bunlar.)

- [ ] **Step 4: GREEN'i doğrula**

Run: `pnpm --filter @pseo/core exec vitest run src/waitlist`
Expected: 5 test PASS (adapters.test.ts henüz yok).

- [ ] **Step 5: Commit**

```bash
git add packages/core
git commit -m "feat: waitlist çekirdeği — joinWaitlist + memory adapter'ları (TDD)"
```

- [ ] **Step 6: Adapter testlerini yaz (RED)**

`packages/core/src/waitlist/adapters.test.ts`:
```typescript
import { describe, expect, it } from "vitest";
import { createResendContactStore } from "./resend-store.js";
import { createPostHogAnalytics } from "./posthog-analytics.js";

function fetchStub(responses: Array<{ status: number; body: unknown }>) {
  const calls: Array<{ url: string; init: RequestInit }> = [];
  const fetchFn = (async (url: string | URL, init?: RequestInit) => {
    calls.push({ url: String(url), init: init ?? {} });
    const next = responses[Math.min(calls.length - 1, responses.length - 1)];
    return new Response(JSON.stringify(next.body), { status: next.status });
  }) as typeof fetch;
  return { calls, fetchFn };
}

describe("createResendContactStore", () => {
  const cfg = { apiKey: "re_test", audienceId: "aud_1" };

  it("POSTs contact to the audience and returns the id", async () => {
    const { calls, fetchFn } = fetchStub([{ status: 201, body: { id: "cont_123" } }]);
    const store = createResendContactStore({ ...cfg, fetchFn });
    const result = await store.createContact({ email: "ada@example.com", source: "hero" });
    expect(result).toEqual({ id: "cont_123", alreadyExisted: false });
    expect(calls[0].url).toBe("https://api.resend.com/audiences/aud_1/contacts");
    expect(calls[0].init.method).toBe("POST");
    expect(calls[0].init.headers).toMatchObject({ Authorization: "Bearer re_test" });
    expect(JSON.parse(String(calls[0].init.body))).toMatchObject({ email: "ada@example.com" });
  });

  it("on duplicate (409) falls back to GET by email", async () => {
    const { calls, fetchFn } = fetchStub([
      { status: 409, body: { name: "conflict" } },
      { status: 200, body: { id: "cont_dup" } },
    ]);
    const store = createResendContactStore({ ...cfg, fetchFn });
    const result = await store.createContact({ email: "ada@example.com", source: "hero" });
    expect(result).toEqual({ id: "cont_dup", alreadyExisted: true });
    expect(calls[1].url).toBe("https://api.resend.com/audiences/aud_1/contacts/ada%40example.com");
  });

  it("throws a friendly error on 401", async () => {
    const { fetchFn } = fetchStub([{ status: 401, body: { message: "invalid key" } }]);
    const store = createResendContactStore({ ...cfg, fetchFn });
    await expect(store.createContact({ email: "a@b.co", source: "x" })).rejects.toThrow(
      /Resend request failed \(401\)/,
    );
  });
});

describe("createPostHogAnalytics", () => {
  it("POSTs capture with api key, event, distinct_id and properties", async () => {
    const { calls, fetchFn } = fetchStub([{ status: 200, body: { status: 1 } }]);
    const analytics = createPostHogAnalytics({ apiKey: "phc_test", fetchFn });
    await analytics.capture({ name: "waitlist_signup", distinctId: "abc", properties: { source: "hero" } });
    expect(calls[0].url).toBe("https://eu.i.posthog.com/capture/");
    const body = JSON.parse(String(calls[0].init.body));
    expect(body).toMatchObject({
      api_key: "phc_test",
      event: "waitlist_signup",
      distinct_id: "abc",
      properties: { source: "hero" },
    });
  });

  it("throws on non-2xx so joinWaitlist can swallow and log", async () => {
    const { fetchFn } = fetchStub([{ status: 500, body: {} }]);
    const analytics = createPostHogAnalytics({ apiKey: "phc_test", fetchFn });
    await expect(analytics.capture({ name: "e", distinctId: "d" })).rejects.toThrow(
      /PostHog capture failed \(500\)/,
    );
  });
});
```

- [ ] **Step 7: RED'i doğrula**

Run: `pnpm --filter @pseo/core exec vitest run src/waitlist`
Expected: adapters.test.ts FAIL ("Cannot find module ./resend-store.js").

- [ ] **Step 8: Adapter'ları yaz**

`packages/core/src/waitlist/resend-store.ts`:
```typescript
import type { ContactStore, WaitlistSignup } from "./waitlist.js";

interface ResendConfig {
  apiKey: string;
  audienceId: string;
  fetchFn?: typeof fetch;
}

const BASE_URL = "https://api.resend.com";

export function createResendContactStore(config: ResendConfig): ContactStore {
  const fetchFn = config.fetchFn ?? fetch;
  const headers = {
    Authorization: `Bearer ${config.apiKey}`,
    "Content-Type": "application/json",
  };

  async function getExisting(email: string): Promise<{ id: string }> {
    const response = await fetchFn(
      `${BASE_URL}/audiences/${config.audienceId}/contacts/${encodeURIComponent(email)}`,
      { headers },
    );
    if (!response.ok) throw new Error(`Resend request failed (${response.status})`);
    const data = (await response.json()) as { id: string };
    return { id: data.id };
  }

  return {
    async createContact(input: WaitlistSignup) {
      const response = await fetchFn(`${BASE_URL}/audiences/${config.audienceId}/contacts`, {
        method: "POST",
        headers,
        body: JSON.stringify({ email: input.email, unsubscribed: false }),
      });
      if (response.status === 409) {
        const existing = await getExisting(input.email);
        return { id: existing.id, alreadyExisted: true };
      }
      if (!response.ok) throw new Error(`Resend request failed (${response.status})`);
      const data = (await response.json()) as { id: string };
      return { id: data.id, alreadyExisted: false };
    },
  };
}
```
Not (işçiye): Resend duplicate contact davranışı hesap ayarına göre 409 veya 200 dönebilir — 2xx dönerse `alreadyExisted: false` kabulü zararsızdır (idempotent UX). Endpoint yolları Resend API referansındandır (`POST /audiences/:id/contacts`, `GET /audiences/:id/contacts/:email`); şüphen varsa context7'den doğrula, URI'yi DEĞİŞTİRME gerekmedikçe.

`packages/core/src/waitlist/posthog-analytics.ts`:
```typescript
import type { AnalyticsClient } from "./waitlist.js";

interface PostHogConfig {
  apiKey: string;
  host?: string;
  fetchFn?: typeof fetch;
}

export function createPostHogAnalytics(config: PostHogConfig): AnalyticsClient {
  const fetchFn = config.fetchFn ?? fetch;
  const host = (config.host ?? "https://eu.i.posthog.com").replace(/\/$/, "");
  return {
    async capture(event) {
      const response = await fetchFn(`${host}/capture/`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          api_key: config.apiKey,
          event: event.name,
          distinct_id: event.distinctId,
          properties: event.properties ?? {},
          timestamp: new Date().toISOString(),
        }),
      });
      if (!response.ok) throw new Error(`PostHog capture failed (${response.status})`);
    },
  };
}
```

- [ ] **Step 9: GREEN + commit**

Run: `pnpm --filter @pseo/core exec vitest run src/waitlist`
Expected: 10 test PASS.
```bash
git add packages/core/src/waitlist packages/core/src/index.ts
git commit -m "feat: Resend contact + PostHog capture fetch adapter'ları (fixture testli)"
```

- [ ] **Step 10: API route testini yaz (RED)**

`apps/web/app/api/waitlist/route.test.ts`:
```typescript
import { beforeEach, describe, expect, it } from "vitest";
import { POST } from "./route";
import { resetWaitlistDepsForTest, setWaitlistDepsForTest } from "../../../lib/waitlist-deps";
import { createCapturingAnalytics, createMemoryContactStore } from "@pseo/core";

function jsonRequest(body: unknown): Request {
  return new Request("http://localhost/api/waitlist", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  });
}

describe("POST /api/waitlist", () => {
  let store: ReturnType<typeof createMemoryContactStore>;

  beforeEach(() => {
    store = createMemoryContactStore();
    setWaitlistDepsForTest({ store, analytics: createCapturingAnalytics() });
    return () => resetWaitlistDepsForTest();
  });

  it("returns 200 with the record id for a valid signup", async () => {
    const response = await POST(jsonRequest({ email: "ada@example.com", source: "hero" }));
    expect(response.status).toBe(200);
    await expect(response.json()).resolves.toMatchObject({ ok: true, id: "wl_1" });
    expect(store.contacts).toHaveLength(1);
  });

  it("returns 400 for an invalid email", async () => {
    const response = await POST(jsonRequest({ email: "nope" }));
    expect(response.status).toBe(400);
    await expect(response.json()).resolves.toMatchObject({ ok: false });
  });

  it("silently accepts honeypot submissions without side effects", async () => {
    const response = await POST(jsonRequest({ email: "bot@spam.com", website: "https://spam" }));
    expect(response.status).toBe(200);
    expect(store.contacts).toHaveLength(0);
  });
});
```

- [ ] **Step 11: RED'i doğrula**

Run: `pnpm --filter @pseo/web exec vitest run app/api`
Expected: FAIL (route ve lib yok). Not: `@pseo/core` web'in dependency'si değilse `apps/web/package.json` dependencies'e `"@pseo/core": "workspace:*"` ekle + `pnpm install`. Turbo dışında salt vitest koşarken core dist'i gerekir: önce `pnpm --filter @pseo/core build` çalıştır (verify.sh bunu turbo `^build` ile kendisi halleder).

- [ ] **Step 12: lib + route'u yaz**

`apps/web/lib/waitlist-deps.ts`:
```typescript
import {
  createCapturingAnalytics,
  createMemoryContactStore,
  createPostHogAnalytics,
  createResendContactStore,
  type WaitlistDeps,
} from "@pseo/core";

let testOverride: WaitlistDeps | null = null;
let devFallback: WaitlistDeps | null = null;

export function setWaitlistDepsForTest(deps: WaitlistDeps): void {
  testOverride = deps;
}

export function resetWaitlistDepsForTest(): void {
  testOverride = null;
}

export function getWaitlistDeps(): WaitlistDeps | null {
  if (testOverride) return testOverride;
  const { RESEND_API_KEY, RESEND_AUDIENCE_ID, POSTHOG_API_KEY, POSTHOG_HOST } = process.env;
  if (RESEND_API_KEY && RESEND_AUDIENCE_ID && POSTHOG_API_KEY) {
    return {
      store: createResendContactStore({ apiKey: RESEND_API_KEY, audienceId: RESEND_AUDIENCE_ID }),
      analytics: createPostHogAnalytics({ apiKey: POSTHOG_API_KEY, host: POSTHOG_HOST }),
    };
  }
  if (process.env.NODE_ENV !== "production") {
    devFallback ??= { store: createMemoryContactStore(), analytics: createCapturingAnalytics() };
    return devFallback;
  }
  return null;
}
```

`apps/web/app/api/waitlist/route.ts`:
```typescript
import { joinWaitlist, WaitlistValidationError } from "@pseo/core";
import { getWaitlistDeps } from "../../../lib/waitlist-deps";

export const runtime = "nodejs";

export async function POST(request: Request): Promise<Response> {
  let body: Record<string, unknown>;
  try {
    body = (await request.json()) as Record<string, unknown>;
  } catch {
    return Response.json({ ok: false, error: "Invalid request body." }, { status: 400 });
  }
  if (typeof body.website === "string" && body.website.length > 0) {
    return Response.json({ ok: true });
  }
  const deps = getWaitlistDeps();
  if (!deps) {
    return Response.json(
      { ok: false, error: "Waitlist is not configured yet. Please try again soon." },
      { status: 503 },
    );
  }
  try {
    const result = await joinWaitlist({ email: body.email, source: body.source ?? "landing" }, deps);
    return Response.json(result);
  } catch (error) {
    if (error instanceof WaitlistValidationError) {
      return Response.json({ ok: false, error: error.message }, { status: 400 });
    }
    console.error("waitlist signup failed:", error);
    return Response.json({ ok: false, error: "Something went wrong. Please try again." }, { status: 500 });
  }
}
```

- [ ] **Step 13: GREEN + commit**

Run: `pnpm --filter @pseo/web exec vitest run app/api`
Expected: 3 test PASS.
```bash
git add apps/web/app/api apps/web/lib/waitlist-deps.ts apps/web/package.json pnpm-lock.yaml
git commit -m "feat: /api/waitlist route — honeypot, env kablolama, dev memory fallback"
```

- [ ] **Step 14: Form component testi (RED) + implementasyon**

`apps/web/components/waitlist-form.test.tsx`:
```tsx
import { cleanup, fireEvent, render, screen, waitFor } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { WaitlistForm } from "./waitlist-form";

describe("WaitlistForm", () => {
  afterEach(() => {
    cleanup();
    vi.unstubAllGlobals();
  });

  it("submits the email and shows the success state", async () => {
    const fetchMock = vi.fn(async () =>
      new Response(JSON.stringify({ ok: true, id: "wl_1", alreadyExisted: false }), { status: 200 }),
    );
    vi.stubGlobal("fetch", fetchMock);
    render(<WaitlistForm source="hero" />);
    fireEvent.change(screen.getByLabelText(/email/i), { target: { value: "ada@example.com" } });
    fireEvent.click(screen.getByRole("button", { name: /join the waitlist/i }));
    await waitFor(() => expect(screen.getByText(/you're on the list/i)).toBeDefined());
    const [, init] = fetchMock.mock.calls[0] as [string, RequestInit];
    expect(JSON.parse(String(init.body))).toMatchObject({ email: "ada@example.com", source: "hero" });
  });

  it("shows the server error message on 400", async () => {
    vi.stubGlobal("fetch", vi.fn(async () =>
      new Response(JSON.stringify({ ok: false, error: "Please enter a valid email address." }), { status: 400 }),
    ));
    render(<WaitlistForm source="hero" />);
    fireEvent.change(screen.getByLabelText(/email/i), { target: { value: "x@y.z" } });
    fireEvent.click(screen.getByRole("button", { name: /join the waitlist/i }));
    await waitFor(() => expect(screen.getByText(/valid email/i)).toBeDefined());
  });
});
```

`apps/web/components/waitlist-form.tsx` (Tailwind sınıfları T2'de gelecek token'ları kullanır; T2'den önce sınıflar etkisizdir, zararı yok):
```tsx
"use client";

import { useId, useState } from "react";

type FormState =
  | { status: "idle" }
  | { status: "loading" }
  | { status: "success"; alreadyExisted: boolean }
  | { status: "error"; message: string };

export function WaitlistForm({ source }: { source: string }) {
  const inputId = useId();
  const [state, setState] = useState<FormState>({ status: "idle" });

  async function handleSubmit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault();
    const form = new FormData(event.currentTarget);
    setState({ status: "loading" });
    try {
      const response = await fetch("/api/waitlist", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          email: String(form.get("email") ?? ""),
          source,
          website: String(form.get("website") ?? ""),
        }),
      });
      const data = (await response.json()) as { ok: boolean; alreadyExisted?: boolean; error?: string };
      if (!response.ok || !data.ok) {
        setState({ status: "error", message: data.error ?? "Something went wrong. Please try again." });
        return;
      }
      setState({ status: "success", alreadyExisted: data.alreadyExisted ?? false });
    } catch {
      setState({ status: "error", message: "Network error. Please try again." });
    }
  }

  if (state.status === "success") {
    return (
      <p role="status" className="text-base font-medium">
        {state.alreadyExisted
          ? "You're already on the list — we'll be in touch at launch."
          : "You're on the list. We'll email you when Ranklens opens."}
      </p>
    );
  }

  return (
    <form onSubmit={handleSubmit} className="flex w-full max-w-md flex-col gap-2 sm:flex-row">
      <label htmlFor={inputId} className="sr-only">
        Email address
      </label>
      <input
        id={inputId}
        name="email"
        type="email"
        required
        autoComplete="email"
        placeholder="you@company.com"
        className="h-11 flex-1 rounded-lg border border-ink/20 bg-paper px-4 text-base outline-none focus:border-accent"
      />
      <input type="text" name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" className="hidden" />
      <button
        type="submit"
        disabled={state.status === "loading"}
        className="h-11 rounded-lg bg-ink px-5 text-base font-semibold text-paper transition-opacity disabled:opacity-60"
      >
        {state.status === "loading" ? "Joining…" : "Join the waitlist"}
      </button>
      {state.status === "error" ? (
        <p role="alert" className="text-sm text-red-600 sm:basis-full">
          {state.message}
        </p>
      ) : null}
    </form>
  );
}
```

Run: `pnpm --filter @pseo/web exec vitest run components`
Expected: 2 test PASS.
```bash
git add apps/web/components
git commit -m "feat: WaitlistForm client component — durum makinesi + honeypot"
```

- [ ] **Step 15: Smoke script + env örnekleri**

`scripts/waitlist-smoke.mjs` (yalnız elle, gerçek anahtarla; CI'da ASLA):
```javascript
// Kullanım: RESEND_API_KEY=... RESEND_AUDIENCE_ID=... POSTHOG_API_KEY=... \
//   node scripts/waitlist-smoke.mjs test+faz1@ornek-adres.com
import { createPostHogAnalytics, createResendContactStore, joinWaitlist } from "@pseo/core";

const email = process.argv[2];
const { RESEND_API_KEY, RESEND_AUDIENCE_ID, POSTHOG_API_KEY, POSTHOG_HOST } = process.env;
if (!email || !RESEND_API_KEY || !RESEND_AUDIENCE_ID || !POSTHOG_API_KEY) {
  console.error("Eksik: e-posta argümanı + RESEND_API_KEY + RESEND_AUDIENCE_ID + POSTHOG_API_KEY");
  process.exit(1);
}
const result = await joinWaitlist(
  { email, source: "smoke" },
  {
    store: createResendContactStore({ apiKey: RESEND_API_KEY, audienceId: RESEND_AUDIENCE_ID }),
    analytics: createPostHogAnalytics({ apiKey: POSTHOG_API_KEY, host: POSTHOG_HOST }),
  },
);
console.error(`KANIT — Resend contact id: ${result.id} (alreadyExisted=${result.alreadyExisted}); PostHog event: waitlist_signup gönderildi. PostHog arayüzünde Activity'den doğrula.`);
```
Root `package.json` scripts'e: `"waitlist:smoke": "node scripts/waitlist-smoke.mjs"`.

`.env.example`'a ekle (değerler BOŞ kalır):
```
RESEND_API_KEY=
RESEND_AUDIENCE_ID=
POSTHOG_API_KEY=
POSTHOG_HOST=https://eu.i.posthog.com
```

- [ ] **Step 16: Kapı + commit**

Run: `bash guardrails/verify.sh`
Expected: `VERIFY: PASS`
```bash
git add scripts/waitlist-smoke.mjs package.json .env.example
git commit -m "feat: waitlist smoke script + env anahtar adları"
```

---

### Task 2: Tailwind v4 + marka token'ları + marketing kabuğu

**Model:** Opus (frontend-design skill bağlamıyla) · **Hakem:** Opus · **Branch:** `feat/faz1-landing` (T1 branch'inden; T3 ile aynı branch/PR)

**Files:**
- Create: `apps/web/postcss.config.mjs`, `apps/web/app/globals.css`
- Modify: `apps/web/app/layout.tsx` (metadata + globals import), `apps/web/package.json` (tailwind dev deps)
- Create: `apps/web/app/(marketing)/layout.tsx`, `apps/web/components/site-header.tsx`, `apps/web/components/site-footer.tsx`
- Move: `apps/web/app/page.tsx` → `apps/web/app/(marketing)/page.tsx` (içerik T3'te yeniden yazılacak; şimdilik mevcut içerik taşınır), `apps/web/app/page.test.tsx` → `apps/web/app/(marketing)/page.test.tsx`
- Create: `apps/web/app/robots.ts`, `apps/web/app/sitemap.ts`, `apps/web/app/icon.svg`
- Create: `.claude/launch.json`

**Interfaces:**
- Consumes: —
- Produces: Tailwind token'ları — `bg-paper`, `text-ink`, `text-accent`, `border-ink/20` gibi sınıflar tüm sonraki UI task'larında kullanılır (`--color-ink`, `--color-paper`, `--color-accent` @theme değişkenleri). `(marketing)/layout.tsx` header+footer kabuğu; header nav: Docs → `/docs`, Pricing → `/pricing`, How it works → `/how-it-works`, CTA "Join waitlist" → `/#waitlist`. `SITE_URL = "https://ranklens.app"` sabiti `lib/site.ts`'te.

- [ ] **Step 1: Tailwind v4 kur**

`apps/web/package.json` devDependencies'e: `"tailwindcss": "^4.3.2"`, `"@tailwindcss/postcss": "^4.3.2"`. `pnpm install`.

`apps/web/postcss.config.mjs`:
```javascript
export default {
  plugins: { "@tailwindcss/postcss": {} },
};
```

`apps/web/app/globals.css`:
```css
@import "tailwindcss";

@theme {
  --color-ink: oklch(0.21 0.02 265);
  --color-paper: oklch(0.985 0.004 95);
  --color-accent: oklch(0.72 0.14 70); /* lens amber */
  --color-accent-strong: oklch(0.55 0.15 60);
  --font-sans: ui-sans-serif, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
}

body {
  background: var(--color-paper);
  color: var(--color-ink);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
}
```
(Karar: sistem font yığını — build'de ağ isteği yok, deterministik, sıfır CLS. Fumadocs preset import'ları T5a'da bu dosyaya eklenecek.)

- [ ] **Step 2: Root layout + metadata**

`apps/web/lib/site.ts`:
```typescript
export const SITE_URL = "https://ranklens.app";
export const SITE_NAME = "Ranklens";
export const SITE_DESCRIPTION =
  "Ranklens is an MCP server for SEO: add your personal URL to Claude, Cursor, or Windsurf and run crawls, audits, quick-win discovery, and Search Console analysis in plain language.";
```

`apps/web/app/layout.tsx`:
```tsx
import type { Metadata } from "next";
import type { ReactNode } from "react";
import { SITE_DESCRIPTION, SITE_NAME, SITE_URL } from "../lib/site";
import "./globals.css";

export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: { default: `${SITE_NAME} — SEO analysis inside your AI assistant`, template: `%s · ${SITE_NAME}` },
  description: SITE_DESCRIPTION,
  openGraph: {
    siteName: SITE_NAME,
    type: "website",
    url: SITE_URL,
  },
  twitter: { card: "summary" },
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>{children}</body>
    </html>
  );
}
```
(`suppressHydrationWarning` T5a'daki fumadocs RootProvider tema attribute'u için şimdiden zararsız.)

- [ ] **Step 3: Header/Footer + marketing layout**

`apps/web/components/site-header.tsx`:
```tsx
import Link from "next/link";

const NAV = [
  { href: "/how-it-works", label: "How it works" },
  { href: "/pricing", label: "Pricing" },
  { href: "/docs", label: "Docs" },
] as const;

export function SiteHeader() {
  return (
    <header className="sticky top-0 z-40 border-b border-ink/10 bg-paper/90 backdrop-blur">
      <div className="mx-auto flex h-14 w-full max-w-5xl items-center justify-between px-4">
        <Link href="/" className="text-lg font-bold tracking-tight">
          Ranklens
        </Link>
        <nav aria-label="Main" className="flex items-center gap-5">
          {NAV.map((item) => (
            <Link key={item.href} href={item.href} className="hidden text-sm text-ink/80 hover:text-ink sm:block">
              {item.label}
            </Link>
          ))}
          <Link
            href="/#waitlist"
            className="rounded-lg bg-ink px-3 py-1.5 text-sm font-semibold text-paper hover:opacity-90"
          >
            Join waitlist
          </Link>
        </nav>
      </div>
    </header>
  );
}
```

`apps/web/components/site-footer.tsx`:
```tsx
import Link from "next/link";

export function SiteFooter() {
  return (
    <footer className="border-t border-ink/10 py-10 text-sm text-ink/70">
      <div className="mx-auto flex w-full max-w-5xl flex-col gap-4 px-4 sm:flex-row sm:items-center sm:justify-between">
        <p>© 2026 Ranklens · Your site data is never used to train AI models.</p>
        <nav aria-label="Footer" className="flex gap-4">
          <Link href="/docs" className="hover:text-ink">Docs</Link>
          <Link href="/pricing" className="hover:text-ink">Pricing</Link>
          <Link href="/terms" className="hover:text-ink">Terms</Link>
          <Link href="/privacy" className="hover:text-ink">Privacy</Link>
        </nav>
      </div>
    </footer>
  );
}
```

`apps/web/app/(marketing)/layout.tsx`:
```tsx
import type { ReactNode } from "react";
import { SiteFooter } from "../../components/site-footer";
import { SiteHeader } from "../../components/site-header";

export default function MarketingLayout({ children }: { children: ReactNode }) {
  return (
    <div className="flex min-h-dvh flex-col">
      <SiteHeader />
      <main className="flex-1">{children}</main>
      <SiteFooter />
    </div>
  );
}
```

`git mv apps/web/app/page.tsx "apps/web/app/(marketing)/page.tsx"` ve `git mv apps/web/app/page.test.tsx "apps/web/app/(marketing)/page.test.tsx"` (test import yolunu `./page` olarak koru; kırıksa düzelt).

- [ ] **Step 4: robots/sitemap/icon**

`apps/web/app/robots.ts`:
```typescript
import type { MetadataRoute } from "next";
import { SITE_URL } from "../lib/site";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: "/" },
    sitemap: `${SITE_URL}/sitemap.xml`,
  };
}
```

`apps/web/app/sitemap.ts`:
```typescript
import type { MetadataRoute } from "next";
import { SITE_URL } from "../lib/site";

const ROUTES = ["", "/pricing", "/how-it-works", "/docs", "/terms", "/privacy"] as const;

export default function sitemap(): MetadataRoute.Sitemap {
  return ROUTES.map((route) => ({ url: `${SITE_URL}${route}` }));
}
```

`apps/web/app/icon.svg` (lens motifi — diyafram halkası + odak noktası):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="7" fill="#1b1e2e"/>
  <circle cx="16" cy="16" r="9" fill="none" stroke="#f0b34e" stroke-width="2.5"/>
  <circle cx="16" cy="16" r="3.5" fill="#f0b34e"/>
</svg>
```

`.claude/launch.json`:
```json
{
  "version": "0.0.1",
  "configurations": [
    { "name": "web", "runtimeExecutable": "pnpm", "runtimeArgs": ["--filter", "@pseo/web", "dev"], "port": 3000 }
  ]
}
```

- [ ] **Step 5: Kapı + commit**

Run: `bash guardrails/verify.sh`
Expected: `VERIFY: PASS` (page.test.tsx taşınmış haliyle geçmeli).
```bash
git add apps/web .claude/launch.json pnpm-lock.yaml
git commit -m "feat: Tailwind v4 + marka token'ları + marketing kabuğu (header/footer, robots/sitemap/icon)"
```
Not: bu commit >200 satıra yaklaşırsa globals+layout ve header/footer'ı iki commit'e böl.

---

### Task 3: Landing sayfası (hero + chat demo + features + trust + waitlist) + Lighthouse kanıtı

**Model:** Opus (frontend-design skill ZORUNLU bağlam) · **Hakem:** beklenen diff >400 satır → **Fable** · **Branch:** `feat/faz1-landing` (T2 devamı, aynı PR)

**Files:**
- Rewrite: `apps/web/app/(marketing)/page.tsx`, `apps/web/app/(marketing)/page.test.tsx`
- Create: `apps/web/components/chat-demo.tsx`
- Create: `lighthouserc.json` (root), root `package.json` `lighthouse` script, `@lhci/cli` root devDependency

**Interfaces:**
- Consumes: `WaitlistForm` (T1), token'lar + kabuk (T2)
- Produces: `/#waitlist` anchor'lı canlı landing; `pnpm lighthouse` komutu (T4/T5a URL listesini genişletir; goals/lighthouse-90.md bunu çağıracak)

**Copy (FİNAL — işçi bunu aynen kullanır; uydurma metrik eklemek FAIL):**
- Eyebrow: `Private beta — join the waitlist`
- H1: `Point a lens at your site.`
- Sub: `Ranklens turns the AI assistant you already use into an SEO analyst. Add one URL to Claude, Cursor, or Windsurf and run real crawls, audits, and Search Console analysis in plain language.`
- CTA altı güven satırı: `Free trial at launch: 200 credits, no card required.`
- Trust üçlüsü: (1) `Your data never trains AI models.` (2) `Bring your own AI subscription — credits only pay for analysis, never tokens.` (3) `Works without Search Console. Connect it only when you want deeper analysis.`
- Features başlığı: `A focused SEO toolkit, spoken in plain language` — 4 grup, spec §2.1'deki GERÇEK tool adlarıyla: Setup (`setup_project`, `connect_gsc`, `list_projects`, `get_credit_balance`) · Data (`crawl_site`, `pull_gsc_data`, `research_keywords`, `get_job_status`) · Discovery (`find_quick_wins`, `detect_cannibalization`, `analyze_content_decay`, `audit_onpage`, `audit_tech`, `audit_schema`) · Output (`generate_report`, `whats_next`).
- Waitlist bölümü (id="waitlist"): başlık `Be first through the lens.`, alt metin `We're opening Ranklens to the waitlist in small batches. Leave your email and we'll send your invite.`
- Chat demo etiketi: `Illustrative example — sample site, sample numbers.`

- [ ] **Step 1: Landing render testini güncelle (RED)**

`apps/web/app/(marketing)/page.test.tsx`:
```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import Page from "./page";

describe("landing page", () => {
  it("renders the brand h1 without placeholder text", () => {
    render(<Page />);
    expect(screen.getByRole("heading", { level: 1 }).textContent).toBe("Point a lens at your site.");
  });

  it("labels the chat demo as illustrative", () => {
    render(<Page />);
    expect(screen.getByText(/illustrative example — sample site, sample numbers/i)).toBeDefined();
  });

  it("has the waitlist section anchor", () => {
    render(<Page />);
    expect(document.getElementById("waitlist")).not.toBeNull();
  });

  it("mentions the real trial terms only", () => {
    render(<Page />);
    expect(screen.getByText(/200 credits, no card required/i)).toBeDefined();
  });
});
```

- [ ] **Step 2: RED'i doğrula**

Run: `pnpm --filter @pseo/web exec vitest run "app/(marketing)"`
Expected: FAIL (eski placeholder page içeriği).

- [ ] **Step 3: ChatDemo component**

`apps/web/components/chat-demo.tsx` — server component, saf CSS animasyonu (JS state yok → hidrasyon maliyeti sıfır):
```tsx
const TURNS = [
  { role: "user", text: "Audit example.com for SEO issues." },
  { role: "tool", text: "crawl_site → job c_42 started · up to 100 URLs · 20 credits" },
  { role: "tool", text: "get_job_status → crawl finished · 87 pages fetched" },
  { role: "tool", text: "audit_onpage → 12 missing meta descriptions · 3 broken internal links" },
  { role: "assistant", text: "Here's your prioritized fix list. Want the shareable report? (generate_report)" },
] as const;

export function ChatDemo() {
  return (
    <figure className="w-full max-w-xl rounded-2xl border border-ink/10 bg-white/60 p-4 shadow-sm">
      <ol className="flex flex-col gap-3">
        {TURNS.map((turn, index) => (
          <li
            key={turn.text}
            style={{ animationDelay: `${index * 900}ms` }}
            className={`chat-turn max-w-[85%] rounded-xl px-3 py-2 text-sm ${
              turn.role === "user"
                ? "self-end bg-ink text-paper"
                : turn.role === "tool"
                  ? "self-start border border-accent/40 bg-accent/10 font-mono text-[13px]"
                  : "self-start bg-ink/5"
            }`}
          >
            {turn.text}
          </li>
        ))}
      </ol>
      <figcaption className="mt-3 text-xs text-ink/60">
        Illustrative example — sample site, sample numbers.
      </figcaption>
    </figure>
  );
}
```
`globals.css`'e ekle:
```css
@keyframes chat-in {
  from { opacity: 0; transform: translateY(6px); }
  to { opacity: 1; transform: translateY(0); }
}
.chat-turn {
  opacity: 0;
  animation: chat-in 0.5s ease-out forwards;
}
@media (prefers-reduced-motion: reduce) {
  .chat-turn { animation: none; opacity: 1; }
}
```

- [ ] **Step 4: Landing page'i yaz**

`apps/web/app/(marketing)/page.tsx` — bölümler sırasıyla: hero (eyebrow + h1 + sub + `<WaitlistForm source="hero" />` + güven satırı + sağda/altta `<ChatDemo />`) · trust üçlüsü (3 kart, copy yukarıda) · features (4 grup kartı; her kartta grup adı, 1 cümle fayda, tool adları `<code>` ile) · how-it-works kısa şerit (3 adım: "Get your personal MCP URL" → "Paste it into your client" → "Ask in plain language" + `/how-it-works`'e link) · waitlist bölümü (`id="waitlist"`, başlık/alt metin yukarıda, `<WaitlistForm source="footer" />`). Sayfa başı metadata:
```tsx
export const metadata = { title: "SEO analysis inside your AI assistant" };
```
Tüm copy yukarıdaki FİNAL bloktan; h2'ler: `Built on trust, not dashboards` (trust) · `A focused SEO toolkit, spoken in plain language` (features) · `How it works` (şerit) · `Be first through the lens.` (waitlist). Ek serbest metin yazma; görsel hiyerarşi ve spacing frontend-design ilkeleriyle (max-w-5xl konteyner, bol beyaz alan, tek accent).

- [ ] **Step 5: GREEN + kapı + commit**

Run: `pnpm --filter @pseo/web exec vitest run "app/(marketing)"` → 4 PASS.
Run: `bash guardrails/verify.sh` → `VERIFY: PASS`.
```bash
git add apps/web
git commit -m "feat: Ranklens landing — hero, illustrative chat demo, features, waitlist bölümü"
```

- [ ] **Step 6: Lighthouse CI kur + kanıt üret**

Root `package.json` devDependencies'e `"@lhci/cli": "^0.15.1"`; scripts'e `"lighthouse": "pnpm --filter @pseo/web build && lhci autorun"`. `pnpm install`.

`lighthouserc.json` (root):
```json
{
  "ci": {
    "collect": {
      "startServerCommand": "pnpm --filter @pseo/web start",
      "startServerReadyPattern": "Ready",
      "url": ["http://localhost:3000/"],
      "numberOfRuns": 1,
      "settings": { "chromeFlags": "--headless=new" }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.9 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.9 }]
      }
    },
    "upload": { "target": "filesystem", "outputDir": ".lighthouseci" }
  }
}
```
`.gitignore`'a `.lighthouseci/` ekle.

Run: `pnpm lighthouse`
Expected: `All results processed!` ve assertion hatası YOK; skorları rapordan not et (kanıt: perf/a11y/SEO ≥ 0.9). FAIL ise skoru düşüren denetimi raporla ve düzelt (tipik: kontrast, meta description, image boyutu) — testi/eşiği GEVŞETME (NEVER #8 ruhu).

```bash
git add lighthouserc.json package.json pnpm-lock.yaml .gitignore
git commit -m "ci: Lighthouse CI (lokal) — landing için perf/a11y/SEO ≥ 0.9 assertion'ları"
```

---

### Task 4: /pricing + /how-it-works + legal taslakları + tam Lighthouse

**Model:** Opus · **Hakem:** Opus (diff >400 ise Fable) · **Branch:** `feat/faz1-pages` (T3 branch'inden)

**Files:**
- Create: `apps/web/app/(marketing)/pricing/page.tsx`, `apps/web/app/(marketing)/pricing/page.test.tsx`, `apps/web/components/pricing-table.tsx`
- Create: `apps/web/app/(marketing)/how-it-works/page.tsx`
- Create: `apps/web/app/(marketing)/terms/page.tsx`, `apps/web/app/(marketing)/privacy/page.tsx`
- Modify: `lighthouserc.json` (URL listesi)

**Interfaces:**
- Consumes: kabuk + token'lar (T2), `WaitlistForm` (T1)
- Produces: header/footer'daki tüm linkler çalışır (404 yok); `pnpm lighthouse` 3 marketing URL'ini de ölçer.

**Rakamlar (spec §3 — HARFİYEN, test pinler, değişiklik = insan onayı):**
Trial $0 kartsız 200 kredi (tek sefer) · Starter $19/ay 1.000 kredi/ay · Pro $49/ay 3.500 · Agency $149/ay 12.000 · Top-up $10→400, $25→1.100, $50→2.400. Kredi maliyetleri (taslak): GSC pull (90 gün) 5 · Site crawl (≤100 URL) 20 · Quick-win / cannibalization / decay taraması 10 · Tam on-page+tech audit 50 · Keyword research (100 kw) 25 · Aylık rapor 15. Politikalar: kullanılmayan kredi 1 ay devreder (tavan: plan kredisinin 2 katı) · trial e-posta doğrulaması ister · domain başına tek trial.

- [ ] **Step 1: Pricing testini yaz (RED)**

`apps/web/app/(marketing)/pricing/page.test.tsx`:
```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import Page from "./page";

describe("pricing page", () => {
  it("pins the draft package numbers from the spec", () => {
    render(<Page />);
    for (const text of ["$19", "$49", "$149", "1,000", "3,500", "12,000", "200 credits"]) {
      expect(screen.getAllByText(new RegExp(text.replace("$", "\\$"))).length).toBeGreaterThan(0);
    }
  });

  it("shows the beta badge and no popularity claims", () => {
    render(<Page />);
    expect(screen.getAllByText(/beta pricing/i).length).toBeGreaterThan(0);
    expect(screen.queryByText(/most popular/i)).toBeNull();
  });
});
```
Run: `pnpm --filter @pseo/web exec vitest run pricing` → FAIL (sayfa yok).

- [ ] **Step 2: Pricing sayfasını yaz**

`components/pricing-table.tsx`: `PLANS` sabit dizisi (name, price, credits, blurb, isTrial) + kart grid'i; her kartta CTA `Join the waitlist` → `/#waitlist`. `pricing/page.tsx`: başlıkta rozet `Beta pricing — numbers may change before launch`, plan grid'i, `What credits buy` tablosu (yukarıdaki 6 satır; caption: `Draft costs — we calibrate against real usage before launch.`), politikalar listesi (rollover/tavan, e-posta doğrulama, tek trial/domain), top-up satırı, altta `Prices in USD.` Metadata `title: "Pricing"`. "Most popular" benzeri popülerlik iddiası YOK (henüz müşteri yok — NEVER #7).

- [ ] **Step 3: GREEN + commit**

Run: `pnpm --filter @pseo/web exec vitest run pricing` → 2 PASS.
```bash
git add "apps/web/app/(marketing)/pricing" apps/web/components/pricing-table.tsx
git commit -m "feat: /pricing — spec §3 taslak rakamları beta rozetiyle, testle pinli"
```

- [ ] **Step 4: How-it-works sayfası**

`how-it-works/page.tsx` — numaralı 4 bölüm + 3 bilgi kartı; copy çerçevesi (aynen ya da çok yakın):
1. `Get your personal MCP URL` — `When your invite arrives, your dashboard gives you a personal MCP URL — one URL that carries your projects and credits.` (Zapier modeli; URL örneği YAZILMAZ — NEVER #9.)
2. `Paste it into the client you already use` — Claude Desktop, claude.ai, Claude Code, Cursor, Windsurf; `/docs/getting-started`e link.
3. `Ask in plain language` — `"Audit my site", "find quick wins", "why did clicks drop?" Ranklens runs the crawl or analysis and answers in your chat. Your first crawl works without Search Console.`
4. `Pay in credits, not seats` — kredi mantığı + `/pricing` link.
Bilgi kartları: `Long jobs never block your chat` (async job + `get_job_status`) · `Expensive runs ask first` (200 kredi üstü tahmin→onay — spec §2.2) · `Reports you can share` (public link'li HTML rapor, `powered by Ranklens` footer'ı). Metadata `title: "How it works"`. Süre/başarı yüzdesi gibi ölçülmemiş iddia YOK.

- [ ] **Step 5: Legal taslakları**

`terms/page.tsx` ve `privacy/page.tsx` — her ikisinin başında görünür rozet: `Draft — this document will be finalized before launch.` İçerik yalnız spec'ten türeyen gerçekler:
- Terms: beta hizmet tanımı (SEO analizi, kredi sistemi), kabul, kredi/ödeme bölümü placeholder değil kısa gerçek çerçeve (krediler analiz işlemleri için harcanır; fiyatlar /pricing'de, launch öncesi değişebilir), kabul edilebilir kullanım (yalnız sahibi/yetkilisi olduğun siteleri analiz et), sorumluluk reddi (beta, "as is"), fesih.
- Privacy: toplanan veri (waitlist e-postası; ileride hesap + crawl verisi), kullanım amacı, saklama (crawl ham verisi 90 gün; rapor çıktıları hesap ömrü; silme talebi = tam purge — spec §6.5), üçüncü taraflar (Resend — e-posta listesi; PostHog — ürün analitiği, EU host), `Your site data is never used to train AI models.`, GDPR/KVKK hakları (erişim/silme), iletişim: `via the waitlist email you used` (uydurma adres YOK).
Her sayfa metadata title; kısa tut (~60-90 satır/sayfa).

- [ ] **Step 6: Lighthouse URL'lerini genişlet + tam koşu**

`lighthouserc.json` `url` dizisini değiştir:
```json
"url": [
  "http://localhost:3000/",
  "http://localhost:3000/pricing",
  "http://localhost:3000/how-it-works"
]
```
Run: `pnpm lighthouse`
Expected: 3 URL × perf/a11y/SEO ≥ 0.9, assertion hatasız. Skorları not et (İş A kanıtı).

- [ ] **Step 7: Kapı + commit**

Run: `bash guardrails/verify.sh` → `VERIFY: PASS`
```bash
git add apps/web lighthouserc.json
git commit -m "feat: /how-it-works + legal taslakları (DRAFT rozetli) + lhci tam kapsam"
```

---

### Task 5a: Fumadocs v16 kablolaması (/docs iskeleti, statik üretim kanıtı)

**Model:** Opus · **Hakem:** Opus · **Branch:** `feat/faz1-docs` (T4 branch'inden)

**Files:**
- Modify: `apps/web/package.json` (fumadocs deps), `apps/web/next.config.ts`, `apps/web/app/globals.css`, `apps/web/app/layout.tsx` (RootProvider), `apps/web/tsconfig.json` (gerekirse `@/.source` path)
- Create: `apps/web/source.config.ts`, `apps/web/lib/source.ts`, `apps/web/lib/layout.shared.tsx`, `apps/web/mdx-components.tsx`
- Create: `apps/web/app/docs/layout.tsx`, `apps/web/app/docs/[[...slug]]/page.tsx`, `apps/web/app/api/search/route.ts`
- Create: `apps/web/content/docs/index.mdx`, `apps/web/content/docs/meta.json` (+ 1 örnek alt sayfa)

**Interfaces:**
- Consumes: T2 kabuğu (docs kendi DocsLayout'unu kullanır, marketing header'ı DEĞİL)
- Produces: `source` loader'ı ve content dizini — T5b yalnız `content/docs/**` MDX + meta.json ekler, kod dosyasına dokunmaz.

- [ ] **Step 1: Bağımlılıklar + config**

`apps/web/package.json`: dependencies'e `"fumadocs-core": "^16.11.2"`, `"fumadocs-ui": "^16.11.2"`, `"fumadocs-mdx": "^15.1.0"`; devDependencies'e `"@types/mdx": "^2.0.13"`. scripts'e `"postinstall": "fumadocs-mdx"`. `pnpm install` (allowBuilds gerekirse `fumadocs-mdx: true` satırı — pnpm uyarısına bak).

`apps/web/source.config.ts`:
```typescript
import { defineConfig, defineDocs } from "fumadocs-mdx/config";

export const docs = defineDocs({ dir: "content/docs" });

export default defineConfig();
```

`apps/web/next.config.ts`'i sarmala:
```typescript
import { createMDX } from "fumadocs-mdx/next";
import type { NextConfig } from "next";

const config: NextConfig = { reactStrictMode: true };

const withMDX = createMDX();

export default withMDX(config);
```
(mevcut config alanlarını KORU, yalnız sarmala.)

- [ ] **Step 2: source + layout + page + search**

ÖNEMLİ (işçiye): `pnpm --filter @pseo/web exec fumadocs-mdx` çalıştır → üretilen `.source/` klasörünün export şeklini AÇ VE BAK; aşağıdaki `@/.source` import'u üretilen gerçek entry ile uyuşmuyorsa (ör. `.source/server`), üretilene uyarla. Uydurma import bırakma; typecheck son hakem. `.source/` gitignore'a eklenir (üretilen dosya).

`apps/web/lib/source.ts`:
```typescript
import { docs } from "@/.source";
import { loader } from "fumadocs-core/source";

export const source = loader({
  baseUrl: "/docs",
  source: docs.toFumadocsSource(),
});
```

`apps/web/lib/layout.shared.tsx`:
```tsx
import type { BaseLayoutProps } from "fumadocs-ui/layouts/shared";

export function baseOptions(): BaseLayoutProps {
  return {
    nav: { title: "Ranklens" },
    links: [
      { text: "Pricing", url: "/pricing" },
      { text: "How it works", url: "/how-it-works" },
    ],
  };
}
```

`apps/web/app/layout.tsx` body içeriğini sarmala:
```tsx
import { RootProvider } from "fumadocs-ui/provider";
// ... mevcut metadata aynı kalır
<body>
  <RootProvider>{children}</RootProvider>
</body>
```

`apps/web/app/docs/layout.tsx`:
```tsx
import { DocsLayout } from "fumadocs-ui/layouts/docs";
import type { ReactNode } from "react";
import { baseOptions } from "../../lib/layout.shared";
import { source } from "../../lib/source";

export default function Layout({ children }: { children: ReactNode }) {
  return (
    <DocsLayout {...baseOptions()} tree={source.getPageTree()}>
      {children}
    </DocsLayout>
  );
}
```

`apps/web/app/docs/[[...slug]]/page.tsx`:
```tsx
import { source } from "../../../lib/source";
import { DocsBody, DocsDescription, DocsPage, DocsTitle } from "fumadocs-ui/page";
import { notFound } from "next/navigation";
import { getMDXComponents } from "../../../mdx-components";

export default async function Page(props: { params: Promise<{ slug?: string[] }> }) {
  const params = await props.params;
  const page = source.getPage(params.slug);
  if (!page) notFound();
  const MDX = page.data.body;
  return (
    <DocsPage toc={page.data.toc} full={page.data.full}>
      <DocsTitle>{page.data.title}</DocsTitle>
      <DocsDescription>{page.data.description}</DocsDescription>
      <DocsBody>
        <MDX components={getMDXComponents()} />
      </DocsBody>
    </DocsPage>
  );
}

export function generateStaticParams() {
  return source.generateParams();
}

export async function generateMetadata(props: { params: Promise<{ slug?: string[] }> }) {
  const params = await props.params;
  const page = source.getPage(params.slug);
  if (!page) notFound();
  return { title: page.data.title, description: page.data.description };
}
```
(fumadocs v16'da `DocsPage` import yolu `fumadocs-ui/page` yerine `fumadocs-ui/layouts/docs/page` olabilir — typecheck neyi kabul ediyorsa onu kullan, ikisini de dene.)

`apps/web/mdx-components.tsx`:
```tsx
import defaultMdxComponents from "fumadocs-ui/mdx";
import type { MDXComponents } from "mdx/types";

export function getMDXComponents(components?: MDXComponents): MDXComponents {
  return { ...defaultMdxComponents, ...components };
}
```

`apps/web/app/api/search/route.ts`:
```typescript
import { source } from "../../../lib/source";
import { createFromSource } from "fumadocs-core/search/server";

export const { GET } = createFromSource(source);
```

`globals.css` başına (tailwind import'undan sonra):
```css
@import "fumadocs-ui/css/neutral.css";
@import "fumadocs-ui/css/preset.css";
```
Fumadocs preset'i marketing token'larıyla çakışırsa (body rengi vb.) marketing stilleri `(marketing)` scope'unda kalacak şekilde düzelt — marketing sayfalarının görünümü DEĞİŞMEMELİ (T3 lhci kanıtı bozulmaz).

- [ ] **Step 3: İlk içerik + statik üretim kanıtı**

`apps/web/content/docs/index.mdx`:
```mdx
---
title: Ranklens Docs
description: What Ranklens is and how to get value from it in your first session.
---

Ranklens is a hosted MCP server for SEO analysis. You add your personal MCP URL
to the AI client you already use — Claude Desktop, claude.ai, Claude Code,
Cursor, or Windsurf — and run crawls, audits, and Search Console analysis by
asking in plain language. Credits pay for analysis work; your AI subscription
stays your own.

> **Beta note:** Ranklens is in private beta. Some details below will be
> finalized at launch.
```
`apps/web/content/docs/meta.json` (T5b tam listeyi getirir; şimdilik):
```json
{ "pages": ["index", "getting-started"] }
```
`apps/web/content/docs/getting-started/index.mdx` (kısa özet + beta notu) ve `getting-started/meta.json` → `{ "pages": ["index"] }`.

Run: `pnpm --filter @pseo/web build 2>&1 | tee /tmp/docs-build.log` sonra `grep -E "docs" /tmp/docs-build.log`
Expected: build çıktısında `/docs` ve `/docs/[[...slug]]` satırlarında `●` (SSG) veya `○` (Static) işareti — KANIT olarak build satırlarını hakem raporuna kopyala.

- [ ] **Step 4: Kapı + commit**

Run: `bash guardrails/verify.sh` → `VERIFY: PASS`
```bash
git add apps/web pnpm-lock.yaml .gitignore
git commit -m "feat: Fumadocs v16 docs hub kablolaması — /docs statik üretiliyor (SSG kanıtlı)"
```

---

### Task 5b: Docs içerik iskeleti — nav spec §4 birebir

**Model:** Sonnet · **Hakem:** beklenen diff >400 satır → **Fable** · **Branch:** `feat/faz1-docs` (T5a devamı, aynı PR)

**Files (hepsi Create, `apps/web/content/docs/` altında):**
- Modify: `meta.json` (kök nav)
- Create: `getting-started/claude-desktop.mdx`, `getting-started/claude-ai.mdx`, `getting-started/claude-code.mdx`, `getting-started/cursor.mdx`, `getting-started/windsurf.mdx` + `getting-started/meta.json` güncelle
- Create: `core-concepts/index.mdx`, `core-concepts/projects.mdx`, `core-concepts/credits.mdx`, `core-concepts/gsc-connection.mdx`, `core-concepts/data-retention.mdx`, `core-concepts/meta.json`
- Create: `recipes/index.mdx`, `recipes/first-site-audit.mdx`, `recipes/monthly-seo-routine.mdx`, `recipes/quick-wins-sprint.mdx`, `recipes/meta.json`
- Create: `billing-and-credits.mdx`, `security.mdx`, `faq.mdx`, `troubleshooting.mdx`

**Interfaces:**
- Consumes: T5a loader'ı (yalnız içerik ekler, koda dokunmaz)
- Produces: nav yapısı spec §4 sırasıyla birebir (Tools Reference HARİÇ — Faz 3'te zod şemalarından otomatik üretilecek, el yazımı yasak).

**Kök `meta.json` (nav sırası = spec §4 sırası):**
```json
{
  "pages": [
    "index",
    "getting-started",
    "core-concepts",
    "recipes",
    "billing-and-credits",
    "security",
    "faq",
    "troubleshooting"
  ]
}
```

**İçerik kuralları (her sayfa için):**
- Frontmatter: `title` + `description` zorunlu. Her sayfa 20-60 satır — iskelet, roman değil.
- Her sayfada beta callout: `> **Beta note:** ...` (kişisel MCP URL'i launch'ta dashboard'dan alınır).
- MCP URL DAİMA `YOUR_MCP_URL` placeholder'ı — gerçek endpoint uydurma (NEVER #9).
- Client kurulum adımları GERÇEK konvansiyonlarla (emin olunmayan UI adımı "check the client's MCP documentation" diye bağlanır, uydurulmaz):
  - Claude Code: `claude mcp add ranklens --transport http YOUR_MCP_URL`
  - Cursor: `.cursor/mcp.json` → `{ "mcpServers": { "ranklens": { "url": "YOUR_MCP_URL" } } }`
  - Windsurf: `~/.codeium/windsurf/mcp_config.json` benzer JSON (`serverUrl` alan adını Windsurf docs'undan doğrula; emin değilsen her iki varyantı not olarak ver)
  - Claude Desktop / claude.ai: Settings → Connectors → Add custom connector → URL yapıştır (plan uygunluğu notu: remote MCP connector'ları ücretli planlarda)
- `core-concepts/credits.mdx`: kredi mantığı + /pricing'e link; RAKAM tekrarı yalnız "draft" ibaresiyle (tek kaynak /pricing).
- `core-concepts/gsc-connection.mdx`: GSC opsiyoneldir, readonly scope, ilk crawl GSC'siz çalışır (spec §1/§6.4).
- `core-concepts/data-retention.mdx`: crawl ham verisi 90 gün · raporlar hesap ömrü · silme = tam purge (spec §6.5).
- `recipes/*`: spec §2.1'deki 3 MCP prompt'una birebir karşılık — `first-site-audit` (new-site-audit), `monthly-seo-routine` (monthly-routine), `quick-wins-sprint` (quick-wins-sprint). Her recipe: amaç → örnek diyalog (2-3 mesaj, tool adları gerçek) → beklenen çıktı türü → kredi notu ("draft costs, see /pricing").
- `security.mdx`: tenant izolasyonu (RLS), GSC token at-rest şifreli, "your data never trains AI models", gitleaks/secret hijyeni müşteri diliyle DEĞİL ürün vaadi diliyle (spec §6).
- `faq.mdx`: 6-8 soru (What is MCP? Which clients work? Do I need GSC? What do credits cost? Is my data used for training? When does beta open? How do I delete my data?).
- `troubleshooting.mdx`: client bağlantı sorunları (URL yanlış yapıştırma, client restart), "tool not showing" (client'ta MCP listesini yenile), beta sınırları; her madde belirti → kontrol → çözüm formatında.

- [ ] **Step 1: Tüm MDX dosyalarını + meta.json'ları yukarıdaki kurallarla yaz**

- [ ] **Step 2: Statik üretim + nav doğrulaması**

Run: `pnpm --filter @pseo/web build 2>&1 | grep -E "○|●" | grep docs`
Expected: tüm docs sayfaları listede (Static/SSG işaretli) — satırları kanıt olarak kaydet.
Görsel nav kontrolü şefin Claude Browser adımında: sidebar sırası = Getting Started · Core Concepts · Recipes · Billing & Credits · Security · FAQ · Troubleshooting.

- [ ] **Step 3: Kapı + commit (içeriği 2 commit'e böl: getting-started+core-concepts, sonra kalanlar — 200 satır kuralı)**

Run: `bash guardrails/verify.sh` → `VERIFY: PASS`
```bash
git add apps/web/content/docs
git commit -m "docs: getting-started (5 client) + core-concepts iskeleti"
# ikinci parça:
git commit -m "docs: recipes (3 taslak) + billing/security/faq/troubleshooting"
```

---

## Şef koreografisi (subagent-driven-development ile)

Her task için sıra: (1) iş emri JSON'u yaz (task metni + done_when + files_in_scope + forbidden) → (2) işçi subagent (dispatch tablosundaki model) → (3) taze hakem subagent'a iş emri + `git diff` ver → PASS/FAIL (qa-loop: ≤3 deneme; 2× FAIL = insanı uyandır) → (4) `bash guardrails/verify.sh` → (5) UI task'ıysa şef verify-change + Claude Browser kanıtı (preview_start "web", gerçek etkileşim, konsol 0 hata, screenshot) → (6) commit'ler branch'te; PR aç/güncelle (`gh pr create --base <önceki-branch>`) → (7) PLAN.md güncelle.

**İş kapanışlarında goals/ commit'leri (şef):**
- İş A (T4 sonrası): `goals/lighthouse-90.md` — predicate: `pnpm run --silent lighthouse`; on-violation: skoru düşüren denetim + son UI commit'leri şüpheli. `goals/landing-live.md` — predicate: `[ -z "${PROD_URL:-}" ] && exit 0; curl -sf "$PROD_URL" | grep -q Ranklens`; on-violation: deploy/DNS runbook (deploy insan kapısından sonra anlam kazanır).
- İş B (T5b sonrası): `goals/docs-static.md` — predicate: `[ -f apps/web/.next/prerender-manifest.json ] || (cd apps/web && pnpm build >/dev/null 2>&1); node -e "const m=require('./apps/web/.next/prerender-manifest.json');process.exit(Object.keys(m.routes).some(r=>r.startsWith('/docs'))?0:1)"`; on-violation: source.config/generateStaticParams değişiklikleri şüpheli.
- İş C (T3 sonrası, form gömülünce): `goals/waitlist-works.md` — predicate: `pnpm --filter @pseo/core exec vitest run src/waitlist >/dev/null`; on-violation: adapter/schema commit'leri şüpheli.

**Kanıt disiplini:** lhci skorları + build SSG satırları + Claude Browser gözlemi `.superpowers/sdd/progress.md` ledger'ına yazılır; PR gövdesine özetlenir. Kanıtsız "tamamlandı" yasak (verify-change skill).

**Bilinen sınırlamalar / insana not:** (1) Waitlist'in "PostHog'da event + gerçek Resend kaydı" kanıtı anahtar gelene dek `pnpm waitlist:smoke` olarak hazır bekler — kod-done, kanıt insan anahtarına bağlı. (2) Vercel deploy + DNS faz sonu insan kapısı. (3) PR merge'leri insan onaylı; stack sırayla merge edilmeli (PR1→PR5).
