# TOOL REVİZYON DEFTERİ — 36 tool, tek tek

> **Bu dosya bir DEFTER, bir plan değil.** Revizyon oturumu tool'ları tek tek gezer ve bulduğu her
> şeyi buraya **append eder**. Hiçbir bulgu bu oturumda düzeltilmez — düzeltme ayrı bir dilimdir,
> ayrı iş emri, ayrı hakem. Karıştırmak, ölçmeyi düzeltmeye kurban eder.
>
> **Neden ayrı:** bir tool'u incelerken aklına gelen düzeltmeyi hemen yapmak, o oturumun geri kalan
> 35 tool'unu inceleme bütçesini yer. Bu oturumun tek işi **görmek ve yazmak**.

## Bulgu satırının şekli — pazarlıksız

Her bulgu **tek satır**, ve dört alanı da dolu olmak zorunda:

```
| tool | sınıf | ne görüldü (ÖLÇÜM) | öneri (kimin) |
```

- **sınıf** — aşağıdaki altıdan biri. Uydurma sınıf yok.
- **ne görüldü** — *gözlenen* şey, yorum değil. "Kötü" bir ölçüm değildir; "üç alandan üçü de `n/a`
  döndü" ölçümdür. Prompt ve çıktının ilgili kısmı yazılır.
- **öneri** — ve **kimin** olduğu: `[operatör]` fiyat/politika kararıysa, `[kod]` kod işiyse,
  `[açık]` henüz karar verilmemişse.

**Bir bulgu, ölçülmeden yazılmaz.** Bu projede bir oturumda altı yanlış iddia bu yüzden çıktı.

## Sınıflar

| sınıf | ne demek |
|---|---|
| `SEÇİM` | LLM doğal cümleden **yanlış tool'u** seçti ya da hiçbirini seçemedi → kusur **açıklamada** |
| `ARGÜMAN` | Doğru tool seçildi ama argüman düz cümleden **doldurulamadı** ya da yanlış dolduruldu → kusur **şema açıklamasında** |
| `VERİ` | Çağrı çalıştı ama alanlar **boş / `n/a` / anlamsız** → vendor sözleşmesi kaymış olabilir |
| `ÇIKTI` | Veri doğru ama sunum **okunmuyor ya da eyleme dönmüyor** — sayı var, cevap yok |
| `DEĞER` | Fiyat ↔ verilen değer dengesi. **Fiyat önerisi NEVER#6'dır, imzasız uygulanmaz** |
| `KAPSAM` | Tool'un yapmadığı ama yapması beklenen şey; ya da hiç olmaması gereken şey |

## Kapsam kuralı — para

Ücretli her çağrı **gerçek vendor parası** harcar. Günlük tavan **$3,00**, fail-closed.
Bir tool'u **birden fazla kez** koşturmak gerekiyorsa (ör. `discover_keywords`'ün dört mode'u)
o karta yazılır. Tavana yaklaşırsan **dur ve yaz** — ertesi gün devam.

`DFS_LIVE` kapalıysa her ücretli tool "not enabled" döner: **0 kredi, 0 defter satırı**, ve
inceleme **hiçbir şey ölçmemiş** olur. İlk ücretli çağrıda bunu doğrula.

---

## BULGULAR

> Aşağıya append edilir. Boşken bırakmak da bir ölçümdür: "bakıldı, bulgu yok" ayrı bir satırdır,
> "bakılmadı"dan farklıdır.

| tool | sınıf | ne görüldü (ÖLÇÜM) | öneri (kimin) |
|---|---|---|---|
| `setup_project` | `KAPSAM` | Prompt: *"Bir sitem var, seogrep.com — sisteme kaydedelim"* → tool doğru seçildi. Argüman probu `https://www.seogrep.com/pricing?utm_source=chatgpt` → çıktı: `Created project for "www.seogrep.com" (created: true)`. Şema/yol/query soyuldu, **`www.` soyulmadı**; `seogrep.com` zaten `4e0caff0-…` olarak vardı → aynı site **iki proje**. Açıklamanın "Idempotent — calling it again for the same domain returns the existing project" iddiası, müşterinin adres çubuğundan yapıştırdığı biçimde tutmuyor. | Kayıtta host normalizasyonuna `www.` soyma eklensin (idempotency iddiası ancak o zaman doğru). Mevcut çift kayıtlar için tekilleştirme ayrı karar. **[kod]** |
| `list_projects` | `VERİ` | Prompt: *"Sistemde hangi siteler kayıtlı, bir bakayım."* → tool doğru seçildi. Çıktı 14 satır; **6 satır `www.` önekli**: `www.bigcattr.com` · `www.noraninsaat.com` · `www.miningaa.com` · `www.lastiksa.com` · `www.eykom.com` (**beşi bu oturumdan ÖNCE**) + probumun `www.seogrep.com`u. `setup_project` bulgusu böylece teorik değil: aynı hesapta **5 kez fiilen olmuş**. Ayrıca `example.net` (257ad998-…) canlı hesapta test kaydı olarak duruyor. | `www.` normalizasyonu (bkz. `setup_project` satırı) + mevcut 5 kaydın geri-doldurulması ayrı karar. **[kod]** ve **[açık]** |
| `list_projects` | `ÇIKTI` | Aynı çağrı. Her satırda **tam iki alan** var: domain + UUID. Hiçbir satırda GSC bağlı mı · son ölçüm tarihi · harcanan kredi · arşiv durumu YOK. 14 projelik bir hesapta çıktı, müşteriye "hangi projede iş var" sorusunu cevaplamıyor; sıralama açıklamada "oldest first" deniyor ve en yeni kayıt gerçekten sonda geldi. | Satır başına en az bir etkinlik sinyali (son koşu tarihi) + GSC bağlı bayrağı. Sıralamanın "oldest first" mi "son etkinlik" mi olacağı ürün kararı. **[açık]** |
| `setup_project` | `KAPSAM` | Prob: `bu-domain-kesinlikle-yok-9f3a2c.com` → çıktı `Created project ... (created: true)`. **Hiçbir varlık/erişilebilirlik kontrolü yok**, uyarı yok, geri dönüş "başarılı" tonunda. Müşteri site adını yanlış yazarsa kusursuz görünen bir proje doğar ve sonraki ücretli tool o hedefe para harcar (harcamayı kimse iptal etmez). | En az bir "bu alan adına ulaşılamadı, yine de kaydedeyim mi" uyarısı; engelleme mi uyarı mı olacağı (yayın öncesi siteler meşru) ürün kararı. **[açık]** |
| `setup_project` | — | Prob: `SEOGREP.COM` → `Project already exists for "seogrep.com" (created: false)`. **Bakıldı, bulgu yok**: büyük/küçük harf normalizasyonu doğru çalışıyor; `www.` kusuruyla aynı yolda DEĞİL. | — |
| `list_projects` | — | Açıklamadaki "(oldest first)" iddiası **iki veri noktasıyla** sınandı: kart 1'de sırayla `www.seogrep.com` sonra `bu-domain-kesinlikle-yok-9f3a2c.com` yaratıldı; ikinci çağrıda ikisi de tam o sırayla listenin sonunda geldi (14 → 15 satır). **Bakıldı, bulgu yok.** | — |
| `list_projects` | `KAPSAM` | Şemada **hiç argüman yok** (`properties: {}`): arama, filtre, sayfalama, sıralama seçeneği yok — çıktı tek blok 15 satır. Ayrıca listede duran ölü/çöp kayıtlar (`example.net`, `bu-domain-kesinlikle-yok-9f3a2c.com`) gerçek sitelerden **hiçbir işaretle ayrılmıyor**; müşteri hangisinin canlı olduğunu listeden bilemiyor. | Proje sayısı büyüdükçe (bu hesapta 15) filtre/arama gerekir; en azından ölü kayıt işareti. Arşiv görünürlüğü kart 9'da (`untrack_project`) ÖLÇÜLECEK, şimdi iddia edilmiyor. **[açık]** |
| `get_credit_balance` | `ÇIKTI` | Prompt: *"Ne kadar kredim kalmış?"* → tool doğru seçildi. Çıktı birebir: `Credit balance: 5630 credits. Paid tools debit credits when they run; a balance of 0 blocks paid tools until you top up.` Cümlenin ikinci yarısı **eksik şart bildiriyor**: `ranked-keywords.ts:128` ve `serp-snapshot.ts:170` gerçek şartı "a paid credit balance: it is not available on trial credits" diye yazıyor. Trial kullanıcısı 200 kredi görüp "0 değil, çalışır" diye okur; DFS tool'u yine de reddeder. | Bakiye cümlesi trial/paid ayrımını SÖYLESİN (ör. "200 trial credits — DFS tools need a paid balance"). Kaynak: `get-credit-balance.ts:18`. **[kod]** |
| `get_credit_balance` | `KAPSAM` | Aynı çağrı. Çıktıda **tek sayı** var: trial mi paid mi · son hareket · hangi tool ne kadar yedi · son yükleme — hiçbiri yok. Defterin kendi 36-tool tablosunda harcama geçmişi ya da kredi maliyet listesi döndüren **hiçbir tool yok**; bu bilgi yalnız `/app/usage` panelinde. MCP'den çalışan müşteri "bu ay ne harcadım" sorusunu hiçbir tool'a soramıyor. | Bakiye çıktısına son N hareket, ya da ayrı ücretsiz harcama-geçmişi ucu. Panel↔MCP yüzey paritesi kararı. **[açık]** |
| `get_job_status` | `ARGÜMAN` | Prompt: *"Az önce başlattığım tarama ne durumda?"* → tool doğru seçildi ama **argüman düz cümleden doldurulamadı**: `job_id` zorunlu ve cümlede yok. Müşterinin elinde kimlik yoksa tool çağrılamaz; şema tek alan istiyor ve alternatif (proje/son koşu) kabul etmiyor. | `project_id` ile "son işi" sorabilme, ya da `job_id`'yi zorunluluktan çıkarıp son N işi listeleme. **[kod]** |
| `get_job_status` | `KAPSAM` | Aynı kart. Defterin 36-tool tablosu tarandı: **iş listeleyen hiçbir tool yok** (`list_jobs` benzeri uç yok). `crawl_site` asenkron ve tek dönüşü `job_id`; o kimlik konuşmadan düşerse sonuca **başka hiçbir yoldan** ulaşılamıyor — ücret ödenmiş iş erişilemez hale geliyor. | Ücretsiz "son işlerim" ucu ya da `get_job_status`'un argümansız çağrıda son işi döndürmesi. **[kod]** |
| `get_job_status` | — | Sınır probu: rastgele `3f2a9c14-…` ve şemanın açıkça izin verdiği nil `00000000-…` → **ikisi de birebir aynı** `No job found with id …`. "Var ama senin değil" ile "yok" ayrımı mesajdan sızmıyor. **Bakıldı, bulgu yok.** Kiracı izolasyonu ölçülmedi (yabancı gerçek `job_id` yok — handoff S4). | — |
| `whats_next` | `KAPSAM` | **Ölü alan adına para harcatıyor.** Kart 1'de bilerek yaratılan `bu-domain-kesinlikle-yok-9f3a2c.com` (4f3eb00a-…) ile çağrıldı → çıktı: `Next step for bu-domain-kesinlikle-yok-9f3a2c.com: run crawl_site.` / `Why: This project has no crawl yet.` Router'da **erişilebilirlik sinyali yok**; dünyada karşılığı olmayan bir hedefe **20 kredilik** iş öneriyor. `setup_project`'in doğrulama eksiğinin ölçülmüş bedeli budur. | Router en azından "bu alan adına ulaşılamıyor" durumunu ayırsın; kayıt anında uyarı (kart 1) ile birlikte tek karar. **[kod]** |
| `whats_next` | `ÇIKTI` | Gerçek projede (dentnotion.com) çıktı doğru ve eyleme dönük: `recommended next: run generate_report` + gerekçe + 7 maddelik "Then" listesi (`find_quick_wins`, `detect_cannibalization`, `analyze_content_decay`, `audit_onpage`, `audit_tech`, `audit_schema`, `monthly-routine`). Ama **hiçbir maddede kredi fiyatı yok**: önerilen `generate_report` 15, listedeki `audit_onpage` 30 kredi. Müşteri fiyatı görmeden ücretli işe yönlendiriliyor; kart 3'te ölçüldüğü gibi fiyat listesi döndüren başka tool da yok. | Yönlendirmenin her maddesine kredi maliyeti yazılsın (fiyat DEĞİŞTİRMEK değil, mevcut fiyatı GÖSTERMEK — NEVER#6 kapsamı dışı). **[kod]** |
| `whats_next` | `ÇIKTI` | Argümansız çağrı (15 projeli hesap) → **hiç yönlendirme yapmadı**: `You are tracking more than one project. Tell me which one to look at…` + `list_projects`'in birebir aynı 15 satırı. Tool açıklaması ise "Optionally pass a project_id; **omit it to route from your project list**" diyor — birden fazla projede bu iddia tutmuyor, yönlendirme yerine soru dönüyor. | Ya açıklama düzeltilsin ("tek proje varsa"), ya router çok-projede en aktif projeyi seçip önersin. **[kod]** |
| `whats_next` | `VERİ` | **Gerekçe cümlesi iki bağımsız tool tarafından yalanlandı.** `whats_next(dentnotion)` → `Why: You have a fresh crawl and **fresh Search Console data** — you're all set.` Aynı oturumda: `list_gsc_properties` → `https://dentnotion.com/ (siteOwner) — **not used by any project**`; `connect_gsc(dentnotion)` → bağlantısız dalı döndü (`To connect Google Search Console for dentnotion.com, open this link…`), oysa gerçekten bağlı `adstark.com.tr`'de `already connected` dalını döndürüyor. Sonuç: router ücretsiz `connect_gsc` adımını atlayıp **15 kredilik** `generate_report`'a yönlendirdi. | Router'ın GSC tazelik sinyali, bağlantının HÂLÂ duruyor olmasıyla birlikte okunmalı (eski çekilmiş satırlar bağlantı sanılıyor olabilir — kök neden ölçülmedi). **[kod]** + kök neden **[açık]** |
| `connect_gsc` | — | Prompt: *"Search Console verilerimi de buraya bağlamak istiyorum."* → tool doğru seçildi. **Bağlı projede çıktı iyi ve durum farkında**: `adstark.com.tr` → `Google Search Console is already connected … property https://adstark.com.tr/` + sonraki adımlar (`pull_gsc_data`, `find_quick_wins`…) + "başka property'ye geçmek için Connection sayfası" + iptal edilmişse yeniden onay linki. Read-only vaadi her iki dalda da açıkça yazıyor. **Bakıldı, bulgu yok.** | — |
| `connect_gsc` | `KAPSAM` | Ölü projede (`bu-domain-kesinlikle-yok-9f3a2c.com`) çıktı, gerçek projeninkiyle **alan adı dışında birebir aynı**: `To connect Google Search Console for bu-domain… open this link and approve access`. Var olmayan bir alan adı için Google onay akışı başlatılması engellenmiyor/uyarılmıyor (kart 1 + `whats_next` ile aynı kök: erişilebilirlik sinyali hiçbir yerde yok). | Kart 1'deki kayıt-anı doğrulamasıyla tek karar olarak ele alınsın. **[kod]** |
| `list_gsc_properties` | `KAPSAM` | Prompt: *"Google tarafında hangi sitelerim var, hangisini okuyabiliyor?"* → tool doğru seçildi. Çıktı: 1 hesap, 27 property, 7'si bir projeye bağlı. **Beş property'nin karşılığı olan proje VAR ama bağlı değil** ve beşi de `siteOwner`: `sc-domain:seogrep.com`↔`seogrep.com` · `https://dentnotion.com/`↔`dentnotion.com` · `https://rkturizm.com/`↔`rkturizm.com` · `https://bayder.com.tr/`↔`bayder.com.tr` · `sc-domain:noraninsaat.com`↔**`www.noraninsaat.com`**. Tool iki tarafı da basıyor, eşleşmeyi **söylemiyor**; bağlamak ücretsiz tek çağrı (`track_gsc_property`). Son çift ayrıca `www.` kusurunun GSC tarafındaki bedeli — düz dizgi eşleşmesiyle asla buluşmazlar. | Bağlanmamış-ama-eşleşen property'ler için tek satırlık öneri ("5 property bağlanabilir"). `www.` normalizasyonu (kart 1) bu eşleşmenin ön şartı. **[kod]** |
| `list_gsc_properties` | `ÇIKTI` | Aynı çağrı. 27 satır **sıralanmamış ve gruplanmamış**: `read by …` ile `not used by any project` satırları iç içe, alfabetik değil. Müşteri "hangisini bağlamayı unuttum" sorusunu 27 satırı elle tarayarak cevaplıyor. `list_projects` (kart 2) ile aynı desen. | Kullanılan/kullanılmayan gruplaması ya da bağlanabilir olanların başa alınması. **[açık]** |
| `list_gsc_properties` | — | Yetki ekseni probu: 6 `siteUnverifiedUser` satırı `NOT QUERYABLE` işaretli + sebep/çözüm cümlesi; `sc-domain:speechscribe.ai (siteRestrictedUser)` **işaretlenmemiş** — geçmiş oturumda `siteRestrictedUser`'ın sorgulayabildiği ölçülmüştü, tool yanlış pozitif basmıyor. Açıklamanın üç vaadi (permission level · queryable · hangi proje okuyor) her satırda karşılanıyor. **Bakıldı, bulgu yok.** | — |
| `track_gsc_property` | `VERİ` | **`www.` kusuru burada siteyi ikiye bölüyor.** Girdi: `sc-domain:noraninsaat.com` (kart 7'de "not used by any project", karşılığı olan proje `www.noraninsaat.com` / dcad126a-… zaten var) → çıktı: `**Created project** "noraninsaat.com" (project_id: ea77221c-…) and set it to read sc-domain:noraninsaat.com`. Sonuç: aynı müşteri sitesi iki projede — **crawl/audit geçmişi `www.noraninsaat.com`'da, GSC verisi `noraninsaat.com`'da**. Crawl+GSC'yi birleştiren tool'lar (`find_quick_wins`, `analyze_content_decay`, `detect_cannibalization`) hangi projeden çağrılırsa çağrılsın **yarım veri** görecek. | Kart 1'in `www.` normalizasyonu bu kartın ön şartı; ayrıca property↔proje eşleştirmesi `www.` farkını yok sayarak yapılmalı. Bu oturumda YARATILAN `noraninsaat.com` (ea77221c-…) temizlik kararı bekliyor. **[kod]** |
| `track_gsc_property` | `ARGÜMAN` | Prompt: *"dentnotion.com'un Search Console verisini bu projeye bağlayalım."* → tool doğru seçildi, ama müşterinin konuştuğu biçim reddedildi: girdi `dentnotion.com` → `"dentnotion.com" is not listed on any Google account you have connected…`. Oysa `list_gsc_properties` çıktısında `https://dentnotion.com/` **tek ve tekil** eşleşme. Tam biçim (`https://dentnotion.com/`) verilince sorunsuz çalıştı. Hata mesajı yol gösteriyor (iyi), ama çözülebilir bir eşleşme çözülmüyor. | Tekil eşleşmede `example.com` → `https://example.com/` / `sc-domain:example.com` çözümü; birden fazla adayda seçenek sunulması. **[kod]** |
| `track_gsc_property` | `ÇIKTI` | Zaten bağlı dalında dönen cümle kırık: `Project "dentnotion.com" was already tracked (project_id: fa9340e5-…) **and set it to read** https://dentnotion.com/ through suleymanncapar@gmail.com.` — özne kayması ("was tracked … and set it"). Ürün dili İngilizce ve bu müşteriye görünen metin. | Cümle düzeltilsin (ör. "…was already tracked; it now reads …"). **[kod]** |
| `track_gsc_property` | — | Tam biçimli, `www.` uyuşmazlığı olmayan durumda davranış doğru: `https://dentnotion.com/` → **yeni proje YARATMADI**, mevcut `fa9340e5-…`'e bağladı ve sonraki adımları (`pull_gsc_data` → `find_quick_wins`/`detect_cannibalization`/`analyze_content_decay`) sıraladı. **Bakıldı, bulgu yok.** | — |
| `untrack_project` | — | Prompt: *"Şu siteyi artık takip etmiyorum, listeden kaldıralım."* → tool doğru seçildi. **Açıklamanın üç iddiası da ölçüldü ve tuttu:** (1) arşive taşındı → `list_projects` çıktısından düştü; (2) `setup_project` ile aynı alan adı → `Restored "…" from your archive — it is tracked again (project_id: **4f3eb00a-… aynı kimlik**, created: false)`; (3) ikinci kez arşivleme idempotent → `…is already in your archive, so nothing was changed.` **Bakıldı, bulgu yok** — bu turda iddialarının tamamı doğrulanan ilk tool. | — |
| `untrack_project` | `ÇIKTI` | İki mesaj çelişiyor. İlk arşivleme: `Run track_gsc_property for the same property — **or setup_project for the same domain** — and this same project comes back`. "Zaten arşivde" mesajı ise **yalnız** birini yazıyor: `track_gsc_property brings it back unchanged`. Bu projenin GSC property'si YOK, yani ikinci mesajın adlandırdığı tek yol bu proje için **çalışmayan** yol; çalışan yol (`setup_project`) o mesajda hiç geçmiyor. | "Zaten arşivde" dalı da iki yolu birden yazsın. **[kod]** |
| `untrack_project` | `KAPSAM` | Arşiv **hiçbir yerden görünmüyor**: arşivlemeden sonra `list_projects` yalnız takip edilenleri döndürdü ve defterin 36-tool tablosunda arşiv listeleyen hiçbir tool yok. Geri getirmek için müşterinin alan adını ya da property'yi **tam** hatırlaması şart; hatırlamıyorsa arşivdeki geçmişe (crawl, rapor, GSC bağı) MCP'den ulaşacak yol yok. | Ücretsiz "arşivim" ucu ya da `list_projects`'e arşiv bölümü. `get_job_status` kartındaki "kimliği kaybedersen iş kayıp" bulgusuyla aynı desen. **[kod]** |
| `track_keywords` | `ARGÜMAN` | **Yer adı hiç doğrulanmıyor.** Girdi `location_name: "Türkiye"` (müşterinin kendi dilinde yazacağı biçim) → kabul edildi: `Tracking 1 keyword … — **Türkiye** · language tr · desktop SERP`. Kaynak: `track-keywords.ts:94` → `location_name: z.string().min(1)`, açıklama "as DataForSEO names it" diyor ama liste kontrolü YOK; dizgi `dfs/serp.ts`'e (`location_name` string, satır 201-202) doğrudan gidiyor. Yanlış/yerelleştirilmiş ad ancak **ücretli ölçüm anında** (`serp_snapshot` 5+8/kw) patlar. İmzalı ders 6'nın aynı deseni (`min(1)` ile yapısal doğrulamanın yerine geçmek). | Kayıt anında DFS yer listesine karşı doğrulama + "bunu mu demek istediniz" önerisi. **[kod]** |
| `track_keywords` | — | Açıklamanın **dört iddiası da ölçüldü ve tuttu**: (1) normalizasyon — girdi `["diş beyazlatma", "  Diş Beyazlatma  ", "implant fiyatları"]` → `Tracking **2** keywords`, üç yazım iki kayda indi; (2) ölçüm yapmıyor — `No search engine was contacted, no position was read, and nothing was charged`; (3) yer/dil kaydın parçası — aynı kelime `Türkiye·tr` altında AYRI kayıt olarak açıldı; (4) `untrack` siliyor değil arşivliyor — `Untracking archives the keyword — every position already measured for it is kept`. Varsayılanlar da çıktıda açıkça yazılı (`United States · language en · desktop SERP`). **Bakıldı, bulgu yok.** | — |
| `track_keywords` | `KAPSAM` | Varsayılan `United States · en`, projenin alan adı/GSC verisi ne olursa olsun **türetilmiyor**; çıktıda dürüstçe yazılıyor ama sorgulanmıyor. Türk bir diş kliniği (`dentnotion.com`) düz cümleyle kelime kaydettiğinde ABD SERP'ine kaydoluyor; sonraki ölçüm (`serp_snapshot` 5+8/kw) **yanlış SERP'te gerçek para** harcar. Projede bağlı GSC varsa baskın ülke zaten bilinebilir. | Varsayılan yer, projenin GSC baskın ülkesinden/TLD'sinden türetilsin ya da ilk kayıtta açıkça sorulsun. **[açık]** |
| `keyword_positions` | `VERİ` | **"Veri var" dalı üretimde hiç görünmemiş.** Prompt: *"dentnotion'ın kelimelerinde kaçıncı sıradayım?"* → tool doğru seçildi; iki projede de `No stored SERP measurement matches this request…`. Depoya bakıldı (Supabase, prod): `select count(*) from keyword_position_measurements` → **0**; `tracked_keywords` → **3** satır ve **üçü de bu oturumda benim yazdıklarım**. Yani sıralama ailesinin tamamı (`track_keywords` → `serp_snapshot` 5+8/kw → `keyword_positions` 10 kr → `/app/rankings` paneli, geçen oturum 12 dosya/2717 satır) üretimde **tek satır veri görmemiş**; defterin kendi notu ("`serp_snapshot` fixture'ı saklanabilir satır üretemiyordu") bununla tutarlı. | Zincirin ucundan (`serp_snapshot`) tek gerçek satır üretilip doğrulanması — ucuz blokta ölçülecek. Panel+tool yatırımının karşılığı buna bağlı. **[kod]** |
| `keyword_positions` | — | **Ücret iddiası bağımsız eksende doğrulandı.** Açıklama: "If nothing has been measured yet, it says so and charges nothing." Ölçüm: çağrı öncesi bakiye **5630**, "nothing to read and you were not charged" mesajı, çağrı sonrası bakiye **5630** — 10 kredilik tool boş dalda hiç düşmedi. Mesaj ayrıca eksik adımı adıyla söylüyor (`track_keywords` ayrı ve ücretsiz). **Bakıldı, bulgu yok.** | — |
| _(oturum gözlemi)_ | `VERİ` | `dfs_spend_today_usd()` = **$0,012480**, ben hiçbir ücretli tool çağırmadan (2026-08-25). Günün harcaması sıfırdan başlamıyor; kaynağı ölçülmedi (cron olabilir). Tur bütçesi bu yüzden $3,00'ın tamamı değil. | Günlük tavanın kim tarafından ne zaman yendiğini gösteren bir okuma yolu yok (kart 3'teki harcama-geçmişi eksiğiyle aynı kök). **[açık]** |
| `audit_schema` | `DEĞER` | Prompt: *"Yapısal veri işaretlemelerim ne durumda, eksik var mı?"* → tool doğru seçildi, ücret tam (bakiye 5630 → **5625**). Çıktı 26 sayfa için **23 satırlık @type sayımı** (Dentist 25, FAQPage 10, MedicalProcedure 13 …) + kapsama (25/26) + tek eylem maddesi. Ama **hiçbir yargı yok**: hangi tip eksik, hangi işaretleme kural dışı, hangi sayfada ne yapılmalı yazmıyor — tool kendi notunda söylüyor: `only @type names are analyzed, **never the JSON-LD body**`. Adı `audit_` ama yaptığı iş **sayım**; geçersiz/eksik alanlı JSON-LD görünmez. | Ya doğrulama eklensin (zorunlu alan/kural denetimi) ya da beklenti adla değil açıklamayla düzeltilsin. 5 kredinin karşılığı sayım mı denetim mi — fiyat NEVER#6. **[operatör]** + **[kod]** |
| `audit_schema` | `ÇIKTI` | Aynı çağrının tek eylem maddesi gerçek bir sayfa değil: `Pages with NO structured data: · https://dentnotion.com/cdn-cgi/l/email-protection` — Cloudflare e-posta koruma artefaktı. 26 sayfalık denetimin çıkardığı **tek iş**, müşterinin dokunamayacağı bir uç. | Crawl kapsamında `/cdn-cgi/` gibi altyapı yollarının elenmesi (kusur crawl tarafında olabilir — `crawl_site` kartında ayrıca ölçülecek). **[kod]** |
| `audit_schema` | `ÇIKTI` | Crawl'ı olmayan projede (`noraninsaat.com`, ea77221c-…) çıktı: `No crawl found for this project. Run crawl_site first.` Bakiye 5630 → **5630**, yani ücret alınmadı — ama **mesaj bunu söylemiyor**. `keyword_positions` aynı durumda `…and you were not charged` diyor. Müşteri, 5 kredisinin yanıp yanmadığını mesajdan bilemiyor. | Ücretsiz-ret dallarında "ücret alınmadı" cümlesi tüm tool'larda tek biçim olsun. **[kod]** |
| `audit_schema` | `VERİ` | Çıktı crawl tarihini yazıyor: `crawl from **2026-08-09**T16:22:48Z` — bugün 2026-08-25, yani **16 gün önce**. Kart 5'te `whats_next` aynı proje için `You have a **fresh** crawl and fresh Search Console data — you're all set` demişti. "Fresh" eşiğinin ne olduğu hiçbir çıktıda yazmıyor; iki tool aynı veriyi iki farklı dille sunuyor (GSC tarafındaki çelişkiyle aynı kart: `whats_next`). | Tazelik eşiği tanımlansın ve iki yüzeyde aynı dille söylensin. **[açık]** |
| `pull_gsc_data` | `VERİ` | Prompt: *"GSC performans verimi çek."* → tool doğru seçildi, ücret tam (5625 → **5620**). Çıktı: `Current window 2026-05-25..2026-08-22: **15000 rows** / Previous window …: **15000 rows** / Note: this window hit the 15,000-row cap`. **İki pencere de tavana dayandı**, yani dönem karşılaştırması yapısal olarak eksik veriyle yapılıyor; hangi ölçüte göre "top rows" seçildiği (tıklama? gösterim?) hiçbir yerde yazmıyor. Ardıl üç tool (`find_quick_wins`, `detect_cannibalization`, `analyze_content_decay`) bu kesik veriden besleniyor. | Kesme ölçütü çıktıda adlandırılsın; tavana dayanan pencerede daha kısa `days` önerisi otomatik verilsin (job mesajı veriyor, tool çıktısı vermiyor). **[kod]** |
| `pull_gsc_data` | `VERİ` | **İş kaydının saatleri tutarsız.** `get_job_status(de8f2440-…)` müşteriye şunu bastı: `created 2026-08-25T15:42:59.928Z · finished 2026-08-25T15:42:46.054Z` — iş, oluşturulmadan **13,9 sn önce bitmiş** görünüyor. Depo doğrulaması: `jobs` tablosunda `pull_gsc_data` işlerinin **dördünde de `started_at` NULL** (aynı tabloda `crawl_site` işleri `started_at`'i dolduruyor). Süre ölçülemiyor ve negatif görünebiliyor. | `started_at` yazılsın, `finished_at` işin gerçek bitişinden alınsın; `get_job_status` süreyi hesaplayabilsin. **[kod]** |
| `whats_next` | `VERİ` | **Kart 6'daki çelişkinin kök nedeni ölçüldü ve kapandı.** `jobs` tablosu: dentnotion için **2026-08-09 20:00** tarihli başarılı bir `pull_gsc_data` var (result 337 KB). Yani GSC satırları depoda duruyordu ama property bağı yoktu — `whats_next` **çekilmiş satırlara** bakıp "fresh Search Console data" diyor, bağlantının hâlâ duruyor olmasına bakmıyor. 16 günlük veri "fresh" sayılıyor; eşik tanımsız. | Router'ın GSC sinyali (a) bağlantı canlı mı (b) veri kaç günlük — ikisini birden okusun. **[kod]** |
| `pull_gsc_data` | `KAPSAM` | Depo ölçümü: public şemada GSC satırlarını tutan **ilişkisel tablo yok**; 30.000 satırlık çekim tek bir `jobs.result` jsonb'sinde duruyor (`pg_column_size` = **972.959 bayt**, önceki çekimler 337 KB). Ardıl analiz tool'ları her koşuda ~1 MB JSON'u ayrıştırmak zorunda; 15.000 tavanı da bu saklama biçimiyle birlikte okunmalı (nedensellik ÖLÇÜLMEDİ, iddia edilmiyor). | Saklama biçimi kararı (jsonb vs tablo) ayrı bir mimari dilim. **[açık]** |
| `serp_snapshot` | `VERİ` | **ÜÇ ücretli çağrı, ÜÇ başarısızlık, üçü de tam ücretli.** (1) `Turkey·tr` → `NOT MEASURED: DataForSEO task failed (status 40501): Invalid Field: 'location_name'`; (2) `Turkiye·tr` (vendor'ın kanonik adı) → `NOT MEASURED: The operation was aborted due to timeout`; (3) varsayılan `United States·en`, farklı kelime → **aynı timeout**. Depo: `keyword_position_measurements` **3 satır, 3'ü de `status='not_measured'`**; kredi −39; `dfs_spend_today_usd()` $0,012480 → **$0,102480 (+$0,09)**. Kart 11'in sorusu böylece kapandı: sıralama zinciri (`track_keywords`→`serp_snapshot`→`keyword_positions`→`/app/rankings`) üretimde **hiç gerçek konum üretmemiş**, ve her deneme tam para yakıyor. | Zincir onarılana kadar tool'un ücretlendirme davranışı gözden geçirilsin: vendor tarafı başarısızsa iade/ücretsiz-ret. **[kod]** + iade politikası **[operatör]** |
| `serp_snapshot` | `VERİ` | **Kök neden ölçüldü (nedensellik hipotez, kanıt güçlü).** Aynı sorgu vendor'a DOĞRUDAN atıldı (`serp/google/organic/live/advanced`, `depth: 100`, **`max_crawl_pages: 1`**) → tam SERP hızla döndü (local pack + 9 organik + PAA + related). Bizim istek gövdemiz `dfs/serp.ts:434 buildSerpRequestBody` — `depth: 100` var, **`max_crawl_pages` YOK**, ve kod yorumu sebebini yazıyor: *"absent because their interaction with depth is **unmeasured** and a guess could truncate a paid scrape"*. Bizim timeout 30 sn (`dfs/client.ts:326`), yorumu ise arızayı önceden tarif ediyor: *"a tight deadline would abort healthy work and **spend budget for nothing**"*. | `max_crawl_pages` ölçülüp gönderilsin (ya da depth düşürülsün); 30 sn sınırı bu ölçümden sonra yeniden değerlendirilsin. **[kod]** |
| `serp_snapshot` | `ARGÜMAN` | Yer adının kanonik biçimi ölçüldü: DFS `serp_locations(TR, Country)` → **`Turkiye`** (`location_code 2792`) — ne `Turkey` ne `Türkiye`. Kart 10'daki doğrulama boşluğu burada **13 kredi + $0,03** olarak faturalandı: yanlış ad kayıt anında değil, ücretli çağrıda vendor tarafından reddedildi. | Yer adı `serp_locations` listesine karşı doğrulansın ve yakın eşleşme önerilsin ("Turkey → Turkiye?"). **[kod]** |
| `serp_snapshot` | — | Çıktı dürüstlüğü bu turun en yükseği: "bulunamadı" ile "ölçülemedi" **ayrı tutuluyor** (`This is NOT a statement that the domain is absent — nothing was examined`), sayılan sonuç adedi ile istenen derinlik ayrı yazılıyor, saat kaynağı adlandırılıyor (`SeoGrep's own clock … DataForSEO did not report when it measured`), `www.` soyma ve alt alan adı kuralı açıkça beyan ediliyor, ve **hiçbir skor uydurulmuyor**. **Bakıldı, bulgu yok.** | — |
| `find_quick_wins` | `ÇIKTI` | Prompt: *"Az çabayla üst sıralara çıkarabileceğim şey var mı?"* → tool doğru seçildi, veri gerçek ve zengin (pozisyon 8–20, gösterim, tıklama, CTR). Ama liste **sorgu varyantı** düzeyinde: **50 satır sayıldı → yalnız 16 farklı sayfa**. `…/20lik-dis-ne-zaman-cikar/` **12 kez**, `…/20lik-dis-agrisina-ne-iyi-gelir/` **10 kez**, `…/izmirin-en-iyi-dis-hastanesi/` **6 kez** tekrarlıyor (ör. "20 lik diş ağrısına ne iyi gelir" · "20lik diş ağrısına ne iyi gelir" · "20 lik dis agrisina ne iyi gelir" — üçü aynı sayfa). Müşterinin eylem birimi SAYFA; ilk-50 listesinin **%68'i tekrar**. | Sayfa bazında gruplama (sayfa + o sayfayı besleyen sorgular + toplam gösterim). **[kod]** |
| `find_quick_wins` | `DEĞER` | Aynı çağrı, 10 kredi. Çıktının son satırı: `…and **642 more** cleared the bands` — yani eşiği geçen 692 aday var, tool ilk 50'yi basıyor ve gerisini sayı olarak bırakıyor. Sıralama gösterime göre; ama hiçbir satırda **ne yapılacağı** yok (başlık mı, içerik mi, iç link mi). "Quick win" adı eylem vaat ediyor, çıktı tablo veriyor. | Sayfa başına tek satırlık eylem önerisi + ilk-N'in neden N olduğu. 10 kredinin karşılığı liste mi tavsiye mi — **[operatör]** + **[kod]** |
| `find_quick_wins` | — | Kesik veri dürüstlüğü **taşınıyor**: `Note: this analysis covers at most 15,000 rows per window — the pull hit that cap, so these results may be partial.` + `Search Console data pulled 2026-08-25 (today)` + analiz penceresi açıkça yazılı. `pull_gsc_data`'nın tavan uyarısı ardıl tool'da kaybolmuyor. **Bakıldı, bulgu yok.** | — |
| `detect_cannibalization` | `VERİ` | **Marka elemesi tam eşleşmeyle yapılıyor, listenin BİRİNCİ sırası markalı.** Çıktının dip notu: `Excluded **2** branded queries ("dent notion", "dentnotion"): several of your pages ranking for your own brand is normal … and is not cannibalization.` Buna rağmen listenin 1. sırası: `"dent notion menderes" — **7 competing pages, 1130 impressions**` (en yüksek gösterim). Aynı listede markalı/marka-yazım-hatası daha var: `"menderes dent notion"` (457) · `"dent nation"` (444) · `"dent notion ı menderes diş polikliniği … yorumlar"` (407) · `"… yorumları"` (171) · `"dent notion yorumları"` (139) · `"… fotoğraflar"` (138) · `"dentmotion"` (73). 60 sonucun **≥8'i** tool'un kendi ilkesine göre elenmeliydi; ilkeye uyulmuyor çünkü eleme yalnız iki DİZGİYE bakıyor. | Marka elemesi alan adı kökünden türetilsin ("dent notion" içeren/benzeyen sorgular + yazım varyantları), tam eşleşme değil. **[kod]** |
| `detect_cannibalization` | `ÇIKTI` | Aynı çağrı, 10 kredi. Veri **gerçekten değerli**: `"izmir diş beyazlatma"` → `/dis-beyazlatma-izmir/` poz. **7,7** vs `/service/tooth-whitening/` poz. **92,4**; `"implant izmir"` → `/implant-tedavisi/` **39,6** vs `/service/dental-implant-treatment/` **76,2` — İngilizce şablon sayfaları Türkçe sayfalarla çakışıyor. Ama çıktı **hangi sayfanın kalacağını / hangisine yönlendirme yapılacağını söylemiyor**; veri kararı destekliyor, tool kararı vermiyor (`find_quick_wins` ile aynı desen). | Çift başına tek satırlık eylem ("92. sıradaki sayfayı kanonikle/301"). **[kod]** |
| `detect_cannibalization` | `KAPSAM` | "Rekabet" ölçütü **pozisyon farkını dikkate almıyor**: `"diş taşı temizliği ne kadar sürer"` → poz. **2,6** ile poz. **80,7** aynı kefede "2 competing pages" sayılıyor; 80. sıradaki sayfa pratikte görünmez, gerçek yamyamlık değil (ama artık-sayfa sinyali olarak değerli olabilir). Aynı durum `"kanal tedavisi acıyor mu"` (35,4 vs 79,4) ve `"gece plağı"` (22,8 vs 43,7) satırlarında da var. | "Yakın rekabet" (ör. ikisi de ilk 20) ile "artık sayfa" ayrı başlıklara ayrılsın. **[açık]** |
| `detect_cannibalization` | — | Kesik-veri uyarısı, pencere ve çekim tarihi burada da **taşınmış**; markalı sorgu elemesinin **gerekçesi** açıkça yazılmış (Google sitelink gösterir, bu yamyamlık değil). Sıralama gösterime göre ve bu söyleniyor. **Bakıldı, bulgu yok** (eleme kapsamının kusuru yukarıda ayrı satırda). | — |
| `analyze_content_decay` | `VERİ` | **Aynı soru, iki farklı cevap — ve varsayılan olan, güvenilirliği en düşük olanı.** 90 gün (varsayılan, iki pencere de 15.000 tavanına dayalı): **12 düşen sayfa**, en büyük kayıp `/implant-agrisi-nasil-gecer/` 157→87 (−70). 30 gün (11.037 / 12.051 satır, **tavan altı**): **4 düşen sayfa**, en büyük kayıp `/zirkonyum-vs-porselen-kaplama/` 31→12 (−19). İki listenin **tek ortak sayfası** var (`/20lik-dis-ne-zaman-cikar/`). Müşteri `days` argümanını hiç yazmazsa tavana dayanan görünümü alır. **ŞERH: farkın ne kadarı tavandan ne kadarı pencere uzunluğundan geldiği bu testle AYRILAMADI ve ayrıldığı iddia edilmiyor** — ölçülen şey, cevabın müşterinin hiç dokunmadığı bir argümanla tamamen değiştiği. | Varsayılan pencere tavana dayanıyorsa tool bunu söyleyip daha kısa pencere önersin (job mesajı öneriyor, tool çıktısı önermiyor); ya da tavan altına sığan en uzun pencere otomatik seçilsin. **[kod]** |
| `analyze_content_decay` | `ÇIKTI` | 12 satırın hiçbirinde **sebep ya da eylem yok**: `"21 → 0 clicks (down 100.0%)"` deniyor ama sayfanın pozisyonu mu düştü, sorgu talebi mi azaldı, sayfa mı kaldırıldı — ayrım yok. `find_quick_wins` ve `detect_cannibalization` ile aynı boşluk, üçüncü kez: GSC ailesinin üç tool'u da 10 kredi karşılığında **tablo veriyor, tavsiye vermiyor**. | Düşüş nedeninin ayrıştırılması (pozisyon kaybı / talep düşüşü / sayfa kaybı) — veri ikisini de içeriyor. **[kod]** + 10 kredinin karşılığı **[operatör]** |
| `analyze_content_decay` | — | Tavan uyarısı **koşullu** ve doğru davranıyor: 90 günlük koşuda `Note: this analysis covers at most 15,000 rows per window — the pull hit that cap` yazdı, 30 günlük koşuda (tavan yenmedi) **yazmadı**. Kalıp metin değil, gerçek duruma bağlı. **Bakıldı, bulgu yok.** | — |
| `audit_content` | `VERİ` | **Türkçe karakter katlaması tutarsız ve katlanmış hâli müşteriye gösteriliyor.** Çıktıdan birebir: `missing "dis"` (sorgu kelimesi **diş**) · `missing "agrımayan", "curuk"` (sorgu: **ağrımayan çürük**) · `missing "saglıgı"` · `missing "kulubu"` · `missing "mavisehir"`. `ş→s`, `ç→c`, `ü→u`, `ğ→g` katlanıyor ama **`ı` korunuyor** (`agrımayan`, `farkı`) — yani katlama yarım, ve müşteri Türkçede var olmayan kelimeler okuyor. | Katlama ya tam ya hiç; **gösterim her hâlükârda sorgunun ORİJİNAL yazımıyla** yapılsın. **[kod]** |
| `audit_content` | `ÇIKTI` | Aynı çağrı, 12 kredi. "Eksik kelime" listesi **işlev sözcükleriyle dolu**: `missing "daha", "iyi"` (13 satırda), `missing "mı"`, `missing "yoksa"`, `missing "hangi"`. Bir başlığa "mı" veya "daha" eklemek eyleme dönüşebilir bir tavsiye değil. Ayrıca **50 satırın 33'ü aynı sayfa** (`/zirkonyum-vs-porselen-kaplama`) — `find_quick_wins`'teki sayfa-gruplama boşluğunun aynısı, bu sefer daha yoğun (%66). | Durak/işlev sözcük listesi (TR+EN) + sayfa bazında gruplama. **[kod]** |
| `audit_content` | `DEĞER` | **Denetim, çiftlerin %85'ine hiç bakmıyor** ve bunu dürüstçe yazıyor: `Checked **1,065 of 6,972** query/page pairs against **20 of the 26** crawled pages. **5,907 could not be checked** because the page drawing them was not in the crawl`. Darboğaz crawl kapsamı (26 sayfa; `crawl_site` tavanı 100). 12 kredi, kapsamanın altıda birine ödeniyor ve müşteri bunu ancak son satırda öğreniyor. | Kapsama oranı çıktının BAŞINDA verilsin; crawl tavanı ile denetim kapsamı arasındaki bağ ürün kararı. **[operatör]** + **[kod]** |
| `audit_content` | `KAPSAM` | **Marka elemesi tool'lar arasında tutarsız.** `detect_cannibalization` markalı sorguları eleyip gerekçesini yazıyor; `audit_content` elemiyor: `"dent notion" → https://dentnotion.com/seferihisar-dis-klinigi — 137 impressions; missing "dent", "notion" (0/2 words present)` — markanın kendisi "eksik kelime" olarak raporlanıyor ve rastgele bir alt sayfaya bağlanmış. | Marka elemesi tek bir ortak yerde tanımlanıp GSC ailesinin tamamında kullanılsın. **[kod]** |
| `audit_tech` | `ÇIKTI` | **"Bozuk iç link" bölümünün 25/25'i aynı hedef.** Çıktı `Broken internal links (target crawled, answered 4xx/5xx): **25**` diyor; 25 satırın hepsi `→ https://dentnotion.com/cdn-cgi/l/email-protection (404)`. Aynı artefakt tek 4xx sayfası ve robots çakışmalarının da başı (`linked from 25 page(s)`). 15 kredilik denetimin en büyük bölümü **tek bir Cloudflare uç noktasının 25 kez tekrarı**; kart 12'de `audit_schema`'nın tek eylem maddesi de aynı URL'ydi. | `/cdn-cgi/*` gibi altyapı yolları crawl'da elensin (kök: `crawl_site`); ya da bozuk linkler **hedefe göre gruplansın** ("1 hedef, 25 kaynak"). **[kod]** |
| `audit_tech` | `VERİ` | **Crawl'ın gerçek sınırı burada görünüyor ve sebebi sayfa tavanı değil:** `Technical audit — **26 page(s), 158 skipped**` · `limit: **154** … (time budget exhausted)`. `crawl_site` şemasında `max_urls` varsayılanı **100**, ama crawl 26'da durmuş — durduran şey URL tavanı değil **süre bütçesi**. Kart 18'in "5.907 çift kontrol edilemedi" bulgusunun kökü bu. Müşteri 20 kredilik crawl'ın neden %14 kapsadığını yalnız bu tool'u ayrıca satın alırsa görüyor. | Süre bütçesi tükendiğinde `crawl_site`'ın KENDİ çıktısı bunu söylesin ve devam/yeniden-tara yolu önersin. **[kod]** |
| `audit_tech` | — | **Gerçek ve yüksek değerli bulgu üretti:** `Robots conflicts (noindex but internally linked): 5` — `/dis-cekimi` (23 sayfadan linkli), `/restoratif-dis-tedavisi` (23), `/gulus-tasarimi` (23), `/genel` (7). Site içinde güçlü şekilde linklenen sayfalar dizine kapalı; müşterinin kendi başına fark etmesi zor, doğrudan eyleme dönen bir kusur. Ayrıca 2 yönlendirme ve atlama sebepleri (`limit` / `redirect` / `robots`) ayrı ayrı adlandırılmış. **Bakıldı, bulgu yok.** | — |
| `audit_tech` | `ÇIKTI` | Atlanan URL listesi çıktıda **50 satır** basılıp `… and 104 more` ile kesiliyor; 154 satırın hepsi aynı sebebi (`time budget exhausted`) taşıyor. Ekranın büyük kısmı tek bilgiyi tekrar ediyor: "crawl yetişemedi". | Sebep bazında özet ("154 URL: süre bütçesi") + tam liste isteğe bağlı. **[açık]** |
| `audit_speed` | `ÇIKTI` | **Geçen bir Lighthouse denetimi "iyileştirme fırsatı" olarak basılıyor.** Çıktı birebir: `Biggest opportunities … - **Initial server response time was short** — an estimated 141 ms saved`. "was short" Lighthouse'un BAŞARILI audit başlığı. Kod bunu elemeye çalışıyor (`dfs/lighthouse.ts:305-318`, iki filtre) ve gerekçeyi yazmış: *"Lighthouse emits passing opportunity audits with `overallSavingsMs: **0**`"* — üretim verisi bu varsayımı **çürüttü**, geçen audit **141 ms** ile geldi. Kart 14'teki `max_crawl_pages` ile aynı sınıf: yorumda yazılı, ölçülmemiş bir varsayım. | Eleme `overallSavingsMs`'e değil audit'in **score/scoreDisplayMode**'una baksın. **[kod]** |
| `audit_speed` | `KAPSAM` | **Ölçümün cihaz profili ne gönderiliyor ne söyleniyor.** `apps/mcp/src/dfs/lighthouse.ts` içinde `strategy` / `mobile` / `desktop` / `device` araması **0 sonuç**; çıktıda da cihaz satırı yok. Mobil ve masaüstü Lighthouse skorları aynı sayfa için çok farklıdır (LCP 1,8 sn masaüstünde iyi, mobilde başka). Aynı üründe `serp_snapshot` cihaz ayrımını ısrarla vurguluyor (`says nothing about a mobile ranking`) — **rigor tool'lar arasında tutarsız**. | Cihaz profili açıkça gönderilsin ve çıktıya yazılsın; tercihen argüman olarak sunulsun. **[kod]** |
| `audit_speed` | — | **DFS canlı yolu burada ÇALIŞIYOR** — aynı istemci, farklı uç: kredi −15, vendor `dfs_spend_today_usd()` $0,102480 → **$0,107480 (+$0,005)**, ve gerçek Lighthouse verisi döndü (skor 87/100, FCP 1,5 sn, LCP 1,8 sn, TBT 0 ms, CLS 0,003). En büyük fırsat gerçek ve `audit_tech`'in yönlendirme bulgusuyla tutarlı: `Avoid multiple page redirects — **1.611 ms**`. Lab uyarısı açıkça yazılı. **Bakıldı, bulgu yok** — ve bu, kart 14'ün kök nedenini güçlendirir: taşıma katmanı sağlam, kusur SERP isteğinin şeklinde. | — |
| `generate_report` | `VERİ` | **Marka gürültüsü MÜŞTERİYE GÖSTERİLEN rapora taşınıyor.** Public rapor (https://seogrep.com/r/NfW7wtr8niq) tarayıcıda açıldı ve okundu. `CANNIBALIZED QUERIES` bölümünün **ilk üç satırı**: `dent notion menderes — 6 competing pages` · `menderes dent notion — 6` · `dent nation — 5`; hemen altında ise `**Excluded 2 branded queries**: … is not cannibalization` yazıyor. Kart 16'daki eleme kusuru, ürünün **paylaşılabilir çıktısında** — yani müşterinin kendi müşterisine gösterdiği yerde — tekrar ediyor. | Marka elemesi tek ortak yerde tanımlansın (kart 16 + kart 18 ile aynı chip). Rapor en yüksek görünürlüklü yüzey, önceliği yüksek. **[kod]** |
| `generate_report` | `ÇIKTI` | Aynı rapor: `BROKEN INTERNAL LINKS (**25**)` bölümünün tamamı `→ /cdn-cgi/l/email-protection (404)`. `audit_schema` (k12), `audit_tech` (k19) ve şimdi paylaşılan rapor — **üç ayrı ücretli yüzeyde aynı Cloudflare artefaktı** birinci sıradaki 'sorun' olarak duruyor. | Crawl'da `/cdn-cgi/*` elenmesi tek düzeltmeyle üç yüzeyi birden temizler. **[kod]** |
| `generate_report` | — | **Vaatlerini tutuyor ve dürüst.** Link kimlik doğrulaması olmadan açıldı ✅; rapor crawl + teknik + schema + GSC + fırsat bölümlerini tek sayfada topluyor; tazelik AÇIKÇA yazılı (`Crawl from 2026-08-09 (**16 days ago**)`) — `whats_next`'in "fresh" dediği veriyi burada gün cinsinden söylüyor; her bölüm hangi tool'la derinleştirileceğini gösteriyor; ve gizlilik notu turun en dürüst cümlelerinden: `The owner can revoke the link … **but cannot un-share what has already been read.**` **Bakıldı, bulgu yok.** | — |
| `generate_report` | `KAPSAM` | Rapor **yalnız crawl + GSC** kapsıyor (açıklama da böyle diyor, yani yanlış beyan yok): sayfa hızı (`audit_speed`, aynı oturumda ölçüldü ve gerçek bulgu üretti: 1.611 ms yönlendirme kaybı) ve sıralama takibi bölümleri **yok**. Müşteriye/ajans müşterisine gösterilen tek belgede hız verisi bulunmuyor. Ayrıca 15 kredilik rapor, 30 kredilik `audit_onpage`'in bulgu SAYILARINI veriyor (`5 — missing meta description`, `5 — missing canonical`, `1 — thin content`). | Rapora hız bölümü eklenmesi ürün kararı; 15↔30 kredi kapsam örtüşmesi fiyat kararı (NEVER#6). **[operatör]** |
| `crawl_site` | `VERİ` | **Harcama kararı anında gösterilen sayı ~8 kat yanlış.** Kuyruk mesajı birebir: `**~28 pages discovered**; this crawl covers up to 100 of them (20 credits).` Gerçek sonuç: `Crawled **100** page(s), skipped **122**` ve depoda `crawl_pages` **222 satır**. Müşteri 20 krediyi "28 sayfalık site" bilgisiyle onaylıyor, site en az 222 URL. | Ön-keşif sayısı ya gerçek keşifle hizalansın ya da "en az N" diye sunulsun. **[kod]** |
| `crawl_site` | `VERİ` | **Aynı fiyata değişen kapsam, ve kendi çıktısı bunu söylemiyor.** 2026-08-09 koşusu **26** sayfada durdu (`audit_tech`: `limit: 154 … time budget exhausted`); bugünkü koşu aynı sitede aynı ayarlarla **100** sayfaya ulaştı (78 sn). Yani 20 kredilik iş bir gün %14, başka gün %45 kapsıyor ve **crawl'ın kendi çıktısında bu fark hiç görünmüyor** — müşteri ancak 15 kredilik `audit_tech`'i alırsa öğreniyor. Kart 18'in "%85 kontrol edilemedi" bulgusunun kökü buydu. | Süre bütçesi tükendiğinde `crawl_site`'ın kendi bitiş mesajı bunu söylesin (`get_job_status` bugün "max URL limit reached" diyebiliyor — aynı yerde "time budget" de denebilir). **[kod]** |
| `crawl_site` | `KAPSAM` | **`/cdn-cgi/` gürültüsünün kökü ölçüldü:** depoda `crawl_pages` içinde `url like '%/cdn-cgi/%'` → **1 satır**. Bu tek satır `audit_schema`'nın tek eylem maddesi (k12), `audit_tech`'in 25/25 kırık linki (k19) ve paylaşılan raporun kırık-link bölümü (k21) olarak **üç ücretli yüzeyde** görünüyor. Crawl'da tek bir yol filtresi üçünü birden temizler. | `/cdn-cgi/*` ve benzeri altyapı yolları crawl'da elensin. **[kod]** |
| `crawl_site` | `ÇIKTI` | Asenkron iş **ilerleme sinyali vermiyor**: iş koşarken `get_job_status` iki kez çağrıldı, ikisinde de birebir aynı satır döndü (`is running. created … started …`) — kaç sayfa tarandı, ne kadar kaldı yok. Müşteri "takıldı mı" ile "çalışıyor" ayrımını yapamıyor. Buna karşılık **bitiş mesajı iyi**: `Crawled 100 page(s), skipped 122, 13 issue(s) found (mostly: max URL limit reached)`. | Koşarken taranan sayfa sayacı. **[kod]** |
| `research_keywords` | `VERİ` | **Sorulan kelime sessizce kayboluyor — iki bağımsız çağrıda.** (1) Girdi `["diş beyazlatma", "implant fiyatları", "zirkonyum kaplama"]` → çıktı `Search volume for **2** keywords` ve listede yalnız `diş beyazlatma`, `zirkonyum kaplama`; **`implant fiyatları` hiçbir yerde yok**. (2) Girdi `["implant", "ortodonti"]` → çıktı `Search volume for **1** keyword`, yalnız `ortodonti`; `implant` yok. Tool veri bulamadığı kelimeleri normalde AÇIKÇA yazıyor (`— no data returned for this keyword`), yani davranış **tutarsız**: bazı veri-yok kelimeleri listeleniyor, bazıları siliniyor. Vendor tarafı doğrulandı: DFS'e doğrudan atılan aynı üç kelimenin **üçü de** döndü (boş gövdeyle). | Sorulan her kelime çıktıda görünsün; hiçbiri sessizce düşmesin. **[kod]** |
| `research_keywords` | `DEĞER` | **Türkiye pazarında vendor'da hiç anahtar kelime verisi yok, ama tam ücret alınıyor.** Üç çağrı, **75 kredi + ~$0,037** vendor; dönen sayı: sıfır. Doğrudan DFS ölçümü (`keywords_data_google_ads_search_volume`, `Turkiye`/`tr`): `items` içinde yalnız `keyword` + `location_code`, **`search_volume`/`cpc`/`competition` alanları YOK**. Karşılaştırma: aynı tool ABD/İngilizce'de tam veri döndürdü (`teeth whitening — volume 165,000, CPC $5.42, competition HIGH, difficulty 70/100, intent informational, trend +23% MoM`). Ürün Türkiye'de satılıyor ve bu tool orada boş dönüyor. | Veri gelmeyen pazar/dil için (a) çağrı öncesi uyarı, (b) boş sonuçta ücret alınmaması — `keyword_positions`'ın "nothing to read, not charged" davranışı emsal. Fiyat/iade politikası **[operatör]** + uyarı **[kod]** |
| `research_keywords` | — | **Bayatlık dürüstlüğü ölçüldü ve doğru çalışıyor:** TR çağrısı `CPC and competition were last refreshed by DataForSEO on 2026-06-15 (**71 days ago**). **This vendor data is stale** — treat CPC and competition as indicative, not current.`; ABD çağrısı `… on 2026-08-14 (**10 days ago**)` ve **bayat uyarısı yok**. Uyarı kalıp metin değil, gerçek tarihe bağlı. **Bakıldı, bulgu yok.** | — |
| `audit_onpage` | `KAPSAM` | **Bozuk başlık karakteri hiçbir kuralın kapsamında değil.** Kart 18'de `audit_content` çıktısı bu başlığı gösterdi: ``Current title: "`İzmirde Diş Beyazlatma Merkezleri 2026 | Fiyat & Yöntemler"`` — başında kaçak bir **ters tırnak** var (gerçek içerik hatası, muhtemelen editörden kalma). `audit_onpage`'in 100 sayfalık çıktısında `/dis-beyazlatma-izmir` **hiç geçmiyor** = temiz sayılmış. Kurallar (`audit/rules/onpage.ts`) uzunluk, tekrar, eksiklik ve hiyerarşiye bakıyor; **içeriğin bozukluğuna bakan kural yok**. | Başlık/meta için biçim kuralı (baştaki-sondaki noktalama, kod artığı, kodlama bozukluğu). **[kod]** |
| `audit_onpage` | `ÇIKTI` | **Eşikler müşteriye söylenmiyor ve karakter bazlı.** Kaynak: `TITLE_MAX = **60**`, `META_MIN/MAX = **50/160**`, `THIN_CONTENT_WORDS = **200**` (`audit/rules/onpage.ts:15-21`). Çıktı `title too long (**62** chars)` diyor ama **sınırın 60 olduğunu söylemiyor**; 17 "title too long" bulgusunun bir kısmı sınırı 1-2 karakter aşıyor. Google başlıkları karakterle değil **piksel genişliğiyle** kırpar, dolayısıyla 61 karakterlik bir başlık "uzun" olmayabilir. | Eşik çıktıda yazılsın (`62/60`); uzun vadede piksel-genişliği tahmini. **[kod]** |
| `audit_onpage` | — | **Turun en yüksek değerli denetimi.** 100 sayfa, 10 kusur tipi, `45 page(s) with findings; **55 clean**`, ve her bulgu **sayfa bazında + sayı ile**: `meta description too short (**10 chars**)`, `thin content (**120 words**)`, `heading hierarchy gap (**11 h3 with no h2**)`, `title duplicates the h1 exactly`. Bulgular gerçek ve eyleme dönük; "temiz" sayfa sayısını da veriyor (yalnız kusur listelemiyor). 30 kredi karşılığında kapsam ve derinlik tutarlı. **Bakıldı, bulgu yok.** | — |
| `audit_onpage` | `ÇIKTI` | `/cdn-cgi/l/email-protection` **bu denetimin de ilk bulgusu** (4 kusurla listenin başında). Aynı Cloudflare artefaktı böylece `audit_schema` · `audit_tech` · paylaşılan rapor · `audit_onpage` = **dört ücretli yüzeyde** birinci sıradaki 'sorun'. Ayrıca 45 sayfalık liste **öncelik sırasına göre değil crawl sırasına göre**; "11 h3 with no h2" ile "meta 1 karakter kısa" aynı görsel ağırlıkta. | Crawl'da `/cdn-cgi/*` elemesi (k22 ile aynı chip) + bulguların ciddiyete göre sıralanması. **[kod]** |
| `backlink_changes` | — | **Veri gerçek, zengin ve dürüstlük notu turun en iyilerinden.** 13 aylık iki seri döndü: yeni/kayıp (ör. `2025-12-31 — 44 new / 0 lost backlinks · 30 new / 0 lost referring domains`) ve profil anlık görüntüsü (`2026-08-31 — 239 backlinks · 136 referring domains · rank 170 of 1,000`). Kapanış notu iki seriyi birleştirmeyi AÇIKÇA reddediyor: *"These are two separate DataForSEO measurements, counted against different definitions … Subtracting one from the other does not reproduce the other, so **SeoGrep does not print a combined figure**"*. Vendor tanımları da yazılı ("new" ve "lost" ne demek). **Bakıldı, bulgu yok.** | — |
| `backlink_changes` | `ÇIKTI` | **Soru "arttı mı azaldı mı", cevap 26 satır ham seri.** Çıktıda hiçbir özet/trend cümlesi yok; oysa veri açık bir hikâye taşıyor: profil 12 ayda **45 → 239** backlink, **33 → 136** referring domain; ama **2026-05'te ilk kez kayıp>kazanç** (8 new / 9 lost) ve profil o ay **223 → 222** düştü, sonraki iki ay da kayıplar sürdü. Bu dönüş noktası hiçbir yerde işaret edilmiyor. Ayrıca ay kovaları `2025-08-31 **00:00:00 +00:00**` biçiminde 26 kez basılıyor (ay kovası için saat bilgisi anlamsız), ve iki seri yan yana değil arka arkaya veriliyor — müşteri tarihleri elle eşleştiriyor. | Tek satırlık trend özeti + dönüş noktası işareti; kova etiketlerinden saat düşürülsün; iki seri tek tabloda yan yana (**birleştirilmiş sayı üretmeden** — mevcut dürüstlük kuralı korunarak). **[kod]** |
| `backlink_changes` | `DEĞER` | Vendor maliyeti ÖLÇÜLDÜ: `dfs_spend_today_usd()` $0,119720 → **$0,192896**, yani tek çağrı **+$0,0732** (kredi −35). Bu, turun tek çağrıda en pahalı kalemi — `audit_speed` +$0,005, `research_keywords` +$0,0122, `serp_snapshot` +$0,03 ile karşılaştırıldığında. | Marj dosyası için ölçüm; fiyat kararı NEVER#6 kapsamında. **[operatör]** |
| `backlink_details` | `ARGÜMAN` | **"limit = fiyat kontrolü" iddiası ölçümle uyuşmuyor.** Şema: *"DataForSEO bills per returned row, so this is **the price control**, not a display preference"*. İki çağrı, aynı hedef: `limit 10 / page_limit 5` → vendor $0,192896→$0,241436 (**+$0,04854**); `limit 200 / page_limit 9` (187 satır döndü) → $0,241436→$0,296492 (**+$0,05506**). **19 kat satır, %13 maliyet artışı** — maliyet neredeyse sabit. Kredi ikisinde de 35. Müşteri "para harcamayayım" diye pencereyi dar tutuyor, oysa dar pencere neredeyse aynı parayı ödüyor ve daha az veri veriyor. | Şemadaki fiyat cümlesi ölçülen davranışa göre düzeltilsin (sabit taban + küçük satır bileşeni). **[kod]** |
| `backlink_details` | `KAPSAM` | **Şemanın izin verdiği argüman, istemciye sığmayan çıktı üretiyor — ve ücret alınıyor.** `limit: 200, page_limit: 9` çağrısı MCP yanıt sınırını aştı: `result (**62,729 characters across 404 lines**) exceeds maximum allowed tokens`. Şema `limit`e **700**'e, `page_limit`e **200**'e izin veriyor; 187 satırda zaten kırılıyor. Kredi (35) ve vendor ($0,055) tahsil edildi, müşterinin istemcisi cevabı **hiç göremedi**. | Çıktı boyutu sınırlanmalı (satır başına daha kısa biçim / otomatik sayfalama) ya da şema sınırları gerçekçi tavana çekilmeli. **[kod]** |
| `backlink_details` | `VERİ` | "Bu sitenin link kazanan sayfaları" listesinde **9 sayfanın 3'ü aynı ana sayfanın varyantı**: `https://dentnotion.com/` (172 backlink) · `https://www.dentnotion.com/` (18) · `http://www.dentnotion.com/` (17). Vendor'ın kendi ayrımı, ama çıktı bunları tek sayfa olduğuna dair **hiçbir not düşmüyor**; müşteri "9 sayfam link alıyor" diye okuyor. Kart 1'deki `www.` ekseninin vendor tarafındaki yansıması. | Protokol/`www` varyantları için tek satırlık not ya da gruplama (vendor sayısını DEĞİŞTİRMEDEN). **[kod]** |
| `backlink_details` | — | Dar pencerede veri zengin ve dürüst: her link için kaynak sayfa, anchor, dofollow, vendor rank, spam skoru, hedef sayfa HTTP durumu, ilk/son görülme. Pencere ile toplam **açıkça ayrılıyor** (`10 backlinks in this window (offset 0, limit 10). DataForSEO counts **187** … this window is a slice of that set, **not a count of it**`). Kapanış notu iki spam skorunun **farklı nesnelere ait** olduğunu vendor adlarıyla söylüyor ve `SeoGrep adds **no link-quality verdict** of its own`. **Bakıldı, bulgu yok.** | — |
| `disavow_candidates` | — | **Turun en yüksek gerçek-dünya değeri.** Prompt: *"Zararlı olabilecek link var mı?"* → tool doğru seçildi (eşik zorunlu: 15 verildi). Çıktı, siteye yönelik **canlı bir spam akınını** ortaya çıkardı: altı ayrı alan adından, hepsi **2026-08** tarihli, anchor metni birebir `"High Quality Dofollow Backlinks DA 50 PA 40 **Premium PBN Network Service** dentnotion.com … **Buy Backlinks Online Cheap**"`; ağ bölümü doğruluyor: `185.244.145.*` → **31 backlink · 13 referring domain** tek subnet. Tasarım ilkeleri de tutuyor: eşik varsayılansız ve zorunlu (*"a default here would be an invented number wearing a recommendation's clothes"*), üç ayrı vendor skoru kendi adlarıyla ve **birleştirilmeden**, skorlanmamış alan `unreported` (asla 0 değil), ve `PROPOSAL ONLY — SeoGrep has not sent this to Google`. **Bakıldı, bulgu yok.** | — |
| `disavow_candidates` | `ÇIKTI` | **Disavow dosyasının satır etiketi, o satırı disavow ETMEMEYİ savunuyor.** Pencere `backlink_spam_score >= 15` ile süzülüyor, ama dosya satırları **başka bir alanla** (`spam_score`, alan adı düzeyi) etiketleniyor. Sonuç: dosyanın sonunda `# spam_score **0**` / `domain:mail.runningwebsites.net`, `# spam_score **1**` / `domain:booksreadr.org`, `# spam_score 1` / `domain:globalecommerce.org` satırları var — üçünün de link-düzeyi skoru **60**. Müşteri "spam_score 0" yazan bir satırı silmesi mi gerektiğini anlayamıyor. Tool iki alanın farkını açıklıyor (dürüst), ama dosyada **yan yana gösterilen tek sayı** yanlış tarafı işaret ediyor. | Disavow satırında her iki skor da yazılsın (`# domain spam_score 0 · worst link spam_score 60`). **[kod]** |
| `disavow_candidates` | `KAPSAM` | **Varsayılan pencere, tanımı gereği zarar veremeyecek linkleri içeriyor.** `dofollow_only` varsayılanı `false`; ölçüm: 46 adayın **21'i** "0 marked dofollow" (yalnız nofollow linkler) — ör. `urls-shortener.eu`, `bye.fyi`, `anyflip.com`, `partecipa.poliste.com`. Google nofollow linkleri zaten saymaz; disavow dosyasına girmelerinin karşılığı yok. Müşteri 40 kredilik çıktının yarısını gereksiz satırlarla alıyor. | `dofollow_only` varsayılanı tartışılsın; en azından çıktı "bu N aday nofollow" diye ayırsın. **[açık]** |
| `disavow_candidates` | `DEĞER` | Vendor maliyeti ölçüldü: $0,296492 → **$0,372308**, tek çağrı **+$0,0758** (kredi −40). Bu, `backlink_changes` (+$0,0732) ile birlikte turun en pahalı iki çağrısı. | Marj dosyası; fiyat NEVER#6. **[operatör]** |
| `discover_keywords` | `VERİ` | **Ürünün en açık vaadi çürütüldü: raporlanmayan alan SIFIR olarak basılıyor.** Çıktının kapanış cümlesi: *"A field DataForSEO did not report is shown as **unreported, never as a zero**."* Ölçüm: `suggestions` modunda 8/8 satır `keyword_difficulty **0**`, `related` modunda 5/5 satır `keyword_difficulty **0**`. Vendor'a AYNI sorgu doğrudan atıldı (`dataforseo_labs_google_keyword_suggestions`, `diş beyazlatma`, Turkiye/tr): dönen `keyword_properties` **`keyword_difficulty` alanını hiç içermiyor** (yalnız `synonym_clustering_algorithm`, `detected_language`, `words_count`, `core_keyword`). İkinci ihlal aynı satırlarda: bizim çıktı `diş beyazlatma fiyatı` için `search_volume_trend **monthly 0%, quarterly 0%**, yearly -45%` bastı; vendor gövdesinde `search_volume_trend: { yearly: -45 }` — monthly ve quarterly **hiç yok**. İki alan, iki mod, tek vaat: uydurulmuş sıfır. | Raporlanmayan alanlar `unreported` basılsın (vaadin gereği). Bu, ürünün çekirdek farklılaştırıcısı — imzalı ders 12'nin sınıfı. **[kod]** |
| `discover_keywords` | `DEĞER` | **İki mod bu müşteri için tamamen gürültü.** `for_site` (dentnotion, TR/tr, 15 satır): dönenler `çeviri` (55,6M), `hava durumu`, `e devlet`, `haberler`, `harem altın`, `altın fiyatı`, `namaz vakitleri`, `son dakika` — bir diş kliniği için **15/15 alakasız**; sebep, vendor sırasının `search_volume` azalan olması ve 2.579.883 kelimelik havuzun tepesinde ulusal ölçekli sorguların olması. `ideas` (seeds: `diş beyazlatma`+`implant`): `hipp combiotic 1`, `trigliserit polimer mi`, `glp-1 agonistleri`, `dermoeczanem orjinal mi`, `testosteron jeli` — **5/5 alakasız**. Buna karşılık `suggestions` **8/8** ve `related` **5/5** alakalı ve zengin. Aynı 40 kredi, dört moda; ikisi kullanılabilir, ikisi değil. | Gürültülü modlarda varsayılan bir alaka/hacim tavanı ya da uyarı; mod seçimi rehberi. Fiyatın moda göre farklılaşıp farklılaşmayacağı **[operatör]**, uyarı **[kod]** |
| `research_keywords` | `KAPSAM` | **Aynı ürün, aynı pazar: bir tool kör, diğeri dolu.** Kart 23'te `research_keywords` (25 kredi) `diş beyazlatma` / TR / tr için `no data returned for this keyword` demişti (kaynak: Google Ads search_volume ucu, vendor gövdesi boş). Bu kartta `discover_keywords` (40 kredi, DataForSEO **Labs** ucu) aynı kelime için **`diş beyazlatma fiyat — search_volume 12.100 · cpc 0,84 · competition 0,54 MEDIUM · intent commercial`** döndürdü ve vendor'a doğrudan atılan sorgu bunu **12 aylık `monthly_searches` geçmişiyle** doğruladı. Yani Türkçe hacim verisi vendor'da VAR; `research_keywords` yalnız verinin olmadığı uçtan soruyor. | `research_keywords` Labs ucuna taşınsın ya da veri gelmediğinde Labs'e düşsün. Kart 23'teki "TR'de ücret alınıyor, veri yok" bulgusunun çözümü budur. **[kod]** |
| `discover_keywords` | — | Dört modun dördü de **hangi vendor ucunu çağırdığını adıyla** yazıyor (`keywords_for_site`, `keyword_suggestions`, `related_keywords`, `keyword_ideas`), her mod ne döndürdüğünü tarif ediyor, `mode` zorunlu ve varsayılansız, başka moda ait alan **reddediliyor** (yok sayılmıyor), pencere↔toplam ayrımı her çıktıda var (`2.579.883 … this window is a slice of that set, not a count of it`), ve `competition` ile `competition_level`'ın iki ayrı vendor alanı olduğu belirtilip birleştirilmiyor. **Bakıldı, bulgu yok** (sıfır-basma kusuru yukarıda ayrı satırda). | — |
| `my_pages` | `ÇIKTI` | **İstenen 12 satıra karşılık 93 satırlık "yok" listesi basılıyor, ve o bölümün limiti yok.** `limit: 12` ile çağrıldı; çıktı 12 sayfayı verdi, sonra `Reported by DataForSEO, not found in that crawl (**5**)` ve `Fetched by that crawl, not named in this window (**93**)` bölümlerini bastı — üçüncü bölüm crawl büyüklüğüyle (100 sayfa) ölçekleniyor, `limit` onu **kontrol etmiyor**. Çıktının dörtte üçü, müşterinin sormadığı bir yokluk listesi. | Üçüncü bölüm için ayrı limit ya da özet sayı ("93 sayfa bu pencerede adı geçmedi — listelemek için X"). **[kod]** |
| `my_pages` | `ÇIKTI` | **Tahmin değerleri 14 ondalık basamakla basılıyor:** `etv **86.03599891066551**` · `estimated_paid_traffic_cost **5.054940015077591**` · `etv 127.16400071978569`. Aynı çıktı bu alanların *"DataForSEO's OWN ESTIMATES … not measurements of your traffic"* olduğunu söylüyor; 14 basamaklı bir tahmin, tam tersi bir kesinlik izlenimi veriyor. | Tahminler anlamlı basamağa yuvarlansın (ör. `etv 86,0`). **[kod]** |
| `my_pages` | — | **Üç yönlü karşılaştırma ve normalizasyon beyanı örnek.** Tool üç kümeyi ayrı ayrı veriyor (vendor+crawl / vendor ama crawl'da yok / crawl'da ama bu pencerede yok) ve her birinin **ne anlama GELMEDİĞİNİ** yazıyor (`not a statement that the page does not rank`). URL eşleştirme kuralı harfiyen beyan edilmiş (şema, `www.`, tek sondaki eğik çizgi yok sayılır; query sırası, path büyük/küçük harfi ve alt alan adı korunur) ve çalıştığı ölçüldü: DFS'in `/20lik-dis-ne-zaman-cikar/` satırı crawl'ın `/20lik-dis-ne-zaman-cikar` satırıyla doğru eşleşti. Pozisyon histogramı ortalamaya **çevrilmiyor**, gerekçesiyle: *"because DataForSEO did not send one"*. **Bakıldı, bulgu yok.** | — |
| `my_pages` | `KAPSAM` | **Kart 22'nin crawl tavanının bedeli burada sayıya döküldü:** sitenin en yüksek tahmini trafikli sayfası `…/hassas-disler-icin-en-iyi-dis-macunu…` (**etv 127,2**, pos_2_3'te 1 sonuç) `Reported by DataForSEO, **not found in that crawl**` listesinde. Yani 100 sayfalık crawl, sitenin en değerli sıralayan sayfasını almamış; ona bağlı bütün denetimler (onpage, schema, content) o sayfayı hiç görmedi. | Crawl önceliklendirmesi: DFS'in sıralayan sayfa listesi crawl'a tohum olarak verilebilir. **[açık]** |
| `keyword_gap` | — | **Turun en okunur çıktısı.** Prompt: *"Rakibimin aldığı ama benim almadığım aramalar neler?"* → tool doğru seçildi. Biçim insan diline çevrilmiş ve yuvarlanmış: `"dentist near me" — **9,900 searches/mo** · citydentistanbul.com **ranks #48** · difficulty 69 of 100 · **CPC $2.28** (MEDIUM competition)` + sıralayan URL + `an estimated **21 visits/mo**`. Başlık pencere↔toplam ayrımını veriyor (`**12 of 58** keywords`). **Bakıldı, bulgu yok.** | — |
| `keyword_gap` | `ÇIKTI` | **Aynı üründe iki ayrı sunum standardı, ölçülmüş karşılaştırma:** bu tool `an estimated 21 visits/mo` ve `9,900 searches/mo` yazarken, `my_pages` aynı cins tahmini `etv **86.03599891066551**` diye basıyor; `discover_keywords` ise ham vendor alan adlarını (`search_volume 12,100 · competition 0.54 · competition_level MEDIUM`) basıyor. Üç ücretli tool, üç farklı okunabilirlik seviyesi. | Ortak bir sayı/birim biçimleme katmanı. **[kod]** |
| `keyword_gap` | `VERİ` | 12 satırın **7'sinde** `difficulty **0** of 100` (`"dentist clinics"`, `"dentist clinic"`, `"dental clinic"`, `"vsc dental clinic"`, `"monolitik zirkonya"`, `"cebeci dent"`, `"dentistanbul"`), 4'ünde gerçek değer (69, 52, 36, 6). Kart 28'de aynı ürünün raporlanmayan `keyword_difficulty`'yi **0 olarak bastığı ölçüldü** (vendor gövdesi alanı hiç içermiyordu) — **ama bu uç (`domain_intersection`) için AYNI ölçüm yapılmadı**, dolayısıyla buradaki sıfırların gerçek mi raporlanmamış mı olduğu **bilinmiyor**. "Difficulty 0" müşteri tarafından "bedava kazanılır" diye okunur; ayrım kritik. | Kart 28'in düzeltmesi bu uçta da doğrulansın; sıfır ile raporlanmamış ayrımı her uçta test edilsin. **[açık]** |
| `link_gap` | `KAPSAM` | **"Outreach kısa listesi" satırlarında örnek URL yok — kardeş tool'da var.** Şema tool'u *"the **outreach shortlist**"* diye tanımlıyor ve söz verdiği beş alanı (rank, canlı backlink sayısı, kaç sayfadan, spam skoru, ilk görülme) eksiksiz veriyor. Ama iletişime geçilecek **hangi sayfa** link veriyor, hangi bağlamda — yok. Aynı üründe `disavow_candidates` her aday alan adı için `example: <URL> → <hedef>` satırı basıyor; `backlink_details` kaynak sayfa + anchor veriyor. Outreach için gereken asgari bağlam bu tool'da eksik. | Aday başına bir örnek kaynak URL (kardeş tool'larda zaten var). **[kod]** |
| `link_gap` | `ÇIKTI` | **Liste "güçlüden zayıfa" sıralı ama outreach değeri filtrelenmemiş.** 12 satır: `citydent.com.tr` rank 104 · **112 backlink / 28 sayfa** — rakiple **marka adını paylaşıyor** (`citydent`), editoryal link değil kardeş-site deseni (mülkiyet ÖLÇÜLMEDİ); `ugain.online` **spam score 35**; `kiwibox.com` rank **7**; `hindibday.com`, `feri24.com`, `glomend.com` rank 23. Yani "en güçlü 12" adayın üst sırası ulaşılamaz, alt sırası değersiz. Sıralama vendor rank'ine göre ve bu doğru beyan ediliyor — eksik olan, outreach'e uygunluk ekseni. | Marka/kardeş alan adı ve yüksek spam skorlu adaylar işaretlensin (elenmesin — işaretlensin). **[açık]** |
| `link_gap` | — | Başlık pencere↔toplam ayrımını veriyor (`**12 of 193** domains that link to citydentistanbul.com and not to your project`), sıralama iddiası ölçüldü ve tuttu (rank 104 → 68 → 51×6 → 23×3 → 7, azalan), her satırda vendor alanları kendi adlarıyla ve **birleştirilmemiş** hâlde. **Bakıldı, bulgu yok.** | — |
| `ranked_keywords` | `VERİ` | **"Raporlanmayan asla sıfır basılmaz" ihlali İKİNCİ uçta da kanıtlandı — ve renderer'ın doğrusunu yapabildiği aynı satırda görülüyor.** Çıktı: 10 satırın **10'unda** `difficulty **0/100**`. Vendor'a doğrudan aynı sorgu (`dataforseo_labs_google_ranked_keywords`, dentnotion.com, Turkiye/tr): `keyword_properties: { **detected_language: "tr"** }` — `keyword_difficulty` alanı **yok**. Aynı çıktıda renderer doğru davranışı da sergiliyor: `diş taşı temizliğinden sonra yemek yenir mi — volume 480, competition LOW` satırında **CPC hiç basılmamış** (vendor vermemiş, atlanmış). Yani atlama yeteneği VAR, difficulty'de kullanılmıyor. Kart 28 (`keyword_suggestions`) + bu kart (`ranked_keywords`) = iki bağımsız uç. | Raporlanmayan `keyword_difficulty` CPC gibi atlansın. İki uçta kanıtlandığı için tek noktada (ortak biçimleyici) düzeltilmeli. **[kod]** |
| `ranked_keywords` | — | **Turun en zengin ve en doğrulanabilir çıktısı.** Domain özeti (190 SERP, pozisyon histogramı, ETV 864, eşdeğer reklam maliyeti $347, `newly ranking 89 · moved up 57 · moved down 41 · no longer found 0`), satır başına organik pozisyon ile **sayfa üzerindeki mutlak pozisyon ayrı** (`position #2 organic (#4 on the page)`), hareket (`moved up from #16`), SERP özellikleri (`ai_overview, people_also_ask, video`), sıralayan URL + SERP başlığı, ve **her satırda `verify:` Google linki** (uule geo parametresiyle) — müşteri iddiayı kendi gözüyle doğrulayabiliyor. Yuvarlama doğru: vendor `etv 116.64` → çıktı `est. traffic **117/mo**`. Bayatlık notu koşullu ve doğru (`46 days ago … treat as indicative`). **Bakıldı, bulgu yok.** | — |
| `my_pages` | `ÇIKTI` | **Kart 29'daki 14-basamak bulgusu kesinleşti: aynı ürün aynı alanı doğru yuvarlayabiliyor.** `ranked_keywords` vendor'ın `etv 116.64` değerini `est. traffic 117/mo` diye basıyor; `my_pages` aynı cins alanı `etv **86.03599891066551**` diye basıyor. Fark tasarım değil, **biçimleyici eksikliği**. | Ortak sayı biçimleyici (kart 30'daki üç-standart bulgusuyla aynı chip). **[kod]** |
| `analyze_backlinks` | `VERİ` | **"Raporlanmayan asla sıfır basılmaz" ihlali ÜÇÜNCÜ uçta, birebir eşleşmeyle kanıtlandı.** Çıktı: `Top referring domains (10 of 134)` — 10 satırın **6'sında `rank 0`**. Vendor'a doğrudan aynı sorgu (`backlinks_referring_domains`, dentnotion.com): `izmirhabergazetesi.com` → `"rank": 43` (bizde 43 ✅) · `haberdar24.com` → `"rank": 33` (bizde 33 ✅) · `izmirdebugun.com`, `sondakikaizmir.com`, `poliste.com`, `habersafir.com` → **`rank` alanı gövdede YOK** (bizde hepsi **0** ❌). Alan varsa doğru, yoksa sıfır — tam eşleşme. Aynı ailenin kardeş tool'u `disavow_candidates` bu vaadi açıkça yazıyor: *"A domain DataForSEO did not score is shown as **unreported, never as a zero**."* Üç uç: Labs `keyword_suggestions` (k28) · Labs `ranked_keywords` (k32) · Backlinks `referring_domains` (k33). | Tek ortak biçimleyicide çözülsün; üç ailede de aynı kusur. Ürünün çekirdek farklılaştırıcısı olduğu için öncelik yüksek. **[kod]** |
| `analyze_backlinks` | — | **Kart 27'nin spam bulgusu profil görünümünde de yüzeye çıkıyor.** `Top anchors (10 of 37)` listesinin 7. sırası: `"High Quality Dofollow Backlinks DA 50 PA 40 Premium PBN Network Service dentnotion.com … Buy Backlinks Online Cheap" — **7 backlinks**`. Yani müşteri 70 kredilik profil özetinde de saldırıyı görebiliyor. Profil sayıları tutarlı ve `backlink_changes` (k25) ile birebir uyumlu: 239 backlink · 136 referring domain · domain rank 170 — k25'in son kovasıyla aynı. **Bakıldı, bulgu yok.** | — |
| `analyze_backlinks` | `KAPSAM` | Vendor her referring domain için `backlinks_spam_score` döndürüyor (ölçüldü: `izmirdebugun.com` 7 · `poliste.com` **26** · `sondakikaizmir.com` 3), ama `Top referring domains` listesi yalnız backlink sayısı + rank basıyor — **spam skoru satırda yok**. Aynı üründe `disavow_candidates` ve `backlink_details` bu alanı gösteriyor. Müşteri, profil özetinde hangi kaynağın şüpheli olduğunu göremiyor. | Referring domain satırına vendor'ın `backlinks_spam_score` alanı eklensin (zaten gövdede geliyor, ek maliyet yok). **[kod]** |
| `compare_competitors` | `VERİ` | **İki ücretli tool aynı domain için aynı alanda ÇELİŞİYOR — dakikalar arayla.** `ranked_keywords` (k32, dentnotion.com, TR/tr): `newly ranking: 89 · moved up: 57 · moved down: 41 · **no longer found: 0**`. `compare_competitors` (bu kart, aynı domain, aynı pazar): `newly ranking: 89 · moved up: 57 · moved down: 41 · **no longer found: 104**`. Üç sayı birebir aynı, dördüncüsü 0 ile 104. Vendor'a doğrudan soruldu (`dataforseo_labs_google_domain_rank_overview`, Turkiye/tr): `is_new: 84 · is_up: 57 · is_down: 38` ve **`is_lost` alanı gövdede HİÇ YOK**. Dolayısıyla `0` = raporlanmayanın sıfır basılması (k28/k32/k33'ten sonra **dördüncü** örnek); `104`'ün vendor özetinde karşılığı yok ve **kaynağı ölçülmedi**. | `is_lost` yoksa "unreported" yazılsın; `104`'ün nereden geldiği düzeltme oturumunda izlenmeli. **[kod]** |
| `compare_competitors` | `ÇIKTI` | **90 kredi — turun en pahalı tek çağrısı — ve tek bir karşılaştırma cümlesi yok.** Çıktı üç bloğu alt alta basıyor; fark, oran, kazanan/kaybeden yok. Oysa veri açık bir hikâye taşıyor: dentnotion **190 SERP / ETV 864 / $347** eşdeğer reklam; citydentistanbul **59 SERP / ETV 556 / $976**; drkundakcioglu **25 SERP / ETV 628 / $1.140**. Yani rakipler **çok daha az sıralamayla** ziyaret başına **3-4 kat değerli** trafik alıyor — tool'un adı "compare" ama karşılaştırmayı müşteri kendi yapıyor. | En az bir fark tablosu (hedef vs rakip, sütun sütun) ya da tek satırlık ETV/SERP oranı. 90 kredinin karşılığı NEVER#6 kapsamında. **[operatör]** + **[kod]** |
| `compare_competitors` | — | Rakip kaynağı açıkça etiketleniyor (`(target)` / `(supplied by you)`), şema otomatik keşfin sınırını dürüstçe yazıyor (*"on a small or niche site can return general-purpose giants (youtube.com, wikipedia.org) that share a SERP without being real rivals"*), ve `limit` argümanının **tabloyu büyütmediği** açıkça belirtiliyor. Pozisyon bantları on iki bandın hepsiyle veriliyor, ortalamaya çevrilmiyor. **Bakıldı, bulgu yok.** | — |
| `ai_visibility` | `VERİ` | **Tool tamamen çökük — iki platformda, iki özne biçiminde, 2/2.** `subject=domain, platform=chat_gpt, project_id` → `Tool "ai_visibility" failed unexpectedly … reference **e383191d** … You were not charged.` · `platform=google, target=dentnotion.com` → aynı arıza, ref **c6400f6b**. Vendor uçları kaynakta mevcut ve adlandırılmış (`dfs/llm-mentions.ts:125` `llm_mentions/aggregated_metrics/live`); arıza yakalanmış vendor hatası değil, **beklenmeyen istisna**. Defterin bu kart için notu ("LLM Mentions'tan hiç canlı yanıt yakalanmadı") artık daha keskin: canlı yanıt yok DEĞİL, tool hiç çalışmıyor. | Arıza `e383191d`/`c6400f6b` referanslarıyla sunucu logundan izlenip düzeltilsin. **[kod]** |
| `ai_visibility` | `DEĞER` | **"You were not charged" YARIM doğru: kredi alınmıyor, vendor parası yanıyor.** Kredi tarafı ölçüldü ve iddia TUTUYOR: net delta **0**, ledger'da **2 satır** (rezervasyon + iade). Ama `dfs_spend_today_usd()` izole ölçüldü: çağrı öncesi **$0,897896** → sonrası **$1,197896** = **+$0,30**, sıfır çıktı için. Bu, turun en pahalı tek çağrısı (ikincisi $0,0758). **Operasyonel risk:** günlük tavan $3,00 ve fail-closed; **on başarısız çağrı tavanı doldurup günün geri kalanında ÇALIŞAN bütün ücretli tool'ları bloke eder.** Müşteriye bu harcamadan hiç söz edilmiyor. | Vendor çağrısı başarısızsa harcama defterine yazılmaması ya da en azından müşteriye/operatöre bildirilmesi; kırık tool yüzeyden çekilene kadar bütçe koruması. **[kod]** + **[operatör]** |
| `ai_visibility_compare` | `VERİ` | **Aynı arıza kardeş tool'da da — aile tamamen çökük.** `platform=chat_gpt`, 2 hedef (`dentnotion.com`, `citydentistanbul.com`) → `Tool "ai_visibility_compare" failed unexpectedly … reference **a6f143eb** … You were not charged.` İki tool FARKLI vendor uçları kullanıyor (`aggregated_metrics` vs `cross_aggregated_metrics`, `llm-mentions.ts:125` ve `:130`), yani arıza tek uca özgü DEĞİL — ortak katmanda. Vendor maliyeti izole ölçüldü: **$1,197896 → $1,647896 = +$0,45** (2 hedef), yine sıfır çıktı. Üç çağrı toplamı **$1,05 = günlük tavanın %35'i**. | Tek kök neden, iki tool. Düzeltilene kadar bu ailenin yüzeyden çekilmesi değerlendirilsin (26 → 34 tool). **[operatör]** + **[kod]** |
| `ai_visibility_compare` | — | Şema tarafı sağlam ve ölçüldü: fiyatın **hedef başına** olduğu açıkça yazılı (`two targets cost 180 and ten cost 900`), 200 kredi üstü için onay eşiği tarif edilmiş, `platform` zorunlu ve varsayılansız (*"a measurement of one says nothing about the other"*), `internal_list_limit`in fiyatı DEĞİŞTİRMEDİĞİ dürüstçe yazılmış (*"Asking for fewer rows costs the same"*) — kart 26'daki `backlink_details`'in yanlış fiyat iddiasının tam tersi, doğru hâli. **Bakıldı, bulgu yok** (çalışma arızası yukarıda ayrı satırda). | — |

---

## KAPSAMA İZLEME — hangi tool bakıldı

> Bu tablo oturumun **kendi dürüstlüğü**: "36'sına da baktık" ölçülmeden yazılmaz.

### Ücretsiz — 11 tool (para harcamaz, önce bunlar)

| tool | bakıldı | not |
|---|---|---|
| `setup_project` | ☑ | |
| `list_projects` | ☑ | |
| `get_credit_balance` | ☑ | |
| `get_job_status` | ☑ | |
| `whats_next` | ☑ | yönlendirme kalitesi — diğer 35'i tanıtan tool |
| `connect_gsc` | ☑ | dış dünya, dikkat |
| `list_gsc_properties` | ☑ | |
| `track_gsc_property` | ☑ | |
| `untrack_project` | ☑ | |
| `track_keywords` | ☑ | |
| `keyword_positions` | ☑ | 10 kredi ama vendor maliyeti SIFIR |

### Ucuz — 6 tool, 5–15 kredi

| tool | kredi | bakıldı | not |
|---|---|---|---|
| `audit_schema` | 5 | ☑ | |
| `pull_gsc_data` | 5 | ☑ | |
| `serp_snapshot` | 5+8/kw | ☑ | **fixture'ı zaten saklanabilir satır üretemiyordu** |
| `find_quick_wins` | 10 | ☑ | |
| `detect_cannibalization` | 10 | ☑ | |
| `analyze_content_decay` | 10 | ☑ | |

### Orta — 7 tool, 12–35 kredi

| tool | kredi | bakıldı | not |
|---|---|---|---|
| `audit_content` | 12 | ☑ | |
| `audit_tech` | 15 | ☑ | |
| `audit_speed` | 15 | ☑ | |
| `generate_report` | 15 | ☑ | |
| `crawl_site` | 20 | ☑ | asenkron — `get_job_status` ile birlikte |
| `research_keywords` | 25 | ☑ | |
| `audit_onpage` | 30 | ☑ | |

### Pahalı — 12 tool, 35–90 kredi · **burada dikkat**

| tool | kredi | bakıldı | not |
|---|---|---|---|
| `backlink_changes` | 35 | ☑ | fixture GERÇEK yakalama |
| `backlink_details` | 35 | ☑ | fixture GERÇEK yakalama |
| `disavow_candidates` | 40 | ☑ | fixture karışık |
| `discover_keywords` | 40 | ☑ | **dört mode = dört ayrı uç, dördü de ayrı incelenir** |
| `my_pages` | 40 | ☑ | |
| `keyword_gap` | 45 | ☑ | |
| `link_gap` | 45 | ☑ | |
| `ranked_keywords` | 65 | ☑ | `n/a`'yı en çok basan renderer |
| `analyze_backlinks` | 70 | ☑ | üç blok, üçü de ayrı fixture |
| `compare_competitors` | 90 | ☑ | |
| `ai_visibility` | 90 | ☑ | **LLM Mentions'tan hiç canlı yanıt yakalanmadı** |
| `ai_visibility_compare` | 90/hedef | ☑ | 3 hedef = 270 → **200 onay eşiğini aşmalı** |

---

## OTURUM SONUNDA — üç satır, hepsi ölçülmüş

1. **Kaç tool'a bakıldı / 36.** Bakılmayan varsa **adıyla** yazılır.
2. **Kaç bulgu, sınıfa göre dağılımı.**
3. **Harcanan vendor doları** (`select dfs_spend_today_usd()`), ve tavana ne kadar kaldığı.

Sonra bulgular **sınıfa ve sahibine** göre gruplanır: `[kod]` olanlar chip envanterine, `[operatör]`
olanlar imza kuyruğuna, `[açık]` olanlar bir sonraki oturumun karar listesine.

---

## OTURUM SONU ÖLÇÜMLERİ — 2026-08-25 (1. tur)

### 1. Kaç tool / 36

**36 / 36 — bakılmayan tool YOK.** Kapsama tablosunda 36 kutunun 36'sı işaretli.
`discover_keywords`'ün dört modu ayrı ayrı koşuldu (`for_site`, `suggestions`, `related`, `ideas`).

### 2. Kaç bulgu, sınıfa göre

**120 satır** (defterden sayılarak):

| sınıf | adet |
|---|---|
| `ÇIKTI` | 28 |
| `VERİ` | 27 |
| `KAPSAM` | 23 |
| `DEĞER` | 8 |
| `ARGÜMAN` | 5 |
| `SEÇİM` | **0** |
| — ("bakıldı, bulgu yok") | 29 |

**Gerçek bulgu: 91** · **"bulgu yok" kaydı: 29**.

Sahibine göre (bir satır birden fazla sahip taşıyabilir):
**`[kod]` 73** → chip envanteri · **`[açık]` 17** → sonraki oturumun karar listesi · **`[operatör]` 13** → imza kuyruğu.

**`SEÇİM` sınıfı SIFIR:** 36 tool'un 36'sı düz müşteri cümlesinden doğru seçildi. Tool açıklamaları
seçim ekseninde kusursuz çalışıyor.

### 3. Harcanan vendor doları

| ölçüm | değer |
|---|---|
| tur başlangıcı (`dfs_spend_today_usd()`, ben hiçbir şey çağırmadan) | **$0,012480** |
| tur sonu | **$1,647896** |
| **turda harcanan** | **$1,635416** |
| günlük tavan | $3,00 (fail-closed) |
| **kalan** | **$1,352104** |

Kredi: **5630 → ~4690** (yaklaşık **−940 kredi**).

**Harcamanın dağılımı — tek kalem toplamın %64'ü:** çöken AI ailesi (`ai_visibility` ×2 +
`ai_visibility_compare` ×1) **$1,05** harcadı ve **sıfır çıktı** üretti; üçü de müşteriye
"You were not charged" dedi (kredi için doğru, vendor parası için sessiz).

### Tekrar eden üç desen (her biri ≥3 kartta bağımsız ölçüldü)

1. **Raporlanmayan alan `0` basılıyor** — vaat: *"unreported, never as a zero"*. Üç vendor ailesinde
   dört kez kanıtlandı, her seferinde vendor gövdesi yan yana konarak:
   `keyword_suggestions` (k28) · `ranked_keywords` (k32) · `backlinks_referring_domains` (k33) ·
   `domain_rank_overview`/`is_lost` (k34). **Ürünün çekirdek farklılaştırıcısı.**
2. **Metin bir şey vaat ediyor, kod başkasını yapıyor** — dördü de ölçüldü:
   `max_crawl_pages` "unmeasured" varsayımı (k14, 3/3 timeout) · `overallSavingsMs: 0` varsayımı
   (k20, geçen audit "fırsat" basıldı) · `limit = fiyat kontrolü` (k26, 19× satır → %13 maliyet) ·
   `unreported never zero` (yukarıdaki madde).
3. **Analiz var, tavsiye yok** — altı ücretli tool'da aynı boşluk: `find_quick_wins`,
   `detect_cannibalization`, `analyze_content_decay`, `audit_content`, `backlink_changes`,
   `compare_competitors` (90 kredi, tek karşılaştırma cümlesi yok).

### Bu turun yarattığı durum (temizlik kararı bekliyor)

- `www.seogrep.com` (971463dd-…) ve `bu-domain-kesinlikle-yok-9f3a2c.com` (4f3eb00a-…, **arşivde**) —
  kart 1 probları.
- `noraninsaat.com` (ea77221c-…) — kart 8'de `www.` kusuru ölçülürken doğdu; `www.noraninsaat.com`
  ile aynı sitenin ikinci kimliği.
- `dentnotion.com` üzerinde 2 tracked keyword (ABD·en·desktop) + 3 `not_measured` SERP satırı +
  taze 100 sayfalık crawl + 30 günlük GSC pull + 1 public rapor (https://seogrep.com/r/NfW7wtr8niq).
