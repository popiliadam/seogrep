# FAZ 4 (LAUNCH) — Uygulama Planı

> Yazıldı: 2026-07-27, GO insan kararı sonrası (ledger "FAZ 4 GO KARARI" kaydı).
> Kaynaklar: spec §9 "FAZ 4 — Launch" · kickoff `docs/plans/2026-07-21-faz4-kickoff.md` ·
> ledger "FAZ 4 ADAY BACKLOG" (1-12) · audit#1 G-tablosu · 2 keşif raporu (bu oturum, repo @6fc391f).
> Yürütme: her görev = iş emri JSON → işçi subagent → taze hakem → `guardrails/verify.sh`.
> **For agentic workers:** işçi YALNIZ kendi iş emrini görür; bu planın tamamı şefin yol haritasıdır.

**Hedef:** Ödeme alan (Paddle LIVE), dizinlerde listelenen, izlenen (uptime+reaper) ürün + launch içerikleri + blog dogfooding. Çıkış: `goals/purchase-flow-live` + `goals/uptime` yeşil (spec §9).

**Mimari yaklaşım:** Tek dal `feat/faz4-launch`; küçük, bağımsız-hakemli görevler; para/webhook/SSRF diff'lerinde taze-Fable hakem; dış-dünya adımları (Paddle onboarding, yayın, dizin gönderimi) insan kapısında.

## Global kısıtlar (her iş emrine örtük dahil)

- NEVER seti aynen (CLAUDE.md): ledger append-only · webhook imza+idempotency · tenant filtresi/RLS · fiyat-kredi rakamı İNSANSIZ DEĞİŞMEZ (NEVER#6) · test zayıflatma yasak · uydurma metrik yok (NEVER#7) · commit ≤200 satır (NEVER#10).
- **UI/docs copy İNGİLİZCE** (imzalı ders #4 — her iş emrine açıkça yazılır).
- Kapı: `bash guardrails/verify.sh` + (DB dokunan işte) `verify-db.sh` + `make goals`.
- İşçi default Opus 4.8; mekanik/copy Sonnet 5. Hakem taze Opus 4.8; para/webhook/auth/RLS/SSRF diff'i → taze Fable. Task toplam diff >400 satır → hakem her durumda Fable.
- Kredi/fiyat SAYILARI hiçbir görevde değişmez (T-A1 dahil — yalnız environment/env-adı işleri).

---

## Triyaj kararı (şef, insan delegasyonuyla — "senin önerilerine göre")

**LAUNCH MVP'ye GİREN** (gerekçe: launch'ı doğrudan destekler + küçük/orta çaba):

| # | İş | Kaynak | Gerekçe |
|---|---|---|---|
| A1-A2 | Paddle LIVE geçiş (kod: server-env switch; ops: checklist) | spec §9-A | Keşif bulgusu: server SDK environment'sız → sandbox key'le LIVE API'ye gider; geçişin ön koşulu |
| D1 | Zamanlı reaper + /status genişletme + `goals/uptime` | spec §9-D + T4/T5 Faz-4 defer notları | "İlk ücretli kullanıcı" şartının otomasyonu; monitoring.md §5 açık borç |
| L1 | `SUPABASE_DB_URL` URL-yapı doğrulaması | imzalı-bekleyen ders L1 | Gerçek incident (async sessiz düştü, /status yeşil); 1 satır + negatif testler |
| S1 | A-C1 DNS-rebinding (undici IP-pin) | Codex koşul-2 kalıntısı, ssrf.ts belgeli | Public launch = düşman trafik; tek kalan güvenlik-Important |
| U1 | GSC durum banner'ı + Connect yüzeyi | backlog #1 + keşif "7 üretici / 0 tüketici" | OAuth dönüşü kullanıcıya TAM sessiz — beta kullanıcısı için UX-bug düzeyi |
| U2 | Crawl robots-5xx dürüst mesaj + tek retry | backlog #3 (insan: "bulamadık diyemeyiz") | Küçük; mesaj bugün "robots.txt unreachable" — nedeni ayırt etmiyor |
| B1 | Blog altyapısı + dinamik sitemap | spec §9-E + keşif sitemap bulgusu | Sitemap'te docs YOK — SEO ürünü için canlı eksik; blog greenfield |
| B2 | İlk 2 blog yazısı (dogfooding, gerçek bulgular) | spec §9-E | NEVER#7 uyumlu gerçek hikâye: kendi audit'imiz |
| C1 | Launch içerik taslakları (PH/HN/X) | spec §9-B | Şef yazar; yayın insan |
| C2 | MCP dizin başvuru metinleri (Smithery/PulseMCP/mcp.so/Anthropic) | spec §9-C | Şef yazar; gönderim insan |

**ERTELENEN** (gerekçeli — launch MVP şişirilmez; launch-sonrası ilk dilimde yeniden triyaj):
- **#10 DFS-derinlik araç ailesi ⭐** (analyze_backlinks/compare_competitors/ranked_keywords): en yüksek gelir potansiyeli AMA yeni paralı tool = fiyat kararı (NEVER#6) + DFS_LIVE açılışı gerektirir → launch-sonrası İLK ürün dilimi önerisi.
- **#7 audit_speed (+#11 CrUX)**: yeni tool ailesi; BYO-key deseniyle birlikte launch-sonrası.
- **#2 Scrapling fetcher, #5 büyük-site kademeleri (fiyat kapısı), #6 import_crawl**: ölçek/altyapı işleri; launch bunlarsız olur.
- **#4 Tools vitrin sayfası**: docs/tools-reference şimdilik yeterli; launch-sonrası pazarlama dilimi.
- **G8 rapor silme/public-slug iptali**: privacy copy'deki "email us" yolu beta-dürüst; launch-sonrası ilk dilimde implement (GDPR otomasyonu).
- **#12 CommonCrawl**: Faz 5+ deney (karar zaten verili).
- **Rollover/erasure implementasyonu**: A2'deki İNSAN fiyat oturumunun çıktısına bağlı — karar "implement" çıkarsa ayrı görev açılır.

---

## Görevler (sıra + bağımlılık)

```
T-A1 (Paddle env switch)  ──►  A2 insan Paddle-live hazırlığını AÇAR
T-L1 (env doğrulama)      ──►  bağımsız, erken
T-D1 (reaper+uptime)      ──►  bağımsız
T-S1 (DNS-rebinding)      ──►  bağımsız (crawler'a dokunur; T-U2'den ÖNCE bitmeli)
T-U2 (robots-5xx UX)      ──►  T-S1 SONRASI (aynı dosya: crawl.ts — çakışma önleme)
T-U1 (GSC UX)             ──►  bağımsız
T-B1 (blog altyapı)       ──►  T-B2 (yazılar) bunun üstüne
T-C1/C2 (içerik)          ──►  şef, koda bağımsız; B2 ile tutarlı ses
İNSAN: Paddle live onboarding+nihai fiyat oturumu → canlı smoke → yayın/dizin gönderimleri
```

---

### T-A1 — Paddle server-SDK environment switch (para-kritik, küçük)

**İş emri:**
```json
{
  "task": "Server-side Paddle SDK'ya sandbox/production environment'ı geçir. Bugün apps/web/app/api/paddle/webhook/route.ts:71 ve apps/web/app/app/billing/actions.ts:44 'new Paddle(apiKey)' — environment YOK → SDK production API tabanına düşer. NEXT_PUBLIC_PADDLE_ENV zaten var (checkout-button.tsx:16-19 deseni: yalnız 'sandbox'|'production' kabul, aksi undefined). Paylaşılan bir yardımcı yaz (apps/web/lib/paddle-env.ts): resolvePaddleEnvironment(): Environment | undefined — process.env.NEXT_PUBLIC_PADDLE_ENV okur, 'sandbox'|'production' dışında undefined döner. İki construction site'ı 'new Paddle(apiKey, { environment })' yap; environment undefined ise SEÇENEĞİ HİÇ GEÇME (SDK default'u = bugünkü davranış, geriye-uyumlu). checkout-button.tsx'teki mevcut RAW_ENV çözümlemesini de bu yardımcıya taşı (davranış bayt-özdeş kalmalı; 'configured' fail-closed mantığına dokunma). TDD: önce testler. UI copy İngilizce (değişen copy yok — kod-only).",
  "done_when": [
    "apps/web/lib/paddle-env.ts yeni; resolvePaddleEnvironment 'sandbox'→sandbox, 'production'→production, 'PRODUCTION'/''/'staging'/unset→undefined testleri PASS",
    "webhook/route.test.ts YENİ test: NEXT_PUBLIC_PADDLE_ENV=sandbox iken Paddle constructor'ının {environment:'sandbox'} ile çağrıldığı mock ile kanıtlı; unset iken environment option'sız çağrıldığı kanıtlı",
    "billing/actions.ts için aynı iki negatif/pozitif kanıt",
    "checkout-button davranışı değişmedi: mevcut site-header/checkout testleri bayt-değişmeden PASS",
    "bash guardrails/verify.sh PASS",
    "hiçbir fiyat/kredi sayısı ve price-id env adı değişmedi (git diff'te pricing-plans.ts/packages.ts YOK)"
  ],
  "files_in_scope": ["apps/web/lib/paddle-env.ts", "apps/web/app/api/paddle/webhook/route.ts", "apps/web/app/app/billing/actions.ts", "apps/web/app/app/billing/checkout-button.tsx", "apps/web/app/api/paddle/webhook/route.test.ts", "apps/web/app/app/billing/*.test.*", ".env.example"],
  "forbidden": ["pricing-plans.ts", "packages/core/src/billing/*", "webhook imza/idempotency akışına davranış değişikliği", "yeni env adı icadı (NEXT_PUBLIC_PADDLE_ENV kullan)", "kredi/fiyat sayısı", "Türkçe UI copy"]
}
```
**İşçi:** Opus 4.8 · **Hakem: TAZE FABLE** (webhook/para dosyası). Tahmini diff ~120 satır.

### T-A2 — Paddle LIVE geçiş kontrol listesi (İNSAN+şef, kod yok)

Şef `docs/runbooks/paddle-live-cutover.md` yazar (işçi gerekmez; şef+hakem-siz, insan okur):
1. **İnsan:** Paddle live onboarding/doğrulama tamam mı (Faz-2'den beri bekleyen dış süreç).
2. **İnsan (Paddle dashboard, LIVE):** 4 ürün / 6 price yeniden yarat → 6 `pri_…` id · live API key (min-yetki) · live client token · Notifications webhook → `https://seogrep.com/api/paddle/webhook` (transaction.completed + subscription.created/updated/canceled) → live webhook secret.
3. **İnsan (Netlify env — DEĞERLERİ CHAT'E YAPIŞTIRMADAN):** 10 değişkenin LIVE değerleri; `NEXT_PUBLIC_PADDLE_ENV=production`. NOT (keşif kanıtı): `NEXT_PUBLIC_PADDLE_PRICE_*` webhook'ta SERVER-SIDE okunur (route.ts:33-52) — deploy env'inde eksik = her satın alma record_only/500-retry döngüsü.
4. **İnsan+şef NİHAİ FİYAT OTURUMU (NEVER#6 kapısı):** $19/$49/$149 + top-up'lar AYNEN mi? Rollover/2×cap (E-I1) kararı burada. Değişiklik çıkarsa kod+docs+pricing üçlü değişikliği İMZALI ayrı görev.
5. **Canlı smoke (Codex koşul-12; şef yönetir, insan öder):** gerçek kartla en küçük top-up ($10) → ledger `purchase +400 ref=txn_…` TEK satır → Paddle Replay → ikinci satır YOK (idempotency LIVE kanıtı, NEVER#3) → `/app` bakiye doğru → iade insan kararıyla (Paddle refund; ledger'a adjust İNSAN onayıyla).
6. Şef `goals/evidence/purchase-flow-live.txt` yazar (txn ref + tarih; secret yok) → `goals/purchase-flow-live.md` yeşile döner.

### T-D1 — Zamanlı reaper + /status genişletme + uptime hedefi

**İş emri:**
```json
{
  "task": "Worker'a periyodik reaper zamanla + /status'a görünürlük ekle + goals/uptime.md yaz. (1) apps/mcp/src/queue/worker.ts startWorker() içine güvenli interval: her 10 dk (REAPER_INTERVAL_MS sabiti, injectable) reconcileStuckJobs() (apps/mcp/src/queue/reaper.ts:125, argümansız defaults yeter) try/catch ile; sonuç metrics'e. setInterval unref()'li; stopWorker() interval'i clearInterval yapar. pg-boss schedule KULLANMA (kuyruk mesaj şeması JobMessage'a pinli — ayrı kuyruk açmak YAGNI). (2) apps/mcp/src/metrics.ts Metrics interface'ine ekle: recordReaperRun(outcome:{released:number}): void; snapshot'a reaperRuns, reservesReleased, lastReaperRunAt (ISO|null). /status (server.ts:296-299) spread'i bunları otomatik taşır — route değişikliği gerekmiyorsa dokunma. (3) goals/uptime.md YENİ (verify-goals.sh predicate formatı — landing-live SKIP-deseni): ilk satır '[ ! -f goals/evidence/uptime-monitor.txt ] && exit 0' (insan monitor kurulumu kanıt dosyası yokken SKIP — mcp-alive healthz'i zaten kovalıyor); kanıt varsa: curl -sf --max-time 15 https://mcp.seogrep.com/healthz | grep -q '\"ok\":true' VE curl -sf --max-time 15 https://mcp.seogrep.com/status | grep -q '\"ok\":true'. on-violation bölümü: flyctl logs + scripts/monitoring.md + scripts/reconciliation.md işaretçileri. NOT: SKIP=faz-çıkışı DEĞİL — çıkış kriteri evidence dosyasının VARLIĞINI ayrıca şart koşar (L2 dersi sınıfı maske önlenir). (4) scripts/monitoring.md §5'ten 'automatic periodic reaping = Faz 4' satırını gerçeğe güncelle (artık zamanlı; interval + metrik adları). TDD; worker testleri fake timer + fake reconcile ile: interval tetikliyor, hata swallow+metrics, SIGTERM'de temiz durma. UI copy İngilizce (runbook/goal dosyaları İngilizce-Türkçe mevcut karışım desenine uy: goal dosyası Türkçe olabilir, kod+log mesajları İngilizce).",
  "done_when": [
    "worker testinde fake-timer ile 2 tick → reconcileStuckJobs 2 kez çağrıldı kanıtı; reject eden reconcile worker'ı ÖLDÜRMÜYOR (sonraki tick yine koşuyor)",
    "stopWorker sonrası tick YOK (clearInterval kanıtı)",
    "metrics.test.ts: recordReaperRun snapshot'ta reaperRuns/reservesReleased/lastReaperRunAt doğru; Object.freeze korunuyor",
    "/status testinde yeni alanlar görünüyor; /healthz BYTE-DEĞİŞMEDİ (sıfır I/O kuralı monitoring.md:3)",
    "goals/uptime.md verify-goals.sh formatında; evidence dosyası YOKKEN SKIP (make goals yeşil kalır); PLACEHOLDER evidence dosyası COMMIT EDİLMEZ (insan kurulumu sonrası şef yazar)",
    "bash guardrails/verify.sh PASS; reaper.ts ve release_reserve RPC'ye DOKUNULMADI (git diff'te yok)"
  ],
  "files_in_scope": ["apps/mcp/src/queue/worker.ts", "apps/mcp/src/metrics.ts", "apps/mcp/src/server.ts (yalnız gerekirse)", "apps/mcp/src/queue/worker.test.ts", "apps/mcp/src/metrics.test.ts", "goals/uptime.md", "scripts/monitoring.md"],
  "forbidden": ["reaper.ts (davranış değişikliği)", "release_reserve/ledger RPC'leri", "/healthz'a I/O", "fly.toml health-check bloğu", "pg-boss schedule/yeni kuyruk", "Türkçe kod/log mesajı"]
}
```
**İşçi:** Opus 4.8 · **Hakem: TAZE FABLE** (reaper=ledger-release tetikleyicisi, money-adjacent).

### T-L1 — SUPABASE_DB_URL yapı doğrulaması (ders L1 kapanışı)

**İş emri:**
```json
{
  "task": "apps/mcp/src/env.ts:19 'SUPABASE_DB_URL: z.string().min(1)' → URL-YAPI doğrulamasına yükselt: superRefine ile (a) new URL() parse OLUYOR, (b) protokol postgres:|postgresql:, (c) hostname boş değil, (d) '[YOUR-PASSWORD]' / '[' KARAKTERİ İÇERMİYOR (gerçek incident: köşeli parantez placeholder'ı) — aksi halde loadEnv Error mesajı hangi kuralın kırıldığını İngilizce söyler. Davranış: GEÇERLİ mevcut prod formatı (postgresql://user:pass@host:5432/db) aynen geçer. verify-db.sh ve lokal env'ler kırılmamalı. TDD: env.test'e PROD-GERÇEK adlarla negatif matris (ders#5): bozuk-URL, http://, köşeli-parantezli, boş-host, port'suz-geçerli (port opsiyonel — Supabase session pooler 5432 default değil zorunlu DEĞİL). UI copy İngilizce.",
  "done_when": [
    "env.test: 5+ negatif form THROW + hata mesajı SUPABASE_DB_URL'i ve nedeni içeriyor; 2 pozitif form (5432'li, portsuz) PASS",
    "loadEnv hata birleştirme davranışı korunuyor (diğer eksik env'lerle tek Error)",
    "bash guardrails/verify.sh PASS + verify-db.sh PASS (lokal stack URL'i hâlâ geçiyor)"
  ],
  "files_in_scope": ["apps/mcp/src/env.ts", "apps/mcp/src/env.test.ts"],
  "forbidden": ["boss.ts", "başka env kuralını değiştirmek", "yeni env adı", "Türkçe hata mesajı"]
}
```
**İşçi:** Opus 4.8 · **Hakem:** taze Opus 4.8. Küçük diff (<80 satır). NOT: ders L1'in CLAUDE.md imzası İNSANDA — bu görev kodu kapatır, imza ayrı.

### T-S1 — A-C1 DNS-rebinding kapanışı (undici IP-pinning)

**İş emri:**
```json
{
  "task": "ssrf.ts:21-28'de belgeli DNS-rebinding residual'ını kapat: checkPublicHost'un doğruladığı IP'yi fetch soketine PINLE. Tasarım: undici'yi @pseo/mcp runtime dependency yap (lisans MIT — hakem teyit eder); apps/mcp/src/crawler/pinned-fetch.ts YENİ modül: makePinnedDispatcher(host, ip) — undici Agent connect hook'uyla soketi DOĞRULANMIŞ IP'ye bağlar, Host header + SNI (servername) ORİJİNAL hostname kalır; fetchPage (crawl.ts:355) ve fetchText (crawl.ts:449) fetch çağrılarına dispatcher geçir. Akış: her hop'ta hostname'i checkPublicHost(lookup) ile çöz+doğrula → dönen IP'yi pinle → fetch. Redirect hop'ları: yeni hostname = yeni doğrula+pinle (fetchText'teki validateRedirectTarget:411-416 zaten pre-emission doğruluyor — artık pin de ekle; fetchPage same-origin hop'larında da rebinding penceresi kapanır: aynı origin BİLE olsa her emisyon pinli). ssrf.ts docstring'i gerçeğe güncelle (residual KAPANDI; hangi modülün pinlediği). Fail-closed korunur: lookup başarısız/blocked-IP → istek EMİT EDİLMEZ. Node fetch'in dispatcher option'ı undici Agent kabul eder (RequestInit.dispatcher — @types/node'da olmayabilir: tip genişletmesini pinned-fetch.ts içinde localize et, 'as' kaçamağını tek noktada belgeli yap). Mevcut 45+ crawler testi DAVRANIŞ-DEĞİŞMEDEN geçmeli (lookup injection deseni korunur); YENİ rebinding testi: ilk lookup public-IP döner, fetch'in bağlandığı adres SPYLANIR → pinlenen adres = doğrulanan adres (ikinci farklı lookup sonucu KULLANILMIYOR kanıtı). UI copy İngilizce.",
  "done_when": [
    "yeni rebinding testi: doğrulama-lookup'ı ile fetch-bağlantı adresi AYNI; düşman ikinci-çözüm senaryosunda istek pinli adrese gidiyor (test connect-hook'u gözlemliyor)",
    "blocked-IP çözümünde 0 emisyon (mevcut zero-emission testleri bayt-korunur + pinli yolda da geçerli)",
    "TLS senaryosu: servername=hostname kanıtı (test veya kod-assert)",
    "apps/mcp/package.json'a undici eklendi; hakem lisans (MIT) teyidi raporda",
    "ssrf.ts docstring güncel (rebinding residual kalktı)",
    "bash guardrails/verify.sh PASS; tüm mevcut crawler testleri değişmeden PASS"
  ],
  "files_in_scope": ["apps/mcp/src/crawler/pinned-fetch.ts", "apps/mcp/src/crawler/crawl.ts", "apps/mcp/src/crawler/ssrf.ts (yalnız docstring + gerekirse export)", "apps/mcp/src/crawler/*.test.ts", "apps/mcp/package.json", "pnpm-lock.yaml"],
  "forbidden": ["SSRF blocklist aralıklarını değiştirmek", "robots/politeness davranışı", "kredi/costs", "test zayıflatma (NEVER#8)", "fetch'i axios/başka kütüphaneye taşımak"]
}
```
**İşçi:** Opus 4.8 · **Hakem: TAZE FABLE** (SSRF güvenlik + yeni runtime dep). En riskli görev — 2× FAIL'de dur, insana rapor (contract).

### T-U2 — Crawl robots-5xx dürüst UX (T-S1 SONRASI)

**İş emri:**
```json
{
  "task": "Robots erişilemezliğinde neden-ayrıştırılmış dürüst mesaj + tek otomatik retry. Bugün: loadRobots (crawl.ts:491-495) 5xx ve network-fail'i tek 'unreachable''a indirger; skip reason 'robots.txt unreachable' (crawl.ts:617); tenant'a giden final metin get-job-status.ts:61 üzerinden 'crawl_site: no pages could be crawled for <origin> (robots.txt unreachable)'. İstenen: (1) loadRobots dönüşünü zenginleştir: {kind:'unreachable', cause:'server_error'|'network'} (5xx vs null ayrımı); (2) unreachable'da TEK yeniden deneme (5s bekleme, injectable delay — politeness korunur, davranış hâlâ TEMKİNLİ: robots yoksa TARAMA YOK); (3) skip reason'ları İngilizce, aksiyon-yönlendirici: server_error → 'robots.txt returned a server error (5xx) on your site; we did not crawl to stay polite. You were not charged for failed crawls — fix robots.txt and run crawl_site again.', network → 'we could not reach robots.txt (network error or timeout); we did not crawl. You were not charged for failed crawls — check that the site is reachable and run crawl_site again.'; (4) handlers/crawl.ts:124-127 hata cümlesi skip reason'ları zaten join ediyor — 'no pages could be crawled' başlığı kalabilir, reason'lar yeni metinleri taşır. ÜCRET GERÇEĞİ: başarısız crawl'da reserve release edilir (mevcut davranış) — mesajdaki 'You were not charged' iddiası test ile PİNLENİR (ledger release yolunun bu path'te koştuğu mevcut testlerle çelişmemeli; iddia yanlışsa MESAJI DEĞİŞTİR, davranışı değil). TDD. UI copy İngilizce.",
  "done_when": [
    "loadRobots testi: 503 → cause=server_error; fetch-null → cause=network; 404 → allow-all DEĞİŞMEDİ",
    "retry testi: ilk çağrı 503 + ikinci 200 → crawl DEVAM EDİYOR (fake delay); iki kez 503 → dürüst server_error mesajıyla skip",
    "mesaj metinleri testle pinli; 'not charged' iddiası charge-yolu gerçeğiyle tutarlı (hakem doğrular)",
    "bash guardrails/verify.sh PASS; SSRF guard yollarına diff YOK"
  ],
  "files_in_scope": ["apps/mcp/src/crawler/crawl.ts", "apps/mcp/src/handlers/crawl.ts", "apps/mcp/src/crawler/crawl.test.ts", "apps/mcp/src/handlers/*.test.ts"],
  "forbidden": ["ssrf.ts / pinned-fetch.ts", "robots'suz tarama (temkinli duruş değişmez)", "kredi/charge davranışı", "arka-plan/zamanlı otomatik yeniden-crawl (YAGNI — mesaj 'run crawl_site again' der)", "Türkçe copy"]
}
```
**İşçi:** Opus 4.8 · **Hakem:** taze Opus 4.8 (SSRF dosyasına dokunursa Fable'a eskale).

### T-U1 — GSC banner + Connect yüzeyi

**İş emri:**
```json
{
  "task": "GSC OAuth dönüş durumunu görünür yap + Connect GSC yüzeyi ekle. Keşif gerçeği: 7 durum üreticisi var (?gsc=error|unknown_project|denied|no_token|connected&property=matched|none — apps/web/app/api/gsc/{connect,callback}/route.ts), SIFIR tüketici; /app/page.tsx searchParams bile almıyor. İş: (1) apps/web/components/gsc-banner.tsx YENİ client-safe RSC-uyumlu banner: gsc paramının 5 durumu + connected'ın property=matched|none alt-durumu için İngilizce, kısa, aksiyon-yönlendirici metinler (error→'Something went wrong connecting Search Console. Try again.', denied→'You declined Google access. You can reconnect anytime.', unknown_project→'That project was not found in your account.', no_token→'Google did not return a token. Try connecting again.', connected+matched→'Search Console connected.', connected+none→'Search Console connected, but no verified property matches this domain yet. Verify the domain in Search Console, then pull data.'); (2) /app/page.tsx imzasına searchParams ekle (Next 16 async searchParams deseni), banner'ı StatCard bloğunun ÜSTÜNE mount et; (3) apps/web/app/app/connection/page.tsx'e 'Google Search Console' bölümü (KeyPanel'den sonra, line~54): kullanıcının projelerini (RLS-scoped supabase client, projects tablosu) gsc_connections LEFT-durumuyla listele — her satır: domain + Connected/Not connected + Connect linki (href=/api/gsc/connect?project_id=<uuid>) — YENİ API YOK, mevcut rota; proje yoksa 'No projects yet. Create one from your MCP client with setup_project.' boş-durumu. TDD (RSC test desenleri mevcut page.test.tsx'lerdeki gibi). UI copy İngilizce.",
  "done_when": [
    "gsc-banner.test: 7 durum × doğru metin + bilinmeyen param → render YOK",
    "/app testinde ?gsc=connected&property=matched banner'ı görünüyor; parametresizde banner YOK",
    "connection sayfası testi: 2 proje fixture (biri bağlı) → doğru status + connect href'leri; 0 proje → boş-durum metni",
    "tenant guard: sorgular user.id-scoped (hakem RLS-yolu kontrolü)",
    "bash guardrails/verify.sh PASS"
  ],
  "files_in_scope": ["apps/web/components/gsc-banner.tsx", "apps/web/app/app/page.tsx", "apps/web/app/app/connection/page.tsx", "ilgili *.test.tsx"],
  "forbidden": ["api/gsc rotalarında davranış değişikliği", "yeni API rotası", "projects tablosuna yazma", "Türkçe copy", "tenant-filtresiz sorgu (NEVER#4)"]
}
```
**İşçi:** Opus 4.8 · **Hakem:** taze Opus 4.8 (auth-core'a dokunmuyor; dokunursa Fable).

### T-B1 — Blog altyapısı + dinamik sitemap

**İş emri:**
```json
{
  "task": "Fumadocs ile /blog + sitemap'i dinamikleştir. (1) apps/web/source.config.ts'e ikinci koleksiyon: blog = defineDocs({dir:'content/blog'}) (fumadocs-mdx defineDocs deseni; frontmatter: title, description, date). (2) apps/web/lib/source.ts'e blogSource loader (baseUrl:'/blog'). (3) app/(marketing)/blog/page.tsx liste (tarih-tersi sıralı, title+description+date) + app/(marketing)/blog/[slug]/page.tsx yazı sayfası (generateStaticParams + generateMetadata: title/description/canonical otomatik — mevcut root alternates:'./' deseni canonical'ı zaten üretir). Marketing layout'ta olduğu için header/footer + JSON-LD Organization sitewide gelir; yazı sayfasına ek Article JSON-LD EKLEME (YAGNI, launch-sonrası). (4) site-header.tsx NAV'a {href:'/blog',label:'Blog'} + site-footer.tsx'e Blog + lib/layout.shared.tsx docs-nav'ına Blog. (5) app/sitemap.ts'i DİNAMİKLEŞTİR: statik ROUTES + source.getPages() (docs) + blogSource.getPages() → tüm URL'ler (keşif: bugün docs sayfaları sitemap'te YOK). (6) app/robots.ts'e disallow:'/app'. (7) content/blog/hello placeholder YOK — koleksiyon boşsa liste sayfası 'No posts yet.' der (T-B2 gerçek yazıları getirir; build boş koleksiyonla kırılmamalı). TDD: sitemap.test.ts (docs+blog URL'leri içeriyor; mutlak URL; duplicate yok), robots.test.ts, blog sayfa testleri. UI copy İngilizce.",
  "done_when": [
    "make verify PASS (build boş blog koleksiyonuyla yeşil)",
    "sitemap.test: /docs/getting-started/* sınıfından en az bir gerçek docs URL'i + statik 6 route + (fixture yazı eklenince) blog URL'i",
    "robots.test: /app disallow + sitemap referansı",
    "nav 3 yerde Blog linki (header/footer/docs-nav) testli",
    "goals/docs-static.md hâlâ PASS (prerender bozulmadı)"
  ],
  "files_in_scope": ["apps/web/source.config.ts", "apps/web/lib/source.ts", "apps/web/app/(marketing)/blog/**", "apps/web/app/sitemap.ts", "apps/web/app/robots.ts", "apps/web/components/site-header.tsx", "apps/web/components/site-footer.tsx", "apps/web/lib/layout.shared.tsx", "ilgili testler"],
  "forbidden": ["docs koleksiyonuna dokunmak", "yeni bağımlılık (fumadocs zaten var)", "/r noindex'ine dokunmak", "Türkçe copy"]
}
```
**İşçi:** Opus 4.8 · **Hakem:** taze Opus 4.8.

### T-B2 — İlk 2 blog yazısı (dogfooding, NEVER#7 disiplini)

**İş emri (T-B1 sonrası):**
```json
{
  "task": "content/blog/ altına 2 GERÇEK yazı (İngilizce, MDX, frontmatter title/description(≤155char)/date). Yazı 1 'We ran SeoGrep on seogrep.com. Here is what it found.': 2026-07-20 bağımsız audit dogfooding'inin GERÇEK bulguları — 42/42 sayfada canonical yoktu, 0/42 JSON-LD, 5 sayfa aynı 178-karakter meta'yı paylaşıyordu; nasıl bulundu (crawl_site→audit_onpage→audit_schema çıktıları) ve nasıl düzeltildi (self-referencing canonical, sayfa-başı özgün meta, Organization+WebSite+SoftwareApplication JSON-LD). SAYILAR bu cümledeki kadar — uydurma metrik/trafik iddiası YOK (NEVER#7); 'rankings improved' SÖYLENMEZ (ölçmedik). Yazı 2 'Why SeoGrep is an MCP server, not another dashboard': konumlandırma — SEO verisinin agent'ının konuştuğu yerde (Claude/Cursor) yaşaması; 16 tool + kredi modeli kısa anlatım; docs'a link. Ton: docs'taki mevcut sade-teknik İngilizce ses. Her yazı 600-900 kelime. Kod değişikliği YOK (yalnız içerik).",
  "done_when": [
    "2 MDX dosyası build'de statik (make verify PASS)",
    "sitemap'te 2 blog URL'i (T-B1 testi otomatik yakalar)",
    "metin içinde $ fiyat rakamı YOK (metadata.test deseni ihlal edilmez), uydurma metrik YOK (hakem NEVER#7 kontrolü)",
    "meta description'lar ≤155 karakter"
  ],
  "files_in_scope": ["apps/web/content/blog/*.mdx"],
  "forbidden": ["kod dosyaları", "fiyat/kredi rakamı yazmak", "müşteri yorumu/logo/uydurma sosyal kanıt", "ölçülmemiş iddia"]
}
```
**İşçi:** Sonnet 5 (copy) · **Hakem:** taze Opus 4.8 (NEVER#7 odaklı).

### T-C1 — Launch içerik taslakları (şef yazar, insan yayınlar)

Şef `docs/launch/2026-07-launch-posts.md` yazar: Product Hunt (tagline+description+first-comment maker story) · Hacker News Show HN (başlık + metin — teknik, dürüst, "built with an agent-orchestration process" hikâyesi İNSAN KARARIYLA girer/girmez) · X/Twitter thread (5-7 post). Kurallar: uydurma metrik/kullanıcı sayısı YOK; fiyat rakamı yazılabilir (yayında insan onayı zaten var); tüm metin İngilizce; her taslağın yanına "insan doldurur" alanları (yayın tarihi, medya/screenshot seçimi). **Yayın butonu = İNSAN** (contract).

### T-C2 — MCP dizin başvuru metinleri (şef; gönderim insan)

Şef `docs/launch/2026-07-directory-submissions.md` yazar: Smithery · PulseMCP · mcp.so · Anthropic connector directory. Her biri için: gereksinim araştırması (şef, web) + hazır başvuru metni (name, one-liner, description, category, MCP endpoint şekli `https://mcp.seogrep.com/mcp/{key}` — kişisel-key modelinin dizinlerde nasıl temsil edileceği notu: "remote MCP, per-user URL after signup") + gönderim adımları. **Gönderim = İNSAN.**

### T-G1 — goals/purchase-flow-live.md (küçük, şef yazabilir)

`goals/landing-live.md` deseninde YENİ goal:
```
predicate:
[ -z "${PROD_URL:-}" ] && exit 0
[ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 -X POST "$PROD_URL/api/paddle/webhook" -H 'content-type: application/json' -d '{}')" = "401" ]
[ ! -f goals/evidence/purchase-flow-live.txt ] && exit 0
grep -q "txn_" goals/evidence/purchase-flow-live.txt
```
(2. satır fail-closed imza kapısının CANLI kanıtı — imzasız POST daima 401 dönmeli; evidence dosyası gelene dek kalanı SKIP; evidence geldiğinde txn ref'i doğrulanır. SKIP=faz-çıkışı DEĞİL — çıkış kriteri evidence'ın varlığını şart koşar.) on-violation: 401 değilse webhook env/secret bozuk (paddle-smoke.md troubleshooting); evidence yoksa A2 adım-5 bekleniyor. Şef commit'ler; hakem gerekmez (goal dosyası, kod değil) — final whole-branch review kapsar.

---

## İnsan kuyruğu (sıralı — kod bunları BEKLEMEZ, paralel yürür)

1. **Paddle LIVE onboarding** (dış süreç; A2 runbook'u rehber) → live key'ler/price'lar/webhook secret → Netlify env (değer chat'e YAPIŞTIRMADAN).
2. **Nihai fiyat oturumu** (NEVER#6): rakamlar aynen mi + rollover/E-I1 kararı. (Şef önerisi: beta rakamları AYNEN kalsın; rollover copy-soften Faz-4-sonrası.)
3. **Canlı smoke** (A2 adım-5, şef yönetir): $10 top-up + Replay-idempotency + evidence dosyası.
4. **UptimeRobot/Better Stack kurulumu** (~5 dk; monitoring.md §2) → `goals/evidence/uptime-monitor.txt` içeriğini şefe bildir ("kuruldu, monitor adı X" yeter — şef dosyayı yazar).
5. **MCP_SMOKE_URL** kalıcı set (ders L2 — yeni personal MCP URL; goals gerçek kapsama döner).
6. **DFS_LIVE launch kararı** (şef önerisi: launch'ta AÇ — amiral veri tool'u; açılırsa DB-sayaç görevi + kayıtlı-koşul gereği DFS credential konusu o an yeniden değerlendirilir; KAPALI kalırsa dürüst "not yet enabled" devam).
7. **Yayın butonları**: PH/HN/X gönderileri (T-C1 taslaklarından) + dizin gönderimleri (T-C2).
8. **repo PRIVATE** (GitHub billing) + **OAuth verification başvurusu** (haftalar sürer — launch'a yakın BAŞLAT) + Gmail "never spam" filtresi.
9. **Ders imzaları**: L1 + L2 CLAUDE.md'ye (şef metni hazır sunar).

## Riskler

| Risk | Önlem |
|---|---|
| T-S1 undici pin crawler testlerini destabilize eder | En riskli görev izole; 2×FAIL→dur+insan; davranış-değişmezliği done_when'de |
| Paddle live'da price-map env eksik → record_only döngüsü | A2 adım-3 keşif-kanıtlı uyarı + smoke adım-5 bunu yakalar |
| Blog boş koleksiyonla build kırılması | T-B1 done_when açık; B2'den bağımsız yeşil |
| Fiyat oturumu rakam değiştirirse | Ayrı imzalı görev (kod+docs+pricing üçlüsü); bu plan rakam DEĞİŞTİRMEZ |
| Context/oturum sınırı | Her görev bağımsız hakem+commit; PLAN.md+ledger her görevde güncellenir; %90'da handoff |

## Çıkış kriterleri (spec §9 + bu plan)

- `make goals` → **16/16** ve iki yeni goal'ün evidence dosyaları (`goals/evidence/uptime-monitor.txt` + `purchase-flow-live.txt`) MEVCUT — SKIP-yeşili faz-çıkışı SAYILMAZ (L2 dersi sınıfı).
- Canlı LIVE satın alma kanıtı ledger'da (tek satır + Replay-idempotent).
- mcp.seogrep.com harici monitor'de; reaper zamanlı; /status genişletilmiş.
- /blog canlı (2 gerçek yazı), sitemap docs+blog dahil.
- Dizin başvuruları GÖNDERİLMİŞ (insan) + PH/HN/X taslakları TESLİM (yayın insan takvimi).
